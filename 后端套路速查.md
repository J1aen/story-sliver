# 后端套路速查（怎么写不慌）

> 目的：遇到没写过的功能时，知道「先想什么、用什么方法、写在哪」。

## 1. 万能四步思考法

遇到任何功能，按顺序问自己四句：

1. **它接收什么？**（请求参数有哪些？谁调用它？）
2. **它要做什么？**（把需求拆成 3~8 个动作步骤，写下来）
3. **每一步用什么方法？**（查下面速查表，或翻做过的代码）
4. **怎么验证写对了？**（单元测试 / 启动后调接口看结果）

写代码 = 把第 2 步的每个动作，用第 3 步找到的方法翻译成 Java。

## 2. 「不知道用什么方法」速查表

| 我想做什么 | 用什么方法 | 例子 |
| --- | --- | --- |
| 随机数 | `ThreadLocalRandom.current().nextInt(1, 10)` | 随机两个加数 |
| 存键值对（key→value） | `Map` / `ConcurrentHashMap` | 验证码答案、限流记录 |
| 生成唯一标识 | `UUID.randomUUID().toString()` | 验证码 key |
| 字符串去首尾空格 | `str.trim()` | 用户名、碎片内容 |
| 判断字符串开头 | `str.startsWith("Bearer ")` | 解析 token |
| 密码加密 / 比对 | `passwordEncoder.encode(x)` / `matches(x, y)` | 注册 / 登录 |
| 签发登录令牌 | `jwtUtil.generateToken(userId, role)` | 登录后返回 token |
| 拿当前登录用户 | `UserContext.getUserId()` | 发布碎片、点赞 |
| 抛业务错误 | `throw new BusinessException(ResultCode.XXX)` | 用户名重复、验证码错误 |
| 校验请求参数 | DTO 字段上的 `@NotBlank` / `@Size` | 注册、发布请求体 |
| 查 / 写数据库 | Mapper 接口方法（注解 SQL 或 XML） | 查用户、插入碎片 |
| 分页查询 | `PageHelper.startPage(页码, 条数)` + 强转 `Page` | 列表接口 |
| 时间 | `LocalDateTime.now()`（或数据库自动填） | 创建时间 |
| 接收 JSON 请求体 | `@RequestBody` + DTO | 注册、发布 |
| 路径里的参数 | `@PathVariable Long id` | `/fragments/{id}/like` |
| 地址栏参数 | `@RequestParam int pageNum` | 分页参数 |

## 3. 实例拆解：注册接口（Task 5）

**需求**：用户填表单点注册。

**第 1 步 接收什么**：`RegisterRequest`（用户名、昵称、密码、验证码 key、验证码答案、是否管理员、管理员特殊密码）。

**第 2 步 要做什么**（按顺序）：

1. 验证码对不对 → `captchaService.verify(key, answer)`
2. IP 限流（防刷）→ `registerRateLimiter.isAllowed(ip)`
3. 密码至少 6 位 → `request.getPassword().length() < 6`
4. 用户名有没有被占用 → `userMapper.selectByUsername(username)` 是否 null
5. 勾选管理员时，特殊密码对不对 → `passwordEncoder.matches(code, 配置里的哈希)`
6. 密码加密 → `passwordEncoder.encode(password)`
7. 存进数据库 → `userMapper.insert(user)`
8. 返回登录令牌 → `jwtUtil.generateToken(user.getId(), role)`

**第 3 步 用到的类**：`CaptchaService`、`RegisterRateLimiter`、`UserMapper`、`UserServiceImpl`、`JwtUtil`。

**第 4 步 怎么验证**：写 `UserServiceTest`（模拟 Mapper），或用 IDEA 启动后调 `/api/auth/register`。

## 4. 遇到不会的怎么办

1. 先想「这个功能最像我做过的哪个」，翻那个代码抄结构（套路都一样，只是类名不同）
2. IDEA 里鼠标放在方法上按 `Ctrl+Q` 看说明、`Ctrl+P` 看参数
3. 报错先看**最后一行**英文，再复制给我
4. 卡住时把「我要做什么 + 我卡在哪」发我，我给提示，不给整段答案
