package com.storysliver.controller;

/**
 * 用户接口（Task 5，待实现）。
 * 待实现：
 *   @RestController @RequestMapping("/api/users")
 *   GET /api/users/me —— 返回当前用户（UserContext.getUserId() → UserService.me()）
 */
public class UserController {
}
