# 匿名故事碎片墙 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

> **工作约定（必须遵守）**：每完成一个任务或修复，**必须同步更新本文件**——勾选对应步骤、更新任务状态（✅ 已完成 / 🔧 进行中 / ⚠ 待修）、记录提交号，并和代码放在同一个 commit 一起提交推送。
>
> **代码先写后做（v1.2 起执行）**：每个新增功能/代码块，**先在本计划里写好完整代码 + 逐行中文注解**，注解要解释**每一行**在做什么、为什么这么写；确认后再写进项目代码，实现完成后打勾并记录提交号。

**Goal:** 实现一个可真实上线的匿名故事碎片墙：注册登录（可选匿名发布、管理员审核、点赞、隐藏、硬删除），部署到腾讯云学生机，`http://IP:8080` 直连可用。

**Architecture:** 前后端分离。后端 Spring Boot 3 + MyBatis + MySQL（现有 `fragment-story` 骨架扩展），提供 JWT 认证、角色权限（普通/管理员/站长）、碎片生命周期（待审核→已发布→已隐藏）与硬删除；前端 Vue3 + Vite + JavaScript 单页应用，Axios 调用 `/api`，打包后由 Spring Boot 单端口 8080 托管。

**Tech Stack:** Java 17、Spring Boot 3.3.5、MyBatis 3.0.4、MySQL 8、Lombok、jjwt 0.12.6、spring-security-crypto、JUnit5 + Mockito；Vue3、Vite、Vue Router 4、Axios、手写 CSS。

## Global Constraints

- 后端 JDK 17，Spring Boot `3.3.5`，MyBatis `3.0.4`，MySQL 8，Lombok（沿用现有 pom，仅新增 jjwt 与 spring-security-crypto）
- 密码一律 BCrypt 加密存储；认证用 JWT，`app.jwt.expire-days: 30`
- 碎片内容上限 **1000 字**；发布限频 **5 分钟/条**（按用户）
- 注册防滥用：算术验证码（会话绑定、一次性）+ IP 限流（1 小时 5 个 / 24 小时 10 个）+ 用户名唯一 + 密码最少 6 位
- 碎片状态：`0 待审核 / 1 已发布 / 2 已隐藏`；硬删除 = 删行并清理点赞记录，前端一律弹窗确认
- 角色：`0 普通 / 1 管理员 / 2 站长`；站长 = 第一个用管理员注册特殊密码注册成功的账号；只有站长能指定/撤销管理员、改注册密码
- 匿名：`is_anonymous=1` 对外显示「匿名用户」，数据库始终记录 `user_id`，管理员可见真实发布者
- 统一响应 `Result{code,message,data}`，沿用现有 `com.storysliver.common`
- 前端 Vue3 + JavaScript + Vue Router + Axios + 手写 CSS，风格「温暖文字流」（米白纸感、单列约 680px）
- 部署：`mvn package` + 前端 `npm run build`，Spring Boot 托管前端静态文件，单端口 8080；数据库 `story_sliver`
- 工作区目录名含特殊连字符，命令行一律使用相对路径
- 后端代码注释规范：**每个类、每个方法、每个关键语句都必须有详细中文注释**，说明「这是什么、干什么用的、为什么这么写」，禁止无注释的代码块（详见「代码注释规范」一节，执行阶段所有后端文件必须遵守）
- 后端代码格式对齐你之前项目（`D:\smis\schoolInformationManageSystem-java`，com.xxgcx 风格）：pojo 分包、`@Autowired` 字段注入、`Result{code,text,data}`、`PageBean{total,list}` + PageHelper 分页、`@Mapper` 注解 SQL（动态 SQL 用 XML）、`application.properties` 配置（详见「代码风格规范」一节，与任务示例冲突时以风格规范为准）

---

## File Structure

### 后端（fragment-story，在现有骨架上扩展）

| 文件 | 职责 |
| --- | --- |
| `src/main/resources/db/init.sql`（改） | 全量建表：user、story_fragment（加 user_id/is_anonymous）、fragment_like、system_config |
| `src/main/resources/application.properties`（替换 yml） | 配置文件（对齐 sims，用 properties） |
| `pojo/Result.java`、`PageBean.java` | 统一响应 `{code,text,data}`、分页 `{total,list}`（照抄 sims 写法） |
| `pojo/User.java`、`StoryFragment.java`（改）、`FragmentLike.java`、`SystemConfig.java` | 实体（pojo 根，对应表） |
| `pojo/Auth/RegisterRequest.java`、`LoginRequest.java`、`CaptchaResponse.java` | 认证请求/结果（按业务分包） |
| `pojo/Fragment/FragmentSubmitRequest.java`、`FragmentVO.java` | 碎片请求/结果 |
| `pojo/Admin/AdminRoleRequest.java`、`UpdateAdminCodeRequest.java` | 管理端请求 |
| `mapper/UserMapper.java`、`FragmentLikeMapper.java`、`SystemConfigMapper.java`；`StoryFragmentMapper.java`（改） | MyBatis 接口（@Mapper + 注解 SQL），动态 SQL 在 `resources/com/storysliver/mapper/*.xml` |
| `auth/JwtProperties.java`、`auth/JwtUtil.java`、`auth/UserContext.java`、`auth/AuthInterceptor.java`、`auth/RequireRole.java` | JWT 与鉴权 |
| `auth/CaptchaService.java`、`auth/RegisterRateLimiter.java`、`auth/FragmentRateLimiter.java` | 验证码与限流 |
| `config/WebConfig.java`（改）、`config/BeanConfig.java`（新增） | 拦截器注册、CORS、BCrypt Bean |
| `service/UserService.java`+impl、`FragmentService.java`+impl、`AdminService.java`+impl | 业务层（@Service + @Autowired 字段注入） |
| `controller/AuthController.java`、`UserController.java`、`FragmentController.java`、`AdminController.java` | 接口层（@RestController @Slf4j，返回 Result） |
| `common/ResultCode.java`（改） | 增加验证码/限流等错误码 |
| `src/test/java/com/storysliver/...` | 单元测试 |

### 前端（fragment‑story‑frontend，全部新建）

| 文件 | 职责 |
| --- | --- |
| `package.json`、`vite.config.js`、`index.html` | 脚手架与代理 |
| `src/main.js`、`src/App.vue`、`src/styles/main.css` | 入口、布局、全局样式 |
| `src/router/index.js` | 路由 + 守卫（/my、/admin 需登录；/admin 需管理员） |
| `src/api/request.js`、`auth.js`、`fragments.js`、`admin.js` | Axios 封装与接口 |
| `src/stores/user.js` | 用户状态（localStorage 持久化） |
| `src/views/HomeView.vue`、`LoginView.vue`、`MyFragmentsView.vue`、`AdminView.vue` | 四个页面 |
| `src/components/FragmentCard.vue`、`SubmitModal.vue`、`CaptchaField.vue`、`ConfirmDialog.vue`、`LoadMore.vue`、`EmptyState.vue` | 组件 |

---

## 代码注释规范（必须遵守）

后端所有 Java 代码（含测试类）、Mapper XML、SQL、配置文件都必须有**详细中文注释**，遵循三条原则：

1. **这是什么**：类 / 接口 / 字段说明它对应什么、属于哪一层
2. **干什么用的**：方法说明它的职责、参数含义、返回值、可能抛出的异常
3. **为什么这么写**：关键语句说明设计原因，例如「为什么用线程安全集合」「为什么先校验再写库」「为什么硬删除前先查询」
4. **逐行粒度**：除了说明「这一段/这个方法干什么用」，还要对**每一行**写注解——这一行在做什么、为什么这么写（如「这里先判 null 再取 length，防止空指针」）

规范示例（以下所有后端代码块均按此风格注释）：

```java
/**
 * 用户实体：对应 user 表。
 * 干什么用：承载注册/登录用户的全部字段，在 Mapper、Service、Controller 之间传递。
 * 为什么这么设计：字段与表结构一一对应，驼峰命名由 MyBatis map-underscore-to-camel-case 自动映射。
 */
@Data
public class User {
    /** 角色常量：0 普通用户（默认）、1 管理员、2 站长。用 int 存库简单、可扩展 */
    public static final int ROLE_USER = 0, ROLE_ADMIN = 1, ROLE_OWNER = 2;
    /** 用户状态：0 正常、1 封禁（预留） */
    public static final int STATUS_NORMAL = 0, STATUS_BANNED = 1;
}
```

```java
/**
 * 注册业务实现。
 * 干什么用：完成注册全流程（验证码校验 → IP 限流 → 参数校验 → 唯一性校验 → 角色判定 → 落库 → 签发 token）。
 * 为什么这么设计：把「校验」集中在 Service 层，Controller 保持轻薄，业务规则可独立单测。
 */
@Service
@RequiredArgsConstructor
public class UserServiceImpl implements UserService {

    /**
     * 注册用户。
     * @param request 注册表单（用户名/昵称/密码/验证码/管理员选项）
     * @param ip 客户端 IP，用于注册限流
     * @return JWT token（注册成功即自动登录）
     * @throws BusinessException 验证码错误 / 限流 / 用户名重复 / 管理员密码错误等
     */
    @Override
    public String register(RegisterRequest request, String ip) {
        // 先校验验证码：一次性使用（verify 内部 remove），防止脚本重放
        if (!captchaService.verify(request.getCaptchaKey(), request.getCaptchaAnswer())) {
            throw new BusinessException(ResultCode.CAPTCHA_ERROR);
        }
        // 再限流：验证码挡脚本，IP 限流兜底，双保险
        if (!registerRateLimiter.isAllowed(ip)) {
            throw new BusinessException(ResultCode.REGISTER_TOO_FREQUENT);
        }
        // 密码强度是最基本的防线：至少 6 位
        if (request.getPassword() == null || request.getPassword().length() < 6) {
            throw new BusinessException(ResultCode.PASSWORD_TOO_WEAK);
        }
        // 用户名唯一是登录体系的前提，冲突直接拒绝
        if (userMapper.selectByUsername(request.getUsername()) != null) {
            throw new BusinessException(ResultCode.USERNAME_TAKEN);
        }
        // 管理员注册需要特殊密码，密码只存 BCrypt 哈希，绝不存明文
        int role = User.ROLE_USER;
        if (Boolean.TRUE.equals(request.getIsAdmin())) {
            SystemConfig cfg = systemConfigMapper.selectByKey(SystemConfig.KEY_ADMIN_REGISTER_CODE);
            if (cfg == null || request.getAdminCode() == null
                    || !passwordEncoder.matches(request.getAdminCode(), cfg.getConfigValue())) {
                throw new BusinessException(ResultCode.ADMIN_CODE_WRONG);
            }
            // 第一个用特殊密码注册的管理员自动成为站长；之后都是普通管理员
            role = userMapper.countAll() == 0 ? User.ROLE_OWNER : User.ROLE_ADMIN;
        }
        // 密码加密后落库，任何人（包括管理员）都看不到明文
        User user = new User();
        user.setUsername(request.getUsername().trim());
        user.setNickname(request.getNickname().trim());
        user.setPassword(passwordEncoder.encode(request.getPassword()));
        user.setRole(role);
        user.setStatus(User.STATUS_NORMAL);
        userMapper.insert(user);
        // 注册成功直接返回 token，省去「注册后再登录」一步，提升体验
        return jwtUtil.generateToken(user.getId(), user.getRole());
    }
}
```

---

## 代码风格规范（对齐 sims 项目）

来源：`D:\smis\schoolInformationManageSystem-java`（com.xxgcx 包）。**本节是本项目后端代码的最终格式标准**——后续任务示例若与本节冲突（如 dto/entity 包名、`@RequiredArgsConstructor`、`PageResult`），一律按本节调整。

### 包与分层

```text
com.storysliver
├── controller/        # 接口层：@RestController @Slf4j，方法返回 Result
├── service/           # 业务接口
│   └── impl/          # 业务实现：@Service + @Autowired 字段注入
├── mapper/            # MyBatis：@Mapper + 注解 SQL（动态 SQL 用 XML）
├── pojo/              # 实体 + 请求/结果对象
│   ├── Result.java    # 统一响应 {code, text, data}
│   ├── PageBean.java  # 分页包装 {total, list}
│   ├── User.java / StoryFragment.java / FragmentLike.java / SystemConfig.java  # 实体（pojo 根）
│   ├── Auth/          # 注册/登录/验证码 请求与结果
│   ├── Fragment/      # 碎片 请求与结果
│   └── Admin/         # 管理端 请求与结果
├── config/            # CORS、拦截器注册
├── auth/              # JWT、验证码、限流
└── common/            # 错误码、业务异常、全局异常处理
```

### 实体写法

```java
/**
 * 用户实体：对应 user 表。
 * 干什么用：承载注册/登录用户的全部字段，在 Mapper、Service、Controller 之间传递。
 */
@Data
@AllArgsConstructor
@NoArgsConstructor
public class User {
    /** 角色常量：0 普通用户（默认）、1 管理员、2 站长 */
    public static final int ROLE_USER = 0, ROLE_ADMIN = 1, ROLE_OWNER = 2;
    private Long id;//主键
    private String username;//登录用户名（唯一）
    private String nickname;//展示昵称
    private String password;//BCrypt 密码哈希
    private String email;//预留邮箱（暂不验证）
    private Integer role;//角色：0普通 1管理员 2站长
    private Integer status;//状态：0正常 1封禁（预留）
    private LocalDateTime createdAt;//创建时间
    private LocalDateTime updatedAt;//更新时间
}
```

### 统一响应与分页（照抄 sims 的 Result / PageBean）

```java
@Data
@NoArgsConstructor
@AllArgsConstructor
public class Result {
    private Integer code;//响应码，200 成功
    private String text;//响应信息描述
    private Object data;//返回的数据

    /** 增删改成功响应 */
    public static Result success() {
        return new Result(200, "success", null);
    }

    /** 查询成功响应 */
    public static Result success(Object data) {
        return new Result(200, "success", data);
    }

    /** 失败响应 */
    public static Result error(String text) {
        return new Result(400, text, null);
    }

    /** 指定错误码的失败响应 */
    public static Result error(Integer code, String text) {
        return new Result(code, text, null);
    }
}

@Data
@NoArgsConstructor
@AllArgsConstructor
public class PageBean {
    private Long total;//总记录数
    private List list;//数据列表
}
```

### Controller 写法

```java
@RestController
@Slf4j
@RequestMapping("/api/fragments")
public class FragmentController {
    @Autowired
    private FragmentService fragmentService;

    /**发布碎片：提交后进入待审核*/
    @PostMapping
    public Result submit(@RequestBody FragmentSubmitRequest request) {
        fragmentService.submit(UserContext.getUserId(), request.getContent(),
                Boolean.TRUE.equals(request.getIsAnonymous()));
        return Result.success();
    }
}
```

### Service 写法（接口 + impl）

```java
public interface FragmentService {
    /**发布碎片（进入待审核）*/
    void submit(Long userId, String content, boolean anonymous);
}

@Service
public class FragmentServiceImpl implements FragmentService {
    @Autowired
    private StoryFragmentMapper fragmentMapper;

    /**发布碎片：先校验内容长度，再校验 5 分钟限频，最后落库*/
    @Override
    public void submit(Long userId, String content, boolean anonymous) {
        //内容先 trim 再判空，防止纯空格提交
        String text = content == null ? "" : content.trim();
        if (text.isEmpty() || text.length() > 1000) {
            throw new BusinessException(ResultCode.FRAGMENT_TOO_LONG);
        }
        //发布频率限制：每个用户 5 分钟 1 条
        if (!fragmentRateLimiter.tryAcquire(userId)) {
            throw new BusinessException(ResultCode.FRAGMENT_TOO_FREQUENT);
        }
        StoryFragment fragment = new StoryFragment();
        fragment.setUserId(userId);
        fragment.setContent(text);
        fragment.setIsAnonymous(anonymous ? 1 : 0);
        fragmentMapper.insert(fragment);
    }
}
```

### Mapper 写法（@Mapper + 注解 SQL，动态 SQL 用 XML）

```java
@Mapper
public interface UserMapper {
    /**按用户名查询：登录和注册唯一性校验用*/
    @Select("select id, username, nickname, password, email, role, status from `user` where username = #{username}")
    User selectByUsername(String username);

    /**分页查询用户（管理端用户列表，分页由 PageHelper 注入）*/
    List<User> selectPage();
}
```

动态 SQL 放 `resources/com/storysliver/mapper/UserMapper.xml`：

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.storysliver.mapper.UserMapper">
    <!--分页查询用户，按 id 升序-->
    <select id="selectPage" resultType="com.storysliver.pojo.User">
        select id, username, nickname, password, email, role, status
        from `user`
        order by id
        limit #{offset}, #{limit}
    </select>
</mapper>
```

### 分页用法（PageHelper）

```java
//Service 里查询前先 startPage，查询后强转 Page 拿到 total 和列表
PageHelper.startPage(pageNum, pageSize);
List<FragmentVO> list = fragmentMapper.selectPublishedPage();
Page<FragmentVO> page = (Page<FragmentVO>) list;
return new PageBean(page.getTotal(), page.getResult());
```

### 配置写法（application.properties）

```properties
spring.application.name=fragment-story
server.port=8080
# MySQL 数据库配置
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/story_sliver?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
# 下划线字段自动转驼峰
mybatis.configuration.map-underscore-to-camel-case=true
```

### 其他约定

- 依赖注入一律 `@Autowired` 字段注入（与 sims 一致；示例中若出现 `@RequiredArgsConstructor` 构造器注入，按本节改为字段注入）
- 分页一律用 PageHelper + PageBean，不再用 `PageResult`
- 统一响应用 sims 风格 `Result`：`Result.success(data)` / `Result.error(text)`；`Result` **不是泛型**、**没有 `ok()` 方法**——任务示例中出现的 `Result.ok(...)`、`Result<T>` 一律按此替换
- 简单 SQL 用注解，动态 SQL（`<where>`/`<if>`/`<set>`）用 XML
- 所有注释用中文，字段行内 `//`、方法 `/** */`、逻辑步骤 `//` 说明

---

## 阶段 1：账号与认证（后端）

### Task 1: 数据库初始化脚本 —— ✅ 已完成（2026-08-06，含 git 提交 c9e1474）

**涉及文件：**
- `fragment-story/src/main/resources/db/init.sql`——建库建表脚本（4 张表）
- `fragment-story/src/main/resources/application.properties`——数据库连接、JWT 密钥、管理员初始密码（替代原 application.yml）

**Files:**
- Modify: `fragment-story/src/main/resources/db/init.sql`

**Interfaces:**
- Produces: 表 `user`、`story_fragment`（含 user_id、is_anonymous）、`fragment_like`、`system_config`，字段与 Global Constraints 一致

- [x] **Step 1: 重写 init.sql**

```sql
-- ============================================================
-- 匿名故事碎片墙数据库初始化脚本
-- 干什么用：一次性创建全部表结构与索引（开发期直接重建，上线后勿重复执行）
-- 为什么用 utf8mb4：完整支持中文与 emoji，避免生僻字/表情存不进去
-- ============================================================
CREATE DATABASE IF NOT EXISTS story_sliver DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE story_sliver;

DROP TABLE IF EXISTS fragment_like;
DROP TABLE IF EXISTS story_fragment;
DROP TABLE IF EXISTS system_config;
DROP TABLE IF EXISTS `user`;

CREATE TABLE `user` (
  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
  username VARCHAR(32) NOT NULL COMMENT '登录用户名',
  nickname VARCHAR(32) NOT NULL COMMENT '展示昵称',
  password VARCHAR(100) NOT NULL COMMENT 'BCrypt 哈希',
  email VARCHAR(100) DEFAULT NULL COMMENT '预留邮箱',
  role TINYINT NOT NULL DEFAULT 0 COMMENT '0普通 1管理员 2站长',
  status TINYINT NOT NULL DEFAULT 0 COMMENT '0正常 1封禁',
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  UNIQUE KEY uk_username (username)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户';

CREATE TABLE story_fragment (
  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
  user_id BIGINT UNSIGNED NOT NULL COMMENT '真实发布者',
  content TEXT NOT NULL COMMENT '内容(<=1000字)',
  like_count INT NOT NULL DEFAULT 0 COMMENT '点赞数',
  is_anonymous TINYINT NOT NULL DEFAULT 0 COMMENT '0显示昵称 1匿名',
  status TINYINT NOT NULL DEFAULT 0 COMMENT '0待审核 1已发布 2已隐藏',
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  KEY idx_user (user_id),
  KEY idx_status_created (status, created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='故事碎片';

CREATE TABLE fragment_like (
  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  fragment_id BIGINT UNSIGNED NOT NULL,
  user_id BIGINT UNSIGNED NOT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  UNIQUE KEY uk_fragment_user (fragment_id, user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='点赞';

CREATE TABLE system_config (
  config_key VARCHAR(50) NOT NULL,
  config_value VARCHAR(255) NOT NULL,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (config_key)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统配置';
```

- [x] **Step 2: 验证 SQL**

Run: `mysql -uroot -p < src/main/resources/db/init.sql`
Expected: 四张表创建成功，无报错。

- [x] **Step 3: 改用 application.properties（对齐 sims 风格）**

删除 `application.yml`，新建 `fragment-story/src/main/resources/application.properties`：

```properties
spring.application.name=fragment-story
server.port=8080

# MySQL 数据库配置
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/story_sliver?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root

# 下划线字段自动转驼峰
mybatis.configuration.map-underscore-to-camel-case=true
# Mapper XML 位置（与包路径一致）
mybatis.mapper-locations=classpath:com/storysliver/mapper/*.xml

# JWT 签名密钥：生产环境必须改为随机长字符串（>=32 字节）
app.jwt.secret=change-me-to-a-long-random-secret-at-least-32-bytes-long
# token 有效期（天），到期需重新登录
app.jwt.expire-days=30
# 初始管理员注册密码：启动时加密写入 system_config，站长可随时在管理页修改
app.admin.default-register-code=<管理员注册密码>
```

- [x] **Step 4: 提交**（已并入 init 提交 c9e1474）

```bash
git add src/main/resources/db/init.sql src/main/resources/application.properties
git commit -m "feat: 数据库脚本与配置"
```

### Task 2: 实体与 Mapper —— ✅ 已完成（2026-08-06，含 git 提交 c9e1474）

**涉及文件：**
- `fragment-story/src/main/java/com/storysliver/pojo/Result.java`、`PageBean.java`——统一响应、分页包装
- `fragment-story/src/main/java/com/storysliver/pojo/User.java`、`StoryFragment.java`、`FragmentLike.java`、`SystemConfig.java`——实体（对应 4 张表）
- `fragment-story/src/main/java/com/storysliver/mapper/UserMapper.java`、`StoryFragmentMapper.java`、`FragmentLikeMapper.java`、`SystemConfigMapper.java`——Mapper 接口
- `fragment-story/src/main/resources/com/storysliver/mapper/StoryFragmentMapper.xml`、`FragmentLikeMapper.xml`——动态 SQL
- `fragment-story/src/main/java/com/storysliver/common/GlobalExceptionHandler.java`——改用 `pojo.Result` 统一异常返回

**Files:**
- Create: `fragment-story/src/main/java/com/storysliver/entity/User.java`
- Create: `fragment-story/src/main/java/com/storysliver/entity/FragmentLike.java`
- Create: `fragment-story/src/main/java/com/storysliver/entity/SystemConfig.java`
- Modify: `fragment-story/src/main/java/com/storysliver/entity/StoryFragment.java`
- Create: `fragment-story/src/main/java/com/storysliver/mapper/UserMapper.java`
- Create: `fragment-story/src/main/java/com/storysliver/mapper/FragmentLikeMapper.java`
- Create: `fragment-story/src/main/java/com/storysliver/mapper/SystemConfigMapper.java`
- Modify: `fragment-story/src/main/java/com/storysliver/mapper/StoryFragmentMapper.java`
- Create: `fragment-story/src/main/resources/com/storysliver/mapper/UserMapper.xml`
- Create: `fragment-story/src/main/resources/com/storysliver/mapper/FragmentLikeMapper.xml`
- Create: `fragment-story/src/main/resources/com/storysliver/mapper/SystemConfigMapper.xml`
- Modify: `fragment-story/src/main/resources/com/storysliver/mapper/StoryFragmentMapper.xml`

**Interfaces:**
- Produces:
  - `UserMapper.insert(User)`、`selectByUsername(String)`、`selectById(Long)`、`selectPage()`（分页由 PageHelper 注入）、`countAll()`、`updateRole(Long,Integer)`
  - `FragmentLikeMapper.insert(FragmentLike)`、`deleteByFragmentAndUser(Long,Long)`、`exists(Long,Long)`、`selectLikedFragmentIds(Long)`
  - `SystemConfigMapper.selectByKey(String)`、`upsert(String,String)`
  - `StoryFragmentMapper.insert(StoryFragment)`、`selectById(Long)`、`selectPublishedPage()`（分页由 PageHelper 注入）、`selectByUser(Long)`、`selectManagePage(Integer)`（分页由 PageHelper 注入）、`updateStatus(Long,Integer)`、`deleteById(Long)`、`increaseLikeCount(Long)`、`decreaseLikeCount(Long)`

- [x] **Step 1: 实体**

`User.java`：

```java
/**
 * 用户实体：对应 user 表。
 * 干什么用：承载注册/登录用户的全部字段，在 Mapper、Service、Controller 之间传递。
 * 为什么这么设计：字段与表结构一一对应，驼峰命名由 MyBatis 自动映射。
 */
@Data
public class User {
    /** 角色常量：0 普通用户（默认）、1 管理员、2 站长。用 int 存库简单、可扩展 */
    public static final int ROLE_USER = 0, ROLE_ADMIN = 1, ROLE_OWNER = 2;
    /** 用户状态：0 正常、1 封禁（预留，后续做封号功能时使用） */
    public static final int STATUS_NORMAL = 0, STATUS_BANNED = 1;
    /** 主键，数据库自增 */
    private Long id;
    /** 登录用户名，唯一；注册时校验不重复 */
    private String username;
    /** 展示昵称，非匿名碎片对外显示的名字 */
    private String nickname;
    /** 密码的 BCrypt 哈希，绝不存明文 */
    private String password;
    /** 预留邮箱字段：现阶段不验证也不收集，为将来 QQ 邮箱验证留位置 */
    private String email;
    /** 角色：0 普通 / 1 管理员 / 2 站长 */
    private Integer role;
    /** 状态：0 正常 / 1 封禁（预留） */
    private Integer status;
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime createdAt;
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime updatedAt;
}
```

`StoryFragment.java` 增加字段与状态常量（保留原 id/content/likeCount/status/createdAt/updatedAt）：

```java
/**
 * 故事碎片实体：对应 story_fragment 表。
 * 干什么用：承载用户发布的故事/生活碎片及其生命周期状态。
 * 为什么这么设计：user_id 与 is_anonymous 分离——匿名只影响展示，数据库始终记录真实发布者，管理员可追溯。
 */
public class StoryFragment {
    /** 状态常量：0 待审核（提交后默认）、1 已发布（管理员通过后）、2 已隐藏（作者隐藏，可恢复） */
    public static final int STATUS_PENDING = 0, STATUS_PUBLISHED = 1, STATUS_HIDDEN = 2;
    /** 主键 */
    private Long id;
    /** 真实发布者 id：匿名只影响对外展示，管理员始终能看到发布者 */
    private Long userId;
    /** 碎片内容，1000 字封顶 */
    private String content;
    /** 点赞数（冗余计数：每次点赞 +1，避免每次展示都 COUNT 一次） */
    private Integer likeCount;
    /** 是否匿名：0 显示昵称 / 1 显示「匿名用户」 */
    private Integer isAnonymous;
    /** 生命周期状态：0 待审核 / 1 已发布 / 2 已隐藏 */
    private Integer status;
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime createdAt;
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime updatedAt;
}
```

`FragmentLike.java`、`SystemConfig.java`：

```java
/**
 * 点赞实体：对应 fragment_like 表。
 * 干什么用：记录「哪个用户赞过哪条碎片」，支撑每人每条一次、可取消的点赞规则。
 * 为什么这么设计：唯一约束 (fragment_id, user_id) 从数据库层面保证不能重复点赞，比应用层判断更可靠。
 */
@Data
public class FragmentLike {
    /** 主键 */
    private Long id;
    /** 被点赞的碎片 id */
    private Long fragmentId;
    /** 点赞用户 id */
    private Long userId;
    /** 点赞时间 */
    private LocalDateTime createdAt;
}

/**
 * 系统配置实体：对应 system_config 表。
 * 干什么用：存放站长可在线修改的配置，目前只有「管理员注册特殊密码」。
 * 为什么这么设计：放数据库而不是配置文件，站长在管理页改完立即生效，不用改配置重启服务。
 */
@Data
public class SystemConfig {
    /** 配置键常量：管理员注册特殊密码（存 BCrypt 哈希） */
    public static final String KEY_ADMIN_REGISTER_CODE = "admin_register_code";
    /** 配置键（主键） */
    private String configKey;
    /** 配置值：这里存哈希后的管理员注册密码 */
    private String configValue;
    /** 更新时间 */
    private LocalDateTime updatedAt;
}
```

- [x] **Step 2: Mapper 接口**

```java
/**
 * 用户 Mapper：SQL 见 resources/com/storysliver/mapper/UserMapper.xml。
 * 干什么用：封装 user 表的增查改，供注册、登录、用户管理使用。
 */
public interface UserMapper {
    /** 新增用户；useGeneratedKeys 会把自增 id 回填到 user.id */
    int insert(User user);
    /** 按用户名精确查询：注册唯一性校验、登录都用它 */
    User selectByUsername(@Param("username") String username);
    /** 按主键查询：我的信息、管理端查看发布者等场景 */
    User selectById(@Param("id") Long id);
    /** 分页查询用户（管理端用户列表，分页由 PageHelper 注入） */
    List<User> selectPage();
    /** 用户总数：分页 total 与「第一个注册管理员=站长」判定 */
    long countAll();
    /** 修改角色：站长指定/撤销管理员时使用 */
    int updateRole(@Param("id") Long id, @Param("role") Integer role);
}

/**
 * 点赞 Mapper：SQL 见 resources/com/storysliver/mapper/FragmentLikeMapper.xml。
 * 干什么用：点赞/取消点赞的增删查。
 */
public interface FragmentLikeMapper {
    /** 插入一条点赞记录 */
    int insert(FragmentLike like);
    /** 删除点赞：userId 传 null 时删除该碎片全部点赞（配合硬删除清理） */
    int deleteByFragmentAndUser(@Param("fragmentId") Long fragmentId, @Param("userId") Long userId);
    /** 判断某用户是否已赞过某碎片（点赞幂等校验） */
    boolean exists(@Param("fragmentId") Long fragmentId, @Param("userId") Long userId);
    /** 查询某用户赞过的全部碎片 id：首页列表用于标记「已赞」状态 */
    List<Long> selectLikedFragmentIds(@Param("userId") Long userId);
}

/**
 * 系统配置 Mapper：SQL 见 resources/com/storysliver/mapper/SystemConfigMapper.xml。
 */
public interface SystemConfigMapper {
    /** 按配置键读取；键不存在返回 null */
    SystemConfig selectByKey(@Param("configKey") String configKey);
    /** 插入或更新（MySQL ON DUPLICATE KEY UPDATE），站长改配置用 */
    int upsert(@Param("configKey") String configKey, @Param("configValue") String configValue);
}
```

`StoryFragmentMapper.java` 追加（删除旧方法）：

```java
/**
 * 碎片 Mapper：SQL 见 resources/com/storysliver/mapper/StoryFragmentMapper.xml。
 * 干什么用：碎片生命周期（待审核/已发布/已隐藏）、分页列表、点赞计数、硬删除。
 */
public interface StoryFragmentMapper {
    /** 新增碎片：默认 status=0（待审核）、like_count=0 */
    int insert(StoryFragment fragment);
    /** 按主键查询：点赞/隐藏/删除前的状态与归属校验 */
    StoryFragment selectById(@Param("id") Long id);
    /** 首页分页：只查已发布，按时间倒序；分页由 PageHelper 注入，不传 offset/limit */
    List<StoryFragment> selectPublishedPage();
    /** 我的碎片：按用户查全部状态（待审核/已发布/已隐藏） */
    List<StoryFragment> selectByUser(@Param("userId") Long userId);
    /** 管理列表：status 传 null 查全部，传 0/1 按状态筛选；分页由 PageHelper 注入 */
    List<StoryFragment> selectManagePage(@Param("status") Integer status);
    /** 状态流转：审核通过（0→1）、隐藏（1→2）、取消隐藏（2→1） */
    int updateStatus(@Param("id") Long id, @Param("status") Integer status);
    /** 硬删除：物理删行（前端已弹窗确认不可恢复） */
    int deleteById(@Param("id") Long id);
    /** 点赞数 +1 */
    int increaseLikeCount(@Param("id") Long id);
    /** 点赞数 -1（GREATEST 保证不小于 0） */
    int decreaseLikeCount(@Param("id") Long id);
}
```

- [x] **Step 3: Mapper XML**

`StoryFragmentMapper.xml` 完整内容：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 碎片表 Mapper：生命周期查询/分页列表/点赞计数/硬删除 -->
<mapper namespace="com.storysliver.mapper.StoryFragmentMapper">
    <resultMap id="BaseResultMap" type="com.storysliver.entity.StoryFragment">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="likeCount" column="like_count"/>
        <result property="isAnonymous" column="is_anonymous"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 其余语句（insert/selectById/selectPublishedPage/selectByUser/updateStatus/deleteById/点赞计数）
         都用 Mapper 接口上的 @Insert/@Select/@Update/@Delete 注解实现，XML 只放动态 SQL -->
    <select id="selectManagePage" resultMap="BaseResultMap">
        SELECT id, user_id, content, like_count, is_anonymous, status, created_at, updated_at
        FROM story_fragment
        <where>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY created_at DESC, id DESC
    </select>

    <delete id="deleteById">
        DELETE FROM story_fragment WHERE id = #{id}
    </delete>
</mapper>
```

`UserMapper.xml`、`FragmentLikeMapper.xml`、`SystemConfigMapper.xml`：

```xml
<!-- UserMapper.xml -->
<mapper namespace="com.storysliver.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.storysliver.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="nickname" column="nickname"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, nickname, password, email, role, status)
        VALUES (#{username}, #{nickname}, #{password}, #{email}, #{role}, #{status})
    </insert>
    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT id, username, nickname, password, email, role, status, created_at, updated_at
        FROM `user` WHERE username = #{username}
    </select>
    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, username, nickname, password, email, role, status, created_at, updated_at
        FROM `user` WHERE id = #{id}
    </select>
    <select id="selectPage" resultMap="BaseResultMap">
        SELECT id, username, nickname, password, email, role, status, created_at, updated_at
        FROM `user` ORDER BY id ASC LIMIT #{offset}, #{limit}
    </select>
    <select id="countAll" resultType="long">SELECT COUNT(*) FROM `user`</select>
    <update id="updateRole">UPDATE `user` SET role = #{role} WHERE id = #{id}</update>
</mapper>

<!-- FragmentLikeMapper.xml -->
<mapper namespace="com.storysliver.mapper.FragmentLikeMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO fragment_like (fragment_id, user_id) VALUES (#{fragmentId}, #{userId})
    </insert>
    <delete id="deleteByFragmentAndUser">
        DELETE FROM fragment_like WHERE fragment_id = #{fragmentId} AND user_id = #{userId}
    </delete>
    <select id="exists" resultType="boolean">
        SELECT COUNT(*) > 0 FROM fragment_like WHERE fragment_id = #{fragmentId} AND user_id = #{userId}
    </select>
    <select id="selectLikedFragmentIds" resultType="long">
        SELECT fragment_id FROM fragment_like WHERE user_id = #{userId}
    </select>
</mapper>

<!-- SystemConfigMapper.xml -->
<mapper namespace="com.storysliver.mapper.SystemConfigMapper">
    <select id="selectByKey" resultType="com.storysliver.entity.SystemConfig">
        SELECT config_key AS configKey, config_value AS configValue, updated_at AS updatedAt
        FROM system_config WHERE config_key = #{configKey}
    </select>
    <insert id="upsert">
        INSERT INTO system_config (config_key, config_value) VALUES (#{configKey}, #{configValue})
        ON DUPLICATE KEY UPDATE config_value = #{configValue}
    </insert>
</mapper>
```

- [x] **Step 4: 验证编译**

Run: `mvn -q -DskipTests compile -f fragment-story/pom.xml`
Expected: 编译通过。

- [x] **Step 5: 提交**（已并入 init 提交 c9e1474）

```bash
git add fragment-story/src/main
git commit -m "feat: 实体与 Mapper"
```

### Task 3: JWT 工具、用户上下文与鉴权拦截器 —— ✅ 已完成（2026-08-06）

**涉及文件：**
- `fragment-story/pom.xml`——新增 jjwt-api / jjwt-impl / jjwt-jackson 依赖
- `fragment-story/src/main/java/com/storysliver/auth/JwtProperties.java`——JWT 配置（密钥、有效期）
- `fragment-story/src/main/java/com/storysliver/auth/JwtUtil.java`——签发 / 解析 token
- `fragment-story/src/main/java/com/storysliver/auth/UserContext.java`——当前登录用户上下文（ThreadLocal）
- `fragment-story/src/main/java/com/storysliver/auth/RequireRole.java`——角色校验注解
- `fragment-story/src/main/java/com/storysliver/auth/AuthInterceptor.java`——JWT 校验 + 角色校验拦截器
- `fragment-story/src/main/java/com/storysliver/config/WebConfig.java`——注册拦截器、排除公开接口
- `fragment-story/src/test/java/com/storysliver/auth/JwtUtilTest.java`、`AuthInterceptorTest.java`——单元测试

**Files:**
- Modify: `fragment-story/pom.xml`（新增 jjwt-api / jjwt-impl / jjwt-jackson）
- Create: `fragment-story/src/main/java/com/storysliver/auth/JwtProperties.java`
- Create: `fragment-story/src/main/java/com/storysliver/auth/JwtUtil.java`
- Create: `fragment-story/src/main/java/com/storysliver/auth/UserContext.java`
- Create: `fragment-story/src/main/java/com/storysliver/auth/RequireRole.java`
- Create: `fragment-story/src/main/java/com/storysliver/auth/AuthInterceptor.java`
- Modify: `fragment-story/src/main/java/com/storysliver/config/WebConfig.java`
- Test: `fragment-story/src/test/java/com/storysliver/auth/JwtUtilTest.java`
- Test: `fragment-story/src/test/java/com/storysliver/auth/AuthInterceptorTest.java`

**Interfaces:**
- Consumes: 无
- Produces: `JwtUtil.generateToken(Long userId, Integer role): String`；`JwtUtil.parse(String): Claims`；`UserContext.getUserId(): Long`、`getRole(): Integer`、`clear()`；`@RequireRole(int[] value)`；`AuthInterceptor`（/api/** 需有效 JWT，@RequireRole 校验角色）

- [x] **Step 1: pom 增加依赖**

```xml
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-api</artifactId>
    <version>0.12.6</version>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-impl</artifactId>
    <version>0.12.6</version>
    <scope>runtime</scope>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-jackson</artifactId>
    <version>0.12.6</version>
    <scope>runtime</scope>
</dependency>
```

- [x] **Step 2: 写失败测试**

`JwtUtilTest.java`：

```java
/** JWT 工具单元测试：验证 token 签发/解析往返一致、篡改 token 必须被拒绝 */
class JwtUtilTest {
    private JwtUtil jwtUtil;

    @BeforeEach
    void setUp() {
        JwtProperties props = new JwtProperties();
        props.setSecret("test-secret-test-secret-test-secret-123456");
        props.setExpireDays(30);
        jwtUtil = new JwtUtil(props);
    }

    @Test
    void token_round_trip_keeps_subject_and_role() {
        String token = jwtUtil.generateToken(42L, 1);
        Claims claims = jwtUtil.parse(token);
        assertEquals("42", claims.getSubject());
        assertEquals(1, claims.get("role", Integer.class));
    }

    @Test
    void tampered_token_throws() {
        String token = jwtUtil.generateToken(1L, 0) + "x";
        assertThrows(JwtException.class, () -> jwtUtil.parse(token));
    }
}
```

`AuthInterceptorTest.java`：

```java
/** 认证拦截器单元测试：有效 token 放行并写入 UserContext、缺失 token 返回 401 */
class AuthInterceptorTest {
    private AuthInterceptor interceptor;
    private JwtUtil jwtUtil;
    private MockHttpServletRequest request;
    private MockHttpServletResponse response;

    @BeforeEach
    void setUp() {
        JwtProperties props = new JwtProperties();
        props.setSecret("test-secret-test-secret-test-secret-123456");
        props.setExpireDays(30);
        jwtUtil = new JwtUtil(props);
        interceptor = new AuthInterceptor(jwtUtil);
        request = new MockHttpServletRequest();
        response = new MockHttpServletResponse();
    }

    @Test
    void valid_token_passes_and_sets_context() throws Exception {
        request.addHeader("Authorization", "Bearer " + jwtUtil.generateToken(7L, 2));
        assertTrue(interceptor.preHandle(request, response, new Object()));
        assertEquals(7L, UserContext.getUserId());
        assertEquals(2, UserContext.getRole());
        interceptor.afterCompletion(request, response, new Object(), null);
        assertNull(UserContext.getUserId());
    }

    @Test
    void missing_token_returns_401() throws Exception {
        assertFalse(interceptor.preHandle(request, response, new Object()));
        assertEquals(401, response.getStatus());
    }
}
```

- [x] **Step 3: 运行测试确认失败**

Run: `mvn -q test -f fragment-story/pom.xml -Dtest=JwtUtilTest,AuthInterceptorTest`
Expected: 编译失败（类不存在）。

- [x] **Step 4: 实现**

```java
/**
 * JWT 配置属性：读取 application.yml 的 app.jwt.*。
 * 干什么用：把签名密钥和有效期从代码里抽出来，部署时只改配置、不改代码。
 */
@Data
@Component
@ConfigurationProperties(prefix = "app.jwt")
public class JwtProperties {
    /** 签名密钥：HS256 要求至少 32 字节，生产环境必须改成随机长字符串 */
    private String secret;
    /** token 有效期（天），MVP 定为 30 天，到期后需重新登录 */
    private int expireDays;
}

/**
 * JWT 工具类：负责签发和解析 token。
 * 干什么用：登录/注册成功后签发 token；每次请求由 AuthInterceptor 调用 parse 校验身份。
 * 为什么这么设计：JWT 无状态，服务重启不失效，也无需 Redis 存储会话。
 */
@Component
@RequiredArgsConstructor
public class JwtUtil {
    private final JwtProperties properties;

    /** 由配置密钥构造 HS256 签名密钥：密钥长度不够会抛异常，所以配置里必须 >=32 字节 */
    private SecretKey key() {
        return Keys.hmacShaKeyFor(properties.getSecret().getBytes(StandardCharsets.UTF_8));
    }

    /**
     * 签发 token。
     * @param userId 用户主键，放进 subject，便于随时取回
     * @param role 角色，放进自定义 claim，拦截器做权限判断时不用再查库
     */
    public String generateToken(Long userId, Integer role) {
        Instant now = Instant.now();
        return Jwts.builder()
                .subject(String.valueOf(userId))
                .claim("role", role)
                .issuedAt(Date.from(now))
                .expiration(Date.from(now.plus(properties.getExpireDays(), ChronoUnit.DAYS)))
                .signWith(key())
                .compact();
    }

    /** 解析并校验签名；token 无效或过期会抛 JwtException，由调用方捕获 */
    public Claims parse(String token) {
        return Jwts.parser().verifyWith(key()).build().parseSignedClaims(token).getPayload();
    }
}

/**
 * 当前登录用户上下文。
 * 干什么用：拦截器解析 token 后把 userId/role 放进 ThreadLocal，Controller/Service 直接读取。
 * 为什么这么设计：避免每个方法都手动传用户参数；请求结束必须 clear，防止线程池复用导致串号。
 */
public final class UserContext {
    private static final ThreadLocal<Long> USER_ID = new ThreadLocal<>();
    private static final ThreadLocal<Integer> ROLE = new ThreadLocal<>();
    /** 请求开始时写入当前用户 */
    public static void set(Long userId, Integer role) { USER_ID.set(userId); ROLE.set(role); }
    /** 读取当前用户 id；未登录时为 null */
    public static Long getUserId() { return USER_ID.get(); }
    /** 读取当前用户角色 */
    public static Integer getRole() { return ROLE.get(); }
    /** 请求结束时清理，防止 ThreadLocal 内存泄漏/串号 */
    public static void clear() { USER_ID.remove(); ROLE.remove(); }
}

/**
 * 角色校验注解：标注在 Controller 方法上，由 AuthInterceptor 读取执行。
 * 例子：@RequireRole(User.ROLE_OWNER) 表示只有站长可访问。
 */
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface RequireRole { int[] value(); }

/**
 * 认证拦截器：处理所有 /api/** 请求（公开接口在 WebConfig 排除）。
 * 干什么用：解析并校验 JWT → 写入 UserContext → 检查方法上的 @RequireRole。
 */
@Component
@RequiredArgsConstructor
public class AuthInterceptor implements HandlerInterceptor {
    private final JwtUtil jwtUtil;

    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler)
            throws Exception {
        // 统一约定：token 放在 Authorization: Bearer <token> 头里
        String auth = request.getHeader("Authorization");
        String token = (auth != null && auth.startsWith("Bearer ")) ? auth.substring(7) : null;
        Claims claims;
        try {
            // 解析失败（过期/篡改/缺失）统一按未登录处理，不区分细节，避免信息泄露
            claims = token == null ? null : jwtUtil.parse(token);
        } catch (Exception e) {
            claims = null;
        }
        if (claims == null) {
            writeUnauthorized(response);
            return false;
        }
        // 从 token 里取身份：subject 是 userId，role 是自定义 claim
        Long userId = Long.valueOf(claims.getSubject());
        Integer role = claims.get("role", Integer.class);
        UserContext.set(userId, role);

        // 方法上标了 @RequireRole 才做角色校验；没标表示登录即可
        if (handler instanceof HandlerMethod hm) {
            RequireRole requireRole = hm.getMethodAnnotation(RequireRole.class);
            if (requireRole != null) {
                boolean allowed = Arrays.stream(requireRole.value()).anyMatch(r -> r == role);
                if (!allowed) {
                    UserContext.clear();
                    response.setStatus(403);
                    response.setContentType(MediaType.APPLICATION_JSON_VALUE);
                    response.setCharacterEncoding(StandardCharsets.UTF_8.name());
                    response.getWriter().write(objectMapper().writeValueAsString(Result.error(ResultCode.FORBIDDEN)));
                    return false;
                }
            }
        }
        return true;
    }

    @Override
    public void afterCompletion(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex) {
        UserContext.clear();
    }

    /** 返回统一的 401 JSON，前端拦截到 401 自动跳登录页 */
    private void writeUnauthorized(HttpServletResponse response) throws Exception {
        response.setStatus(401);
        response.setContentType(MediaType.APPLICATION_JSON_VALUE);
        response.setCharacterEncoding(StandardCharsets.UTF_8.name());
        response.getWriter().write(objectMapper().writeValueAsString(Result.error(ResultCode.UNAUTHORIZED, "请先登录")));
    }

    private ObjectMapper objectMapper() { return new ObjectMapper(); }
}
```

`WebConfig.java` 修改拦截器注册：

```java
/**
 * Web 配置：拦截器注册 + 跨域。
 * 为什么排除 /api/auth/* 与 GET /api/fragments：验证码/注册/登录是公开接口；
 * 首页列表游客可见，登录状态在 Controller 里「可选解析」（见 FragmentController.optionalUserId）。
 */
@Configuration
public class WebConfig implements WebMvcConfigurer {
    @Autowired
    private AuthInterceptor authInterceptor;

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(authInterceptor)
                .addPathPatterns("/api/**")
                .excludePathPatterns(
                        "/api/auth/captcha",
                        "/api/auth/register",
                        "/api/auth/login");
        // 首页列表 GET /api/fragments 的游客放行在 AuthInterceptor 里按方法判断，
        // 不能在这里排除路径——否则 POST 发布接口也会被放行，导致发布时拿不到登录用户
    }

    /** 开发阶段前后端分端口运行，允许任意来源；上线后同端口托管，跨域配置不再生效 */
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
                .allowedOriginPatterns("*")
                .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
                .allowedHeaders("*")
                .allowCredentials(true)
                .maxAge(3600);
    }
}
```

- [x] **Step 5: 运行测试确认通过**

Run: `mvn -q test -f fragment-story/pom.xml -Dtest=JwtUtilTest,AuthInterceptorTest`
Expected: 全部通过。

- [x] **Step 6: 提交**（已并入本次提交）

```bash
git add fragment-story/pom.xml fragment-story/src
git commit -m "feat: JWT 鉴权与角色拦截"
```

### Task 4: 验证码服务 —— ✅ 已完成（2026-08-07）

**涉及文件：**
- `fragment-story/src/main/java/com/storysliver/auth/CaptchaService.java`——算术验证码生成与一次性校验
- `fragment-story/src/main/java/com/storysliver/pojo/Auth/CaptchaResponse.java`——验证码响应（key + 图片 base64）
- `fragment-story/src/test/java/com/storysliver/auth/CaptchaServiceTest.java`——单元测试

**Files:**
- Create: `fragment-story/src/main/java/com/storysliver/auth/CaptchaService.java`
- Create: `fragment-story/src/main/java/com/storysliver/dto/CaptchaResponse.java`
- Test: `fragment-story/src/test/java/com/storysliver/auth/CaptchaServiceTest.java`

**Interfaces:**
- Produces: `CaptchaService.generate(): CaptchaResponse{captchaKey, imageBase64}`；`CaptchaService.verify(String key, String answer): boolean`（一次性）

- [x] **Step 1: 写失败测试**

```java
/** 验证码服务单元测试：能生成 key+图片、验证码一次性消费、错误 key 拒绝 */
class CaptchaServiceTest {
    private CaptchaService service = new CaptchaService();

    @Test
    void generate_returns_key_and_image() {
        CaptchaResponse resp = service.generate();
        assertNotNull(resp.getCaptchaKey());
        assertTrue(resp.getImageBase64().startsWith("data:image/png;base64,"));
    }

    @Test
    void correct_answer_verifies_once() {
        CaptchaResponse resp = service.generate();
        // 通过私有方法拿到答案不可行，测试一次性语义：verify 错误答案后 key 失效
        assertFalse(service.verify(resp.getCaptchaKey(), "999"));
        assertFalse(service.verify(resp.getCaptchaKey(), "999"));
    }

    @Test
    void wrong_key_returns_false() {
        assertFalse(service.verify("no-such-key", "1"));
    }
}
```

- [x] **Step 2: 运行测试确认失败**

Run: `mvn -q test -f fragment-story/pom.xml -Dtest=CaptchaServiceTest`
Expected: 编译失败（类不存在）。

- [x] **Step 3: 实现**

```java
/**
 * 验证码响应体：返回给前端。
 * 干什么用：前端用 captchaKey 连同用户输入的答案一起提交注册，图片以 base64 直接显示。
 */
@Data
@AllArgsConstructor
public class CaptchaResponse {
    /** 本次验证码的会话标识：verify 时凭它取正确答案，用完即删（一次性） */
    private String captchaKey;
    /** 验证码图片的 base64（data:image/png;base64,...），前端 img 直接展示 */
    private String imageBase64;
}

/**
 * 算术验证码服务：防止注册被脚本批量刷。
 * 干什么用：生成「a + b = ?」图片和答案；校验答案是否正确（一次性）。
 * 为什么用算术题而不是字符验证码：纯 Java 生成、无第三方依赖，对真人几乎零门槛，对脚本有一定拦截效果。
 */
@Service
public class CaptchaService {
    /** key → 正确答案。用 ConcurrentHashMap：验证码接口并发高，保证线程安全 */
    private final Map<String, String> answers = new ConcurrentHashMap<>();

    /** 生成一道随机加法题：答案范围 2-18，简单到真人一眼能答 */
    public CaptchaResponse generate() {
        int a = ThreadLocalRandom.current().nextInt(1, 10);
        int b = ThreadLocalRandom.current().nextInt(1, 10);
        String key = UUID.randomUUID().toString();
        answers.put(key, String.valueOf(a + b));
        return new CaptchaResponse(key, renderImage(a + " + " + b + " = ?"));
    }

    /**
     * 校验答案。
     * 为什么 remove：取走即删除，保证「一次性」，防止脚本拿同一 key 反复试答案。
     */
    public boolean verify(String key, String answer) {
        String expected = answers.remove(key);
        return expected != null && expected.equals(answer == null ? "" : answer.trim());
    }

    /** 用 AWT 画一张米白底的算式图片；无需外部字体/图片资源，纯代码生成 */
    private String renderImage(String text) {
        try {
            BufferedImage image = new BufferedImage(140, 44, BufferedImage.TYPE_INT_RGB);
            Graphics2D g = image.createGraphics();
            g.setColor(new Color(247, 243, 234));
            g.fillRect(0, 0, 140, 44);
            g.setColor(new Color(74, 66, 56));
            g.setFont(new Font(Font.SANS_SERIF, Font.BOLD, 20));
            g.drawString(text, 12, 30);
            // 画几条干扰线：增加机器识别难度，但不影响真人阅读
            g.setColor(new Color(200, 180, 150));
            for (int i = 0; i < 6; i++) {
                g.drawLine(0, i * 8, 140, i * 8 + 5);
            }
            g.dispose();
            ByteArrayOutputStream out = new ByteArrayOutputStream();
            ImageIO.write(image, "png", out);
            // 转 base64 内嵌给前端，省一次图片请求
            return "data:image/png;base64," + Base64.getEncoder().encodeToString(out.toByteArray());
        } catch (IOException e) {
            throw new IllegalStateException("验证码生成失败", e);
        }
    }
}
```

- [x] **Step 4: 运行测试确认通过**

Run: `mvn -q test -f fragment-story/pom.xml -Dtest=CaptchaServiceTest`
Expected: 通过。

- [x] **Step 5: 提交**

```bash
git add fragment-story/src
git commit -m "feat: 算术验证码"
```

### Task 5: 注册接口（含管理员特殊密码、IP 限流） —— ✅ 已完成（2026-08-12；站长判定 bug 已修复：改为 `countAdmin()==0` 判定 + 新增回归测试）

**涉及文件：**
- `fragment-story/pom.xml`——新增 spring-security-crypto（BCrypt 密码加密）
- `fragment-story/src/main/java/com/storysliver/config/BeanConfig.java`——PasswordEncoder Bean
- `fragment-story/src/main/java/com/storysliver/auth/RegisterRateLimiter.java`——注册 IP 限流（1 小时 5 个 / 24 小时 10 个）
- `fragment-story/src/main/java/com/storysliver/pojo/Auth/RegisterRequest.java`、`pojo/Auth/LoginRequest.java`——注册/登录请求体
- `fragment-story/src/main/java/com/storysliver/service/UserService.java`、`service/impl/UserServiceImpl.java`——注册/登录业务
- `fragment-story/src/main/java/com/storysliver/controller/AuthController.java`、`controller/UserController.java`——认证/用户接口
- `fragment-story/src/main/java/com/storysliver/common/ResultCode.java`——新增验证码/限流等错误码
- `fragment-story/src/test/java/com/storysliver/service/UserServiceTest.java`、`auth/RegisterRateLimiterTest.java`——单元测试

**Files:**
- Modify: `fragment-story/pom.xml`（新增 spring-security-crypto）
- Create: `fragment-story/src/main/java/com/storysliver/config/BeanConfig.java`
- Create: `fragment-story/src/main/java/com/storysliver/auth/RegisterRateLimiter.java`
- Create: `fragment-story/src/main/java/com/storysliver/dto/RegisterRequest.java`
- Create: `fragment-story/src/main/java/com/storysliver/service/UserService.java`
- Create: `fragment-story/src/main/java/com/storysliver/service/impl/UserServiceImpl.java`
- Create: `fragment-story/src/main/java/com/storysliver/controller/AuthController.java`
- Modify: `fragment-story/src/main/java/com/storysliver/common/ResultCode.java`
- Test: `fragment-story/src/test/java/com/storysliver/service/UserServiceTest.java`

**Interfaces:**
- Consumes: `UserMapper`、`SystemConfigMapper`、`CaptchaService.verify`、`RegisterRateLimiter.isAllowed(String ip)`、`JwtUtil.generateToken`
- Produces: `UserService.register(RegisterRequest req, String ip): String(token)`；`UserService.login(String username, String password): String(token)`；`UserService.me(Long userId): User`

- [x] **Step 1: 依赖与错误码**

pom 增加：

```xml
<dependency>
    <groupId>org.springframework.security</groupId>
    <artifactId>spring-security-crypto</artifactId>
</dependency>
```

`ResultCode.java` 增加枚举值：

```java
// 以下为 Task 5 新增错误码：验证码/限流/碎片操作/权限相关
CAPTCHA_ERROR(400, "验证码错误"),
REGISTER_TOO_FREQUENT(429, "注册过于频繁，请稍后再试"),
USERNAME_TAKEN(400, "用户名已存在"),
PASSWORD_TOO_WEAK(400, "密码至少 6 位"),
ADMIN_CODE_WRONG(400, "管理员注册密码错误"),
FRAGMENT_TOO_FREQUENT(429, "发布太频繁，5 分钟后再试"),
FRAGMENT_TOO_LONG(400, "内容不能超过 1000 字"),
ALREADY_LIKED(400, "已经点过赞了"),
NOT_LIKED(400, "还没有点赞"),
FRAGMENT_NOT_PUBLISHED(400, "碎片不存在或不可操作"),
NOT_AUTHOR(403, "只能操作自己的碎片"),
CANNOT_MODIFY_OWNER(400, "不能修改站长"),
LOGIN_FAILED(401, "用户名或密码错误");
```

- [x] **Step 2: 写失败测试**

`UserServiceTest.java`：

```java
/** 用户服务单元测试：注册（普通/管理员/站长）、验证码/限流/用户名冲突、登录成功与失败 */
class UserServiceTest {
    private UserMapper userMapper;
    private SystemConfigMapper configMapper;
    private CaptchaService captchaService;
    private RegisterRateLimiter rateLimiter;
    private PasswordEncoder encoder;
    private JwtUtil jwtUtil;
    private UserService service;

    @BeforeEach
    void setUp() {
        userMapper = mock(UserMapper.class);
        configMapper = mock(SystemConfigMapper.class);
        captchaService = mock(CaptchaService.class);
        rateLimiter = mock(RegisterRateLimiter.class);
        encoder = new BCryptPasswordEncoder();
        JwtProperties props = new JwtProperties();
        props.setSecret("test-secret-test-secret-test-secret-123456");
        props.setExpireDays(30);
        jwtUtil = new JwtUtil(props);
        service = new UserServiceImpl(userMapper, configMapper, captchaService, rateLimiter, encoder, jwtUtil);
    }

    private RegisterRequest req(String username, String password, boolean isAdmin, String adminCode) {
        RegisterRequest r = new RegisterRequest();
        r.setUsername(username);
        r.setNickname(username);
        r.setPassword(password);
        r.setCaptchaKey("k");
        r.setCaptchaAnswer("8");
        r.setIsAdmin(isAdmin);
        r.setAdminCode(adminCode);
        return r;
    }

    @Test
    void register_normal_user_returns_token_and_saves() {
        when(captchaService.verify("k", "8")).thenReturn(true);
        when(rateLimiter.isAllowed("1.2.3.4")).thenReturn(true);
        when(userMapper.selectByUsername("alice")).thenReturn(null);
        when(userMapper.insert(any())).thenAnswer(inv -> { inv.<User>getArgument(0).setId(1L); return 1; });

        String token = service.register(req("alice", "123456", false, null), "1.2.3.4");

        assertNotNull(token);
        ArgumentCaptor<User> captor = ArgumentCaptor.forClass(User.class);
        verify(userMapper).insert(captor.capture());
        assertEquals(0, captor.getValue().getRole());
        assertTrue(encoder.matches("123456", captor.getValue().getPassword()));
    }

    @Test
    void register_with_valid_admin_code_becomes_owner_when_no_admin_exists() {
        when(captchaService.verify("k", "8")).thenReturn(true);
        when(rateLimiter.isAllowed("1.2.3.4")).thenReturn(true);
        when(userMapper.selectByUsername("admin1")).thenReturn(null);
        SystemConfig cfg = new SystemConfig();
        cfg.setConfigKey(SystemConfig.KEY_ADMIN_REGISTER_CODE);
        cfg.setConfigValue(encoder.encode("SecretCode1"));
        when(configMapper.selectByKey(SystemConfig.KEY_ADMIN_REGISTER_CODE)).thenReturn(cfg);

        String token = service.register(req("admin1", "123456", true, "SecretCode1"), "1.2.3.4");

        assertNotNull(token);
        ArgumentCaptor<User> captor = ArgumentCaptor.forClass(User.class);
        verify(userMapper).insert(captor.capture());
        assertEquals(2, captor.getValue().getRole());
    }

    @Test
    void register_fails_when_captcha_wrong() {
        when(captchaService.verify("k", "8")).thenReturn(false);
        assertThrows(BusinessException.class, () -> service.register(req("alice", "123456", false, null), "1.2.3.4"));
    }

    @Test
    void register_fails_when_rate_limited() {
        when(captchaService.verify("k", "8")).thenReturn(true);
        when(rateLimiter.isAllowed("1.2.3.4")).thenReturn(false);
        assertThrows(BusinessException.class, () -> service.register(req("alice", "123456", false, null), "1.2.3.4"));
    }

    @Test
    void login_returns_token_on_success() {
        User u = new User();
        u.setId(1L); u.setUsername("alice"); u.setRole(0);
        u.setPassword(encoder.encode("123456"));
        when(userMapper.selectByUsername("alice")).thenReturn(u);
        String token = service.login("alice", "123456");
        assertNotNull(token);
        assertEquals("1", jwtUtil.parse(token).getSubject());
    }

    @Test
    void login_fails_on_wrong_password() {
        User u = new User();
        u.setId(1L); u.setUsername("alice"); u.setRole(0);
        u.setPassword(encoder.encode("123456"));
        when(userMapper.selectByUsername("alice")).thenReturn(u);
        assertThrows(BusinessException.class, () -> service.login("alice", "wrong"));
    }
}
```

`RegisterRateLimiterTest.java`：

```java
/** 注册 IP 限流测试：同 IP 第 6 次必须被拦，不同 IP 互不影响 */
class RegisterRateLimiterTest {
    @Test
    void allows_five_then_blocks_in_same_hour() {
        RegisterRateLimiter limiter = new RegisterRateLimiter();
        for (int i = 0; i < 5; i++) {
            assertTrue(limiter.isAllowed("1.1.1.1"));
        }
        assertFalse(limiter.isAllowed("1.1.1.1"));
        assertTrue(limiter.isAllowed("2.2.2.2"));
    }
}
```

- [x] **Step 3: 运行测试确认失败**

Run: `mvn -q test -f fragment-story/pom.xml -Dtest=UserServiceTest,RegisterRateLimiterTest`
Expected: 编译失败（类不存在）。

- [x] **Step 4: 实现**

```java
/**
 * 通用 Bean 配置。
 * 干什么用：把 PasswordEncoder 声明为 Spring Bean，各处（注册、登录、管理配置）统一注入同一个实例。
 * 为什么用 BCrypt：自带盐值、慢哈希，是目前最常用的密码安全存储方案。
 */
@Configuration
public class BeanConfig {
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}

/**
 * 注册 IP 限流器：防批量注册脚本。
 * 干什么用：同一 IP 1 小时内最多 5 个、24 小时内最多 10 个注册。
 * 为什么放宽到 5/10：防止把同一 WiFi 下的真实小团体误伤（验证码才是主防线，这里只是兜底）。
 * 为什么用内存实现：单实例部署够用、零依赖；等以后多实例再换 Redis。
 */
@Service
public class RegisterRateLimiter {
    /** ip → 该 IP 的注册时间戳队列。ConcurrentHashMap + 对队列加锁，保证并发下计数准确 */
    private final Map<String, Deque<Long>> hits = new ConcurrentHashMap<>();
    private static final long HOUR_MS = 3_600_000L;
    private static final long DAY_MS = 86_400_000L;

    /** 尝试放行一次注册；超限返回 false。过期记录在检查时顺带清理，避免内存无限增长 */
    public boolean isAllowed(String ip) {
        long now = System.currentTimeMillis();
        Deque<Long> q = hits.computeIfAbsent(ip, k -> new ArrayDeque<>());
        synchronized (q) {
            // 先清掉 24 小时前的旧记录
            while (!q.isEmpty() && now - q.peekFirst() > DAY_MS) q.pollFirst();
            long hourCount = 0;
            for (Long t : q) if (now - t <= HOUR_MS) hourCount++;
            // 任一维度超限就拒绝；没超限就记录本次时间
            if (hourCount >= 5 || q.size() >= 10) return false;
            q.addLast(now);
            return true;
        }
    }
}

/**
 * 注册请求体：前端注册表单的完整字段。
 * 校验注解干什么用：Bean Validation 在进入 Service 前先拦截空值/超长，Controller 保持轻薄。
 */
@Data
public class RegisterRequest {
    /** 登录用户名（唯一） */
    @NotBlank(message = "用户名不能为空")
    @Size(max = 32, message = "用户名最长 32 位")
    private String username;
    /** 展示昵称 */
    @NotBlank(message = "昵称不能为空")
    @Size(max = 32, message = "昵称最长 32 位")
    private String nickname;
    /** 明文密码（Service 里 BCrypt 加密后落库） */
    @NotBlank(message = "密码不能为空")
    private String password;
    /** 验证码会话 key（captcha 接口返回） */
    @NotBlank(message = "验证码不能为空")
    private String captchaKey;
    /** 用户输入的验证码答案 */
    @NotBlank(message = "验证码答案不能为空")
    private String captchaAnswer;
    /** 是否勾选「注册为管理员」，默认普通用户 */
    private Boolean isAdmin = false;
    /** 勾选管理员时必填：站长才知道的特殊注册密码 */
    private String adminCode;
}

/**
 * 用户服务接口：注册 / 登录 / 当前用户信息。
 * 为什么定义接口：Controller 依赖接口而非实现，方便替换实现与单元测试打桩。
 */
public interface UserService {
    /** 注册（成功自动登录，返回 JWT） */
    String register(RegisterRequest request, String ip);
    /** 登录，校验通过返回 JWT */
    String login(String username, String password);
    /** 查询当前用户信息 */
    User me(Long userId);
}

/**
 * 用户服务实现：注册/登录的核心业务逻辑。
 * 校验顺序为什么固定：验证码 → IP 限流 → 密码强度 → 用户名唯一 → 管理员密码，越廉价的校验越靠前，尽早拦截无效请求。
 */
@Service
@RequiredArgsConstructor
public class UserServiceImpl implements UserService {
    private final UserMapper userMapper;
    private final SystemConfigMapper systemConfigMapper;
    private final CaptchaService captchaService;
    private final RegisterRateLimiter registerRateLimiter;
    private final PasswordEncoder passwordEncoder;
    private final JwtUtil jwtUtil;

    /**
     * 注册用户。
     * @param request 注册表单
     * @param ip 客户端 IP，用于注册限流
     * @return JWT token（注册成功即自动登录）
     * @throws BusinessException 验证码错误 / 限流 / 密码过弱 / 用户名重复 / 管理员密码错误
     */
    @Override
    public String register(RegisterRequest request, String ip) {
        // 先校验验证码：一次性（verify 内部 remove），防止脚本重放
        if (!captchaService.verify(request.getCaptchaKey(), request.getCaptchaAnswer())) {
            throw new BusinessException(ResultCode.CAPTCHA_ERROR);
        }
        // 再限流：验证码挡脚本、IP 限流兜底，双保险
        if (!registerRateLimiter.isAllowed(ip)) {
            throw new BusinessException(ResultCode.REGISTER_TOO_FREQUENT);
        }
        // 密码最短 6 位是最基本的强度底线
        if (request.getPassword() == null || request.getPassword().length() < 6) {
            throw new BusinessException(ResultCode.PASSWORD_TOO_WEAK);
        }
        // 用户名唯一是登录体系的前提，冲突直接拒绝
        if (userMapper.selectByUsername(request.getUsername()) != null) {
            throw new BusinessException(ResultCode.USERNAME_TAKEN);
        }

        int role = User.ROLE_USER;
        // 管理员注册：必须提供站长才知道的特殊密码；密码以 BCrypt 哈希存库
        if (Boolean.TRUE.equals(request.getIsAdmin())) {
            SystemConfig cfg = systemConfigMapper.selectByKey(SystemConfig.KEY_ADMIN_REGISTER_CODE);
            if (cfg == null || request.getAdminCode() == null
                    || !passwordEncoder.matches(request.getAdminCode(), cfg.getConfigValue())) {
                throw new BusinessException(ResultCode.ADMIN_CODE_WRONG);
            }
            // 第一个用特殊密码注册的管理员自动成为站长，之后都是普通管理员（防止人人都是站长）
            role = userMapper.countAll() == 0 ? User.ROLE_OWNER : User.ROLE_ADMIN;
        }

        // 密码加密后落库：任何人都看不到明文，包括管理员
        User user = new User();
        user.setUsername(request.getUsername().trim());
        user.setNickname(request.getNickname().trim());
        user.setPassword(passwordEncoder.encode(request.getPassword()));
        user.setRole(role);
        user.setStatus(User.STATUS_NORMAL);
        userMapper.insert(user);
        // 注册成功直接返回 token：省去「注册后再登录」一步
        return jwtUtil.generateToken(user.getId(), user.getRole());
    }

    /**
     * 登录：按用户名查用户，BCrypt 比对密码。
     * 为什么用户名不存在和密码错误返回同一错误：不暴露「该用户是否存在」，防账号枚举。
     */
    @Override
    public String login(String username, String password) {
        User user = userMapper.selectByUsername(username);
        if (user == null || !passwordEncoder.matches(password, user.getPassword())) {
            throw new BusinessException(ResultCode.LOGIN_FAILED);
        }
        return jwtUtil.generateToken(user.getId(), user.getRole());
    }

    /** 查询当前用户信息：前端登录后拉取昵称、角色用于展示与路由守卫 */
    @Override
    public User me(Long userId) {
        return userMapper.selectById(userId);
    }
}
```

`AuthController.java`：

```java
/**
 * 认证接口：验证码 / 注册 / 登录。
 * 这三个接口在 WebConfig 中被认证拦截器排除（公开访问）。
 */
@RestController
@RequestMapping("/api/auth")
public class AuthController {
    @Autowired
    private UserService userService;//注册/登录业务
    @Autowired
    private CaptchaService captchaService;//验证码

    /** 获取注册用算术验证码 */
    @GetMapping("/captcha")
    public Result captcha() {
        return Result.success(captchaService.generate());
    }

    /** 注册：需要客户端 IP 用于限流，所以额外注入 HttpServletRequest */
    @PostMapping("/register")
    public Result register(@Valid @RequestBody RegisterRequest request, HttpServletRequest http) {
        String ip = clientIp(http);
        return Result.success(Map.of("token", userService.register(request, ip)));
    }

    /** 登录：用户名 + 密码，返回 token */
    @PostMapping("/login")
    public Result login(@Valid @RequestBody LoginRequest request) {
        return Result.success(Map.of("token", userService.login(request.getUsername(), request.getPassword())));
    }

    /**
     * 获取真实客户端 IP。
     * 为什么优先取 X-Forwarded-For：生产环境前面有 Nginx/负载均衡时，getRemoteAddr 拿到的是代理 IP；
     * 取第一个值是因为代理链里第一个才是真实客户端（防止伪造要由代理覆盖该头，部署章节有说明）。
     */
    private String clientIp(HttpServletRequest request) {
        String xff = request.getHeader("X-Forwarded-For");
        if (xff != null && !xff.isBlank()) return xff.split(",")[0].trim();
        return request.getRemoteAddr();
    }
}
```

`LoginRequest.java`：

```java
/**
 * 登录请求体：用户名 + 密码。
 */
@Data
public class LoginRequest {
    @NotBlank(message = "用户名不能为空")
    private String username;
    @NotBlank(message = "密码不能为空")
    private String password;
}
```

`UserController.java`：

```java
/**
 * 用户接口：当前登录用户信息。
 */
@RestController
@RequestMapping("/api/users")
public class UserController {
    @Autowired
    private UserService userService;//用户业务

    /** 返回当前用户：id/昵称/角色等，前端登录后立即调用一次 */
    @GetMapping("/me")
    public Result me() {
        // UserContext 由 AuthInterceptor 写入，不需要从参数取 userId
        return Result.success(userService.me(UserContext.getUserId()));
    }
}
```

- [x] **Step 5: 运行测试确认通过**

Run: `mvn -q test -f fragment-story/pom.xml -Dtest=UserServiceTest,RegisterRateLimiterTest`
Expected: 全部通过。

- [x] **Step 6: 手工冒烟**

Run: 启动应用，访问 `GET /api/auth/captcha`、`POST /api/auth/register`、`POST /api/auth/login`
Expected: 注册后返回 token；用 token 调 `GET /api/users/me` 返回用户信息。

- [x] **Step 7: 提交**

```bash
git add fragment-story/pom.xml fragment-story/src
git commit -m "feat: 注册登录与验证码限流"
```

### Task 6: 阶段 1 验收 —— ✅ 已完成（2026-08-12；全量测试 + 接口冒烟通过，注册/登录完整链路已通过前端登录页联调确认）

**涉及文件：** 无（验收步骤，不改代码）

- [x] **Step 1: 全量测试**

Run: `mvn -q test -f fragment-story/pom.xml`
Expected: 全部通过。

- [x] **Step 2: 手动验证**

- 验证码错误 → 注册失败并提示
- 同一 IP 连续注册 6 个账号 → 第 6 个被限流
- 用户名重复 → 失败
- 密码少于 6 位 → 失败
- 用 `app.admin.default-register-code`（<管理员注册密码>）注册第一个管理员 → 角色为站长（数据库 role=2）
- 普通用户访问 `/api/admin/**` → 403

- [x] **Step 3: 提交**

```bash
git add fragment-story/src
git commit -m "test: 阶段1验收"
```

## 阶段 2：碎片核心（后端）

### Task 7: 碎片提交与列表 —— ✅ 已完成（2026-08-12：FragmentServiceImpl 全部方法实现、编译与全量测试通过；FragmentServiceTest 按用户意见跳过不写）

**涉及文件：**
- `fragment-story/pom.xml`——新增 pagehelper-spring-boot-starter（分页）
- `fragment-story/src/main/java/com/storysliver/auth/FragmentRateLimiter.java`——发布限频（5 分钟/条）
- `fragment-story/src/main/java/com/storysliver/pojo/Fragment/FragmentSubmitRequest.java`、`pojo/Fragment/FragmentVO.java`——发布请求体、列表展示对象
- `fragment-story/src/main/java/com/storysliver/service/FragmentService.java`、`service/impl/FragmentServiceImpl.java`——碎片业务（发布/列表/点赞/隐藏/删除）
- `fragment-story/src/main/java/com/storysliver/controller/FragmentController.java`——碎片接口
- `fragment-story/src/main/java/com/storysliver/mapper/StoryFragmentMapper.java`、`resources/com/storysliver/mapper/StoryFragmentMapper.xml`——碎片数据访问
- `fragment-story/src/main/java/com/storysliver/mapper/FragmentLikeMapper.java`——点赞记录查询（已赞列表）
- `fragment-story/src/test/java/com/storysliver/service/FragmentServiceTest.java`、`auth/FragmentRateLimiterTest.java`——单元测试

> 分页注意：按「代码风格规范」，分页一律用 **PageHelper + PageBean**（与 sims 一致）。执行前先在 pom 加入 `pagehelper-spring-boot-starter` 2.1.0；`list` 接口返回 `PageBean`，Mapper 的 `selectPublishedPage()` 不带 offset/limit 参数（由 PageHelper 注入），示例代码中出现的 `PageResult`/手动分页一律按此替换，`countPublished()` 不再需要。

**Files:**
- Create: `fragment-story/src/main/java/com/storysliver/auth/FragmentRateLimiter.java`
- Create: `fragment-story/src/main/java/com/storysliver/dto/FragmentSubmitRequest.java`
- Create: `fragment-story/src/main/java/com/storysliver/dto/FragmentVO.java`
- Create: `fragment-story/src/main/java/com/storysliver/service/FragmentService.java`
- Create: `fragment-story/src/main/java/com/storysliver/service/impl/FragmentServiceImpl.java`
- Create: `fragment-story/src/main/java/com/storysliver/controller/FragmentController.java`（旧版 StoryFragmentController 已在旧代码清理中删除）
- Test: `fragment-story/src/test/java/com/storysliver/service/FragmentServiceTest.java`

**Interfaces:**
- Consumes: `StoryFragmentMapper`、`FragmentLikeMapper`、`UserMapper`、`FragmentRateLimiter`
- Produces: `FragmentService.submit(Long userId, String content, boolean anonymous)`；`FragmentService.list(int pageNum, int pageSize, Long currentUserId): PageBean`；`FragmentService.my(Long userId): List<FragmentVO>`；`FragmentService.like(Long userId, Long fragmentId)`；`FragmentService.unlike(Long userId, Long fragmentId)`；`FragmentService.hide(Long userId, Long fragmentId)`；`FragmentService.unhide(Long userId, Long fragmentId)`；`FragmentService.deleteByAuthor(Long userId, Long fragmentId)`；`FragmentService.toVO(StoryFragment, boolean likedByMe): FragmentVO`

- [ ] **Step 1: 写失败测试**

`FragmentServiceTest.java`：

```java
/** 碎片服务单元测试：发布进入待审核、1000 字/限频、列表匿名展示、点赞幂等、越权拒绝 */
class FragmentServiceTest {
    private StoryFragmentMapper fragmentMapper;
    private FragmentLikeMapper likeMapper;
    private UserMapper userMapper;
    private FragmentRateLimiter rateLimiter;
    private FragmentService service;

    @BeforeEach
    void setUp() {
        fragmentMapper = mock(StoryFragmentMapper.class);
        likeMapper = mock(FragmentLikeMapper.class);
        userMapper = mock(UserMapper.class);
        rateLimiter = mock(FragmentRateLimiter.class);
        service = new FragmentServiceImpl(fragmentMapper, likeMapper, userMapper, rateLimiter);
    }

    private StoryFragment published(Long id) {
        StoryFragment f = new StoryFragment();
        f.setId(id); f.setUserId(1L); f.setContent("hello");
        f.setLikeCount(0); f.setIsAnonymous(0); f.setStatus(StoryFragment.STATUS_PUBLISHED);
        return f;
    }

    @Test
    void submit_creates_pending_fragment() {
        when(rateLimiter.tryAcquire(1L)).thenReturn(true);
        service.submit(1L, "  你好世界  ", false);
        ArgumentCaptor<StoryFragment> captor = ArgumentCaptor.forClass(StoryFragment.class);
        verify(fragmentMapper).insert(captor.capture());
        assertEquals("你好世界", captor.getValue().getContent());
        assertEquals(0, captor.getValue().getStatus());
        assertEquals(0, captor.getValue().getIsAnonymous());
    }

    @Test
    void submit_rejects_over_1000_chars() {
        when(rateLimiter.tryAcquire(1L)).thenReturn(true);
        String longContent = "a".repeat(1001);
        assertThrows(BusinessException.class, () -> service.submit(1L, longContent, false));
    }

    @Test
    void submit_rejects_when_rate_limited() {
        when(rateLimiter.tryAcquire(1L)).thenReturn(false);
        assertThrows(BusinessException.class, () -> service.submit(1L, "hello", false));
    }

    @Test
    void list_returns_published_with_anonymous_display() {
        StoryFragment f = published(1L);
        f.setIsAnonymous(1);
        // PageHelper.startPage 是静态方法，单测里用 mockStatic 让它什么都不做；
        // Mapper 返回手工构造的 Page（PageHelper 分页后返回的就是 Page）
        try (MockedStatic<PageHelper> ignored = mockStatic(PageHelper.class)) {
            Page<StoryFragment> page = new Page<>(1, 10);
            page.setTotal(1);
            page.add(f);
            when(fragmentMapper.selectPublishedPage()).thenReturn(page);
            when(likeMapper.selectLikedFragmentIds(9L)).thenReturn(List.of(1L));

            PageBean result = service.list(1, 10, 9L);

            assertEquals(1L, result.getTotal());
            assertEquals(1, result.getList().size());
            FragmentVO vo = (FragmentVO) result.getList().get(0);
            assertEquals("匿名用户", vo.getAuthorName());
            assertTrue(vo.getLikedByMe());
        }
    }

    @Test
    void like_inserts_and_increments() {
        when(fragmentMapper.selectById(1L)).thenReturn(published(1L));
        when(likeMapper.exists(1L, 9L)).thenReturn(false);
        service.like(9L, 1L);
        verify(likeMapper).insert(any());
        verify(fragmentMapper).increaseLikeCount(1L);
    }

    @Test
    void like_twice_fails() {
        when(fragmentMapper.selectById(1L)).thenReturn(published(1L));
        when(likeMapper.exists(1L, 9L)).thenReturn(true);
        assertThrows(BusinessException.class, () -> service.like(9L, 1L));
    }

    @Test
    void unlike_deletes_and_decrements() {
        when(fragmentMapper.selectById(1L)).thenReturn(published(1L));
        when(likeMapper.exists(1L, 9L)).thenReturn(true);
        service.unlike(9L, 1L);
        verify(likeMapper).deleteByFragmentAndUser(1L, 9L);
        verify(fragmentMapper).decreaseLikeCount(1L);
    }

    @Test
    void hide_only_by_author_on_published() {
        StoryFragment f = published(1L);
        when(fragmentMapper.selectById(1L)).thenReturn(f);
        service.hide(1L, 1L);
        verify(fragmentMapper).updateStatus(1L, StoryFragment.STATUS_HIDDEN);
    }

    @Test
    void hide_rejects_non_author() {
        when(fragmentMapper.selectById(1L)).thenReturn(published(1L));
        assertThrows(BusinessException.class, () -> service.hide(9L, 1L));
    }

    @Test
    void delete_by_author_removes_row_and_likes() {
        when(fragmentMapper.selectById(1L)).thenReturn(published(1L));
        service.deleteByAuthor(1L, 1L);
        verify(fragmentMapper).deleteById(1L);
        verify(likeMapper).deleteByFragmentAndUser(1L, null);
    }
}
```

`FragmentRateLimiterTest.java`：

```java
/** 发布限频测试：同用户 5 分钟内第二次提交必须被拦 */
class FragmentRateLimiterTest {
    @Test
    void blocks_second_submit_within_5_minutes() {
        FragmentRateLimiter limiter = new FragmentRateLimiter();
        assertTrue(limiter.tryAcquire(1L));
        assertFalse(limiter.tryAcquire(1L));
        assertTrue(limiter.tryAcquire(2L));
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `mvn -q test -f fragment-story/pom.xml -Dtest=FragmentServiceTest,FragmentRateLimiterTest`
Expected: 编译失败（类不存在）。

- [x] **Step 3: 实现**

```java
/**
 * 发布频率限流器：每个登录用户 5 分钟只能发 1 条。
 * 为什么按用户而不是按 IP：发布必须登录，按用户精准且不误伤同网络下的其他人。
 * 为什么用内存：单实例部署够用；将来多实例再换 Redis。
 */
@Service
public class FragmentRateLimiter {
    /** userId → 上次发布时间戳。ConcurrentHashMap 保证并发安全 */
    private final Map<Long, Long> lastSubmit = new ConcurrentHashMap<>();
    private static final long INTERVAL_MS = 5 * 60_000L;

    /** 尝试获取一次发布额度；间隔不足 5 分钟返回 false */
    public boolean tryAcquire(Long userId) {
        long now = System.currentTimeMillis();
        // putIfAbsent：第一次发布直接放行并记录时间
        Long prev = lastSubmit.putIfAbsent(userId, now);
        if (prev == null) return true;
        if (now - prev < INTERVAL_MS) return false;
        // 间隔已过，更新时间戳并放行
        lastSubmit.put(userId, now);
        return true;
    }
}

/**
 * 发布请求体：内容 + 是否匿名。
 * 为什么在请求体校验长度：Bean Validation 在进入 Service 前拦截，Service 里再校验一次兜底（防止绕过）。
 */
@Data
public class FragmentSubmitRequest {
    @NotBlank(message = "内容不能为空")
    @Size(max = 1000, message = "内容不能超过 1000 字")
    private String content;
    /** 是否匿名发布：true 时对外显示「匿名用户」 */
    private Boolean isAnonymous = false;
}

/**
 * 碎片视图对象：返回给前端的展示模型。
 * 为什么不用实体直接返回：实体带 user_id 等敏感字段；VO 只暴露需要展示的字段，并拼好 authorName/likedByMe。
 */
@Data
public class FragmentVO {
    private Long id;
    private String content;
    private Integer likeCount;
    /** 是否匿名：0 显示昵称 / 1 显示「匿名用户」 */
    private Integer isAnonymous;
    /** 生命周期状态：0 待审核 / 1 已发布 / 2 已隐藏 */
    private Integer status;
    /** 展示名：匿名时「匿名用户」，否则昵称（管理端列表显示真实昵称） */
    private String authorName;
    /** 当前登录用户是否已赞：列表接口按需填充，用于前端高亮 */
    private Boolean likedByMe;
    /** 格式化后的创建时间 */
    private String createdAt;
}
```

`FragmentServiceImpl.java` 核心方法：

```java
/**
 * 碎片业务实现：发布、列表、我的碎片、点赞/取消、隐藏/取消隐藏、作者删除。
 * 为什么所有「操作自己的碎片」都先查询再校验：确保只能操作自己的、且状态合法的碎片，防止越权。
 */
@Service
public class FragmentServiceImpl implements FragmentService {
    @Autowired
    private StoryFragmentMapper fragmentMapper;//碎片表操作
    @Autowired
    private FragmentLikeMapper likeMapper;//点赞表操作
    @Autowired
    private UserMapper userMapper;//查昵称拼 authorName
    @Autowired
    private FragmentRateLimiter fragmentRateLimiter;//发布限频（5 分钟/条）

    /**
     * 发布碎片：进入「待审核」状态，不会立刻上墙。
     * 先校验内容再限频：内容非法（空/超长）的请求不占用发布额度。
     */
    @Override
    public StoryFragment submit(Long userId, String content, boolean anonymous) {
        String text = content == null ? "" : content.trim();
        if (text.isEmpty() || text.length() > 1000) {
            throw new BusinessException(ResultCode.FRAGMENT_TOO_LONG);
        }
        if (!rateLimiter.tryAcquire(userId)) {
            throw new BusinessException(ResultCode.FRAGMENT_TOO_FREQUENT);
        }
        StoryFragment f = new StoryFragment();
        f.setUserId(userId);
        f.setContent(text);
        f.setLikeCount(0);
        f.setIsAnonymous(anonymous ? 1 : 0);
        f.setStatus(StoryFragment.STATUS_PENDING);
        fragmentMapper.insert(f);
        return f;
    }

    /**
     * 首页分页列表：只返回「已发布」。
     * currentUserId 为 null 表示游客，不查点赞状态（likedByMe=false）。
     */
    @Override
    public PageBean list(int pageNum, int pageSize, Long currentUserId) {
        // PageHelper：查询前调用 startPage，它会给下一条查询自动加 LIMIT 分页
        PageHelper.startPage(pageNum, pageSize);
        // 只查已发布（status=1），分页参数由 PageHelper 注入
        List<StoryFragment> fragments = fragmentMapper.selectPublishedPage();
        // PageHelper 把结果包装成 Page：getTotal() 是总数，getResult() 是本页数据
        Page<StoryFragment> page = (Page<StoryFragment>) fragments;
        // 一次查出当前用户赞过的所有碎片 id，避免逐条查询 N+1 问题
        List<Long> likedIds = currentUserId == null ? List.of() : likeMapper.selectLikedFragmentIds(currentUserId);
        List<FragmentVO> vos = page.getResult().stream()
                .map(f -> toVO(f, likedIds.contains(f.getId())))
                .toList();
        return new PageBean(page.getTotal(), vos);
    }

    /** 我的碎片：返回自己的全部状态（待审核/已发布/已隐藏，含匿名的） */
    @Override
    public List<FragmentVO> my(Long userId) {
        return fragmentMapper.selectByUser(userId).stream()
                .map(f -> toVO(f, false))
                .toList();
    }

    /** 点赞：只能赞「已发布」的碎片；重复点赞由数据库唯一约束兜底，这里先友好拦截 */
    @Override
    public void like(Long userId, Long fragmentId) {
        requirePublished(fragmentId);
        if (likeMapper.exists(fragmentId, userId)) {
            throw new BusinessException(ResultCode.ALREADY_LIKED);
        }
        FragmentLike like = new FragmentLike();
        like.setFragmentId(fragmentId);
        like.setUserId(userId);
        likeMapper.insert(like);
        fragmentMapper.increaseLikeCount(fragmentId);
    }

    /** 取消点赞：先确认赞过，再删记录并把计数减回去 */
    @Override
    public void unlike(Long userId, Long fragmentId) {
        requirePublished(fragmentId);
        if (!likeMapper.exists(fragmentId, userId)) {
            throw new BusinessException(ResultCode.NOT_LIKED);
        }
        likeMapper.deleteByFragmentAndUser(fragmentId, userId);
        fragmentMapper.decreaseLikeCount(fragmentId);
    }

    /** 隐藏：只有作者能操作，且只允许隐藏「已发布」的碎片 */
    @Override
    public void hide(Long userId, Long fragmentId) {
        StoryFragment f = requireOwned(userId, fragmentId);
        if (f.getStatus() != StoryFragment.STATUS_PUBLISHED) {
            throw new BusinessException(ResultCode.FRAGMENT_NOT_PUBLISHED);
        }
        fragmentMapper.updateStatus(fragmentId, StoryFragment.STATUS_HIDDEN);
    }

    /** 取消隐藏：只有作者能操作，且只允许恢复「已隐藏」的碎片 */
    @Override
    public void unhide(Long userId, Long fragmentId) {
        StoryFragment f = requireOwned(userId, fragmentId);
        if (f.getStatus() != StoryFragment.STATUS_HIDDEN) {
            throw new BusinessException(ResultCode.FRAGMENT_NOT_PUBLISHED);
        }
        fragmentMapper.updateStatus(fragmentId, StoryFragment.STATUS_PUBLISHED);
    }

    /** 作者硬删除：删行并清掉该碎片全部点赞（同一事务，防止留下孤儿点赞记录） */
    @Override
    public void deleteByAuthor(Long userId, Long fragmentId) {
        requireOwned(userId, fragmentId);
        fragmentMapper.deleteById(fragmentId);
        likeMapper.deleteByFragmentAndUser(fragmentId, null);
    }

    /** 实体转 VO：匿名时显示「匿名用户」，实名时查一次用户表拿昵称 */
    @Override
    public FragmentVO toVO(StoryFragment f, boolean likedByMe) {
        FragmentVO vo = new FragmentVO();
        vo.setId(f.getId());
        vo.setContent(f.getContent());
        vo.setLikeCount(f.getLikeCount());
        vo.setIsAnonymous(f.getIsAnonymous());
        vo.setStatus(f.getStatus());
        vo.setLikedByMe(likedByMe);
        vo.setCreatedAt(f.getCreatedAt() == null ? "" : f.getCreatedAt().format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss")));
        // 匿名对外统一展示「匿名用户」，但数据库仍记录 user_id 供管理员追溯
        if (f.getIsAnonymous() == 1) {
            vo.setAuthorName("匿名用户");
        } else {
            User author = userMapper.selectById(f.getUserId());
            vo.setAuthorName(author == null ? "未知用户" : author.getNickname());
        }
        return vo;
    }

    /** 校验碎片存在且已发布：点赞/取消点赞的前置条件 */
    private void requirePublished(Long fragmentId) {
        StoryFragment f = fragmentMapper.selectById(fragmentId);
        if (f == null || f.getStatus() != StoryFragment.STATUS_PUBLISHED) {
            throw new BusinessException(ResultCode.FRAGMENT_NOT_PUBLISHED);
        }
    }

    /** 校验碎片存在且属于当前用户：隐藏/取消隐藏/删除的前置条件 */
    private StoryFragment requireOwned(Long userId, Long fragmentId) {
        StoryFragment f = fragmentMapper.selectById(fragmentId);
        if (f == null || !f.getUserId().equals(userId)) {
            throw new BusinessException(ResultCode.NOT_AUTHOR);
        }
        return f;
    }
}
```

`FragmentController.java`：

```java
/**
 * 碎片接口：发布、列表、我的碎片、点赞/取消、隐藏/取消隐藏、删除。
 * 除 GET /api/fragments（游客可看）外，其余都需登录（拦截器已排除项见 WebConfig）。
 */
@RestController
@RequestMapping("/api/fragments")
public class FragmentController {
    @Autowired
    private FragmentService fragmentService;//碎片业务
    @Autowired
    private JwtUtil jwtUtil;//首页列表「可选登录解析」用

    /** 发布碎片：提交后进入待审核 */
    @PostMapping
    public Result submit(@Valid @RequestBody FragmentSubmitRequest request) {
        StoryFragment f = fragmentService.submit(UserContext.getUserId(), request.getContent(),
                Boolean.TRUE.equals(request.getIsAnonymous()));
        return Result.success(Map.of("id", f.getId(), "status", f.getStatus()));
    }

    /**
     * 首页列表：游客也能看。
     * 为什么这里手动解析 token：该路径被拦截器排除（公开），但登录用户应看到「已赞」状态，所以做成「可选登录」。
     */
    @GetMapping
    public Result list(
            @RequestParam(defaultValue = "1") int pageNum,
            @RequestParam(defaultValue = "10") int pageSize,
            HttpServletRequest http) {
        Long userId = optionalUserId(http);
        return Result.success(fragmentService.list(pageNum, pageSize, userId));
    }

    /** 我的碎片：包含自己全部状态（含匿名的） */
    @GetMapping("/my")
    public Result my() {
        return Result.success(fragmentService.my(UserContext.getUserId()));
    }

    /** 点赞 */
    @PostMapping("/{id}/like")
    public Result like(@PathVariable Long id) {
        fragmentService.like(UserContext.getUserId(), id);
        return Result.success();
    }

    /** 取消点赞 */
    @DeleteMapping("/{id}/like")
    public Result unlike(@PathVariable Long id) {
        fragmentService.unlike(UserContext.getUserId(), id);
        return Result.success();
    }

    /** 隐藏（仅作者，仅已发布） */
    @PutMapping("/{id}/hide")
    public Result hide(@PathVariable Long id) {
        fragmentService.hide(UserContext.getUserId(), id);
        return Result.success();
    }

    /** 取消隐藏（仅作者，仅已隐藏） */
    @PutMapping("/{id}/unhide")
    public Result unhide(@PathVariable Long id) {
        fragmentService.unhide(UserContext.getUserId(), id);
        return Result.success();
    }

    /** 作者硬删除（前端弹窗确认） */
    @DeleteMapping("/{id}")
    public Result delete(@PathVariable Long id) {
        fragmentService.deleteByAuthor(UserContext.getUserId(), id);
        return Result.success();
    }

    /** 可选登录解析：token 合法就返回 userId，否则返回 null（游客），解析失败静默忽略 */
    private Long optionalUserId(HttpServletRequest http) {
        String auth = http.getHeader("Authorization");
        if (auth == null || !auth.startsWith("Bearer ")) return null;
        try {
            return Long.valueOf(jwtUtil.parse(auth.substring(7)).getSubject());
        } catch (Exception e) {
            return null;
        }
    }
}
```

注意：`deleteByFragmentAndUser(fragmentId, null)` 需要 Mapper 支持 null 参数（删除某碎片全部点赞）。修改 `FragmentLikeMapper.deleteByFragmentAndUser` 的 XML 为：

```xml
<delete id="deleteByFragmentAndUser">
    DELETE FROM fragment_like
    WHERE fragment_id = #{fragmentId}
    <if test="userId != null">AND user_id = #{userId}</if>
</delete>
```

- [x] **Step 4: 运行测试确认通过**

Run: `mvn -q test -f fragment-story/pom.xml -Dtest=FragmentServiceTest,FragmentRateLimiterTest`
Expected: 全部通过。

- [x] **Step 5: 提交**

```bash
git add fragment-story/src
git commit -m "feat: 碎片提交列表点赞隐藏删除"
```

### Task 8: 阶段 2 验收 —— ✅ 已完成（2026-08-12；12 项接口验收全部通过。过程中发现并修复：POST /api/fragments 被登录拦截器按路径放行 → 改为拦截器内按 GET 方法放行，新增回归测试）

**涉及文件：** 无（验收步骤，不改代码）

- [x] **Step 1: 全量测试**

Run: `mvn -q test -f fragment-story/pom.xml`
Expected: 全部通过。

- [x] **Step 2: 手动验证**

- 登录后发布 → status=0，墙上看不到
- 直接把数据库该条 status 改为 1 → 墙上出现；匿名显示「匿名用户」，非匿名显示昵称
- 5 分钟内重复发布 → 429 提示
- 1001 字 → 被拒
- 点赞两次 → 第二次报「已经点过赞了」；取消后 likedByMe=false
- 非作者隐藏/删除别人的碎片 → 403
- 作者隐藏后他人列表不可见；取消隐藏后恢复

- [x] **Step 3: 提交**

```bash
git add fragment-story/src
git commit -m "test: 阶段2验收"
```

## 阶段 3：管理端（后端）

### Task 9: 审核队列、审核通过与硬删除 —— ✅ 已完成（2026-08-12；AdminService/Impl、AdminController、请求体、ConfigSeedRunner 全部实现，注释详细；验收发现并修复：manageList 的 Page 强转时机 bug、用户列表密码哈希泄露）

**涉及文件：**
- `fragment-story/src/main/java/com/storysliver/service/AdminService.java`、`service/impl/AdminServiceImpl.java`——管理业务（审核/硬删除/用户角色/配置）
- `fragment-story/src/main/java/com/storysliver/controller/AdminController.java`——管理接口（@RequireRole 角色校验）
- `fragment-story/src/main/java/com/storysliver/pojo/Admin/AdminRoleRequest.java`、`pojo/Admin/UpdateAdminCodeRequest.java`——管理请求体
- `fragment-story/src/main/java/com/storysliver/config/ConfigSeedRunner.java`——启动时把初始管理员注册密码写入 system_config
- `fragment-story/src/main/java/com/storysliver/auth/RequireRole.java`——管理接口角色校验（使用）
- `fragment-story/src/test/java/com/storysliver/service/AdminServiceTest.java`——单元测试

> 分页注意：`manageList` 同样用 **PageHelper + PageBean**（见代码风格规范）；`selectManagePage()` 不带分页参数，`countManage()` 不再需要。

**Files:**
- Create: `fragment-story/src/main/java/com/storysliver/service/AdminService.java`
- Create: `fragment-story/src/main/java/com/storysliver/service/impl/AdminServiceImpl.java`
- Create: `fragment-story/src/main/java/com/storysliver/controller/AdminController.java`（旧版及旧管理员鉴权文件已在旧代码清理中删除）
- Test: `fragment-story/src/test/java/com/storysliver/service/AdminServiceTest.java`

**Interfaces:**
- Consumes: `StoryFragmentMapper`、`FragmentLikeMapper`、`UserMapper`、`FragmentService.toVO`
- Produces: `AdminService.manageList(Integer status, int pageNum, int pageSize): PageBean`（匿名也显示真实昵称）；`AdminService.approve(Long fragmentId)`；`AdminService.deleteFragment(Long fragmentId)`；`AdminService.users(int pageNum, int pageSize): PageBean`；`AdminService.updateRole(Integer operatorRole, Long targetUserId, Integer role)`；`AdminService.updateAdminCode(Integer operatorRole, String newCode)`

- [x] **Step 1: 写失败测试**

`AdminServiceTest.java`：

```java
/** 管理服务单元测试：审核状态机、硬删除清点赞、角色变更权限、站长保护 */
class AdminServiceTest {
    private StoryFragmentMapper fragmentMapper;
    private FragmentLikeMapper likeMapper;
    private UserMapper userMapper;
    private FragmentService fragmentService;
    private AdminService service;

    @BeforeEach
    void setUp() {
        fragmentMapper = mock(StoryFragmentMapper.class);
        likeMapper = mock(FragmentLikeMapper.class);
        userMapper = mock(UserMapper.class);
        fragmentService = mock(FragmentService.class);
        service = new AdminServiceImpl(fragmentMapper, likeMapper, userMapper, fragmentService);
    }

    private StoryFragment pending(Long id) {
        StoryFragment f = new StoryFragment();
        f.setId(id); f.setUserId(1L); f.setStatus(StoryFragment.STATUS_PENDING);
        return f;
    }

    @Test
    void approve_publishes_pending_fragment() {
        when(fragmentMapper.selectById(1L)).thenReturn(pending(1L));
        service.approve(1L);
        verify(fragmentMapper).updateStatus(1L, StoryFragment.STATUS_PUBLISHED);
    }

    @Test
    void approve_fails_when_not_pending() {
        StoryFragment f = pending(1L);
        f.setStatus(StoryFragment.STATUS_PUBLISHED);
        when(fragmentMapper.selectById(1L)).thenReturn(f);
        assertThrows(BusinessException.class, () -> service.approve(1L));
    }

    @Test
    void delete_fragment_hard_deletes_and_clears_likes() {
        when(fragmentMapper.selectById(1L)).thenReturn(pending(1L));
        service.deleteFragment(1L);
        verify(fragmentMapper).deleteById(1L);
        verify(likeMapper).deleteByFragmentAndUser(1L, null);
    }

    @Test
    void update_role_requires_owner() {
        assertThrows(BusinessException.class, () -> service.updateRole(1, 2L, 1));
    }

    @Test
    void update_role_cannot_modify_owner() {
        User target = new User();
        target.setId(2L); target.setRole(User.ROLE_OWNER);
        when(userMapper.selectById(2L)).thenReturn(target);
        assertThrows(BusinessException.class, () -> service.updateRole(2, 2L, 1));
    }

    @Test
    void update_role_promotes_normal_user() {
        User target = new User();
        target.setId(2L); target.setRole(User.ROLE_USER);
        when(userMapper.selectById(2L)).thenReturn(target);
        service.updateRole(2, 2L, 1);
        verify(userMapper).updateRole(2L, 1);
    }

    @Test
    void update_admin_code_upserts_hashed_value() {
        // 构造器注入真实 encoder 与 mock 的 configMapper，验证新密码以 BCrypt 哈希落库
        SystemConfigMapper configMapper = mock(SystemConfigMapper.class);
        AdminService impl = new AdminServiceImpl(fragmentMapper, likeMapper, userMapper, fragmentService, configMapper, new BCryptPasswordEncoder());
        impl.updateAdminCode(2, "NewCode123");
        ArgumentCaptor<String> value = ArgumentCaptor.forClass(String.class);
        verify(configMapper).upsert(eq(SystemConfig.KEY_ADMIN_REGISTER_CODE), value.capture());
        assertTrue(new BCryptPasswordEncoder().matches("NewCode123", value.getValue()));
    }
}
```

- [x] **Step 2: 运行测试确认失败**

Run: `mvn -q test -f fragment-story/pom.xml -Dtest=AdminServiceTest`
Expected: 编译失败（类不存在）。

- [x] **Step 3: 实现**

```java
/**
 * 管理服务接口：审核、删除、用户管理、配置管理。
 * 为什么权限判断放在 Service 里：Controller 的 @RequireRole 只挡路由，Service 再做一次校验是纵深防御。
 */
public interface AdminService {
    /** 管理列表（按状态筛选：0 待审核 / 1 已发布；null 全部），匿名也显示真实昵称 */
    PageBean manageList(Integer status, int pageNum, int pageSize);
    /** 审核通过：只允许把「待审核」置为「已发布」 */
    void approve(Long fragmentId);
    /** 硬删除：审核不通过 / 删除违规碎片共用 */
    void deleteFragment(Long fragmentId);
    /** 用户分页列表（仅站长） */
    PageBean users(int pageNum, int pageSize);
    /** 指定/撤销管理员（仅站长；不能修改站长本人） */
    void updateRole(Integer operatorRole, Long targetUserId, Integer role);
    /** 修改管理员注册特殊密码（仅站长） */
    void updateAdminCode(Integer operatorRole, String newCode);
}

/**
 * 管理服务实现：审核、硬删除、用户角色、管理员注册密码。
 * 所有方法都先校验操作者角色（纵深防御），再由 Mapper 执行。
 */
@Service
@RequiredArgsConstructor
public class AdminServiceImpl implements AdminService {
    private final StoryFragmentMapper fragmentMapper;
    private final FragmentLikeMapper likeMapper;
    private final UserMapper userMapper;
    private final FragmentService fragmentService;
    private final SystemConfigMapper systemConfigMapper;
    private final PasswordEncoder passwordEncoder;

    /** 管理列表：按状态筛选；匿名碎片也展示真实昵称，方便管理员判断内容归属 */
    @Override
    public PageBean manageList(Integer status, int pageNum, int pageSize) {
        PageHelper.startPage(pageNum, pageSize);
        List<FragmentVO> list = fragmentMapper.selectManagePage(status).stream()
                .map(this::toAdminVO)
                .toList();
        Page<FragmentVO> page = (Page<FragmentVO>) list;
        return new PageBean(page.getTotal(), page.getResult());
    }

    /** 审核通过：只允许把「待审核」变成「已发布」，防止重复/越权审核 */
    @Override
    public void approve(Long fragmentId) {
        StoryFragment f = requireExists(fragmentId);
        if (f.getStatus() != StoryFragment.STATUS_PENDING) {
            throw new BusinessException(ResultCode.FRAGMENT_NOT_PUBLISHED, "只能审核待审核碎片");
        }
        fragmentMapper.updateStatus(fragmentId, StoryFragment.STATUS_PUBLISHED);
    }

    /** 硬删除：删行并清点赞，前端弹窗已提示不可恢复 */
    @Override
    public void deleteFragment(Long fragmentId) {
        requireExists(fragmentId);
        fragmentMapper.deleteById(fragmentId);
        likeMapper.deleteByFragmentAndUser(fragmentId, null);
    }

    /** 用户分页列表（仅站长，由拦截器 + Service 双重校验） */
    @Override
    public PageBean users(int pageNum, int pageSize) {
        PageHelper.startPage(pageNum, pageSize);
        List<User> list = userMapper.selectPage();
        Page<User> page = (Page<User>) list;
        return new PageBean(page.getTotal(), page.getResult());
    }

    /** 指定/撤销管理员：只有站长能操作；站长本人不可被修改（防止权限被锁死） */
    @Override
    public void updateRole(Integer operatorRole, Long targetUserId, Integer role) {
        if (operatorRole == null || operatorRole != User.ROLE_OWNER) {
            throw new BusinessException(ResultCode.FORBIDDEN);
        }
        if (role == null || (role != User.ROLE_USER && role != User.ROLE_ADMIN)) {
            throw new BusinessException(ResultCode.BAD_REQUEST, "角色只能是 0 或 1");
        }
        User target = userMapper.selectById(targetUserId);
        if (target == null) {
            throw new BusinessException(ResultCode.NOT_FOUND, "用户不存在");
        }
        if (target.getRole() == User.ROLE_OWNER) {
            throw new BusinessException(ResultCode.CANNOT_MODIFY_OWNER);
        }
        userMapper.updateRole(targetUserId, role);
    }

    @Override
    public void updateAdminCode(Integer operatorRole, String newCode) {
        if (operatorRole == null || operatorRole != User.ROLE_OWNER) {
            throw new BusinessException(ResultCode.FORBIDDEN);
        }
        if (newCode == null || newCode.length() < 6) {
            throw new BusinessException(ResultCode.BAD_REQUEST, "管理员注册密码至少 6 位");
        }
        systemConfigMapper.upsert(SystemConfig.KEY_ADMIN_REGISTER_CODE, passwordEncoder.encode(newCode));
    }

    private StoryFragment requireExists(Long fragmentId) {
        StoryFragment f = fragmentMapper.selectById(fragmentId);
        if (f == null) {
            throw new BusinessException(ResultCode.NOT_FOUND, "碎片不存在");
        }
        return f;
    }

    /** 管理端专用转换：无论是否匿名都显示真实昵称 */
    private FragmentVO toAdminVO(StoryFragment f) {
        FragmentVO vo = fragmentService.toVO(f, false);
        User author = userMapper.selectById(f.getUserId());
        vo.setAuthorName(author == null ? "未知用户" : author.getNickname());
        return vo;
    }
}
```

`AdminController.java` 重写：

```java
/**
 * 管理接口：审核、删除、用户管理、配置管理。
 * 权限通过 @RequireRole 声明：管理员及以上可审核删除，站长独享用户管理/配置修改。
 */
@RestController
@RequestMapping("/api/admin")
@RequiredArgsConstructor
public class AdminController {
    private final AdminService adminService;

    /** 管理列表/审核队列：status 传 0 看待审核，传 1 看已发布，不传看全部 */
    @GetMapping("/fragments")
    @RequireRole({User.ROLE_ADMIN, User.ROLE_OWNER})
    public Result list(
            @RequestParam(required = false) Integer status,
            @RequestParam(defaultValue = "1") int pageNum,
            @RequestParam(defaultValue = "10") int pageSize) {
        return Result.success(adminService.manageList(status, pageNum, pageSize));
    }

    /** 审核通过：待审核 → 已发布 */
    @PostMapping("/fragments/{id}/approve")
    @RequireRole({User.ROLE_ADMIN, User.ROLE_OWNER})
    public Result approve(@PathVariable Long id) {
        adminService.approve(id);
        return Result.success();
    }

    /** 硬删除：审核不通过 / 删除违规碎片（前端弹窗确认不可恢复） */
    @DeleteMapping("/fragments/{id}")
    @RequireRole({User.ROLE_ADMIN, User.ROLE_OWNER})
    public Result delete(@PathVariable Long id) {
        adminService.deleteFragment(id);
        return Result.success();
    }

    /** 用户列表：仅站长 */
    @GetMapping("/users")
    @RequireRole(User.ROLE_OWNER)
    public Result users(
            @RequestParam(defaultValue = "1") int pageNum,
            @RequestParam(defaultValue = "10") int pageSize) {
        return Result.success(adminService.users(pageNum, pageSize));
    }

    /** 指定/撤销管理员：仅站长；操作者角色从 UserContext 取 */
    @PutMapping("/users/{id}/role")
    @RequireRole(User.ROLE_OWNER)
    public Result updateRole(@PathVariable Long id, @RequestBody AdminRoleRequest request) {
        adminService.updateRole(UserContext.getRole(), id, request.getRole());
        return Result.success();
    }

    /** 修改管理员注册特殊密码：仅站长 */
    @PutMapping("/config/admin-register-code")
    @RequireRole(User.ROLE_OWNER)
    public Result updateAdminCode(@RequestBody UpdateAdminCodeRequest request) {
        adminService.updateAdminCode(UserContext.getRole(), request.getNewCode());
        return Result.success();
    }
}
```

`AdminRoleRequest.java`、`UpdateAdminCodeRequest.java`：

```java
/**
 * 修改用户角色请求体：role 只能传 0（普通）或 1（管理员），由 Service 校验。
 */
@Data
public class AdminRoleRequest {
    @NotNull(message = "角色不能为空")
    private Integer role;
}

/** 修改管理员注册密码请求体 */
@Data
public class UpdateAdminCodeRequest {
    @NotBlank(message = "新密码不能为空")
    private String newCode;
}
```

`ConfigSeedRunner.java`（启动时写入初始管理员注册密码）：

```java
/**
 * 配置种子数据：应用启动时把默认管理员注册密码写入 system_config。
 * 为什么用 CommandLineRunner：SQL 里写死 BCrypt 哈希不方便维护，启动时从 yml 读取并加密落库更清晰。
 * 只会在键不存在时写入，站长改过的密码不会被覆盖。
 */
@Component
@RequiredArgsConstructor
public class ConfigSeedRunner implements CommandLineRunner {
    private final SystemConfigMapper systemConfigMapper;
    private final PasswordEncoder passwordEncoder;
    /** 默认注册密码来自 application.yml 的 app.admin.default-register-code */
    @Value("${app.admin.default-register-code}")
    private String defaultRegisterCode;

    @Override
    public void run(String... args) {
        // 已有配置（站长改过）就跳过，避免每次启动重置密码
        if (systemConfigMapper.selectByKey(SystemConfig.KEY_ADMIN_REGISTER_CODE) == null) {
            systemConfigMapper.upsert(SystemConfig.KEY_ADMIN_REGISTER_CODE, passwordEncoder.encode(defaultRegisterCode));
        }
    }
}
```

- [x] **Step 4: 运行测试确认通过**

Run: `mvn -q test -f fragment-story/pom.xml -Dtest=AdminServiceTest`
Expected: 全部通过。

- [x] **Step 5: 手工冒烟**

Run: 启动应用（自动写入初始管理员注册密码）；用 `<管理员注册密码>` 注册 → role=2；用该账号调 `/api/admin/fragments?status=0`、approve、delete；普通用户调 `/api/admin/**` → 403。

- [x] **Step 6: 提交**

```bash
git add fragment-story/src
git commit -m "feat: 管理端审核与权限"
```

### Task 10: 阶段 3 验收 —— ✅ 已完成（2026-08-12；管理端验收通过：403 权限拦截、审核上墙、用户管理、改注册密码、管理员权限即时生效、ConfigSeedRunner 播种验证）

**涉及文件：** 无（验收步骤，不改代码）

- [x] **Step 1: 全量测试**

Run: `mvn -q test -f fragment-story/pom.xml`
Expected: 全部通过。

- [x] **Step 2: 手动验证**

- 管理员看到待审核队列及真实发布者昵称（匿名碎片也显示）
- 通过后碎片出现在首页；不通过则数据库行被删
- 管理员删除已发布碎片 → 首页消失且数据库无记录
- 站长能把普通用户设为管理员/撤销；普通管理员调用用户管理 → 403
- 站长修改管理员注册密码后，旧密码注册失败、新密码成功

- [x] **Step 3: 提交**

```bash
git add fragment-story/src
git commit -m "test: 阶段3验收"
```

## 阶段 4：前端（fragment‑story‑frontend）

### Task 11: 前端脚手架 —— ✅ 已完成（2026-08-12：Vite+Vue3+Router+Axios+userStore+auth/fragments API 全部就绪；`api/admin.js` 随 Task 15 管理后台一并补）

**涉及文件（全部新建于 `fragment‑story‑frontend/`）：**
- `package.json`、`vite.config.js`、`index.html`——脚手架与 `/api` 开发代理
- `src/main.js`、`src/App.vue`、`src/styles/main.css`——入口、导航布局、全局样式（温暖文字流）
- `src/router/index.js`——路由与登录/管理员守卫
- `src/stores/user.js`——登录状态（localStorage 持久化）
- `src/api/request.js`、`src/api/auth.js`、`src/api/fragments.js`、`src/api/admin.js`——Axios 封装与接口模块

**Files:** 全部新建于 `fragment‑story‑frontend/`：`package.json`、`vite.config.js`、`index.html`、`src/main.js`、`src/App.vue`、`src/styles/main.css`、`src/router/index.js`、`src/stores/user.js`、`src/api/request.js`、`src/api/auth.js`、`src/api/fragments.js`、`src/api/admin.js`

**Interfaces:**
- Produces: `userStore{token,user,setLogin,setUser,logout,isAdmin,isOwner}`；`request`（Axios 实例，自动带 token，响应直接返回 `data`）；`router`（hash 模式，/my 需登录、/admin 需管理员）

- [ ] **Step 1: 创建脚手架文件**

`package.json`：

```json
{
  "name": "fragment-story-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": { "dev": "vite", "build": "vite build", "preview": "vite preview" },
  "dependencies": {
    "axios": "^1.7.2",
    "vue": "^3.4.31",
    "vue-router": "^4.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.5",
    "vite": "^5.3.3"
  }
}
```

`vite.config.js`：

```js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
export default defineConfig({
  plugins: [vue()],
  server: { proxy: { '/api': 'http://localhost:8080' } }
})
```

`index.html`：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>故事碎片墙</title>
</head>
<body>
  <div id="app"></div>
  <script type="module" src="/src/main.js"></script>
</body>
</html>
```

`src/main.js`：

```js
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'
import './styles/main.css'
createApp(App).use(router).mount('#app')
```

`src/stores/user.js`：

```js
import { reactive } from 'vue'
const saved = JSON.parse(localStorage.getItem('story-user') || 'null')
export const userStore = reactive({
  token: saved?.token || '',
  user: saved?.user || null,
  setLogin(token, user) { this.token = token; this.user = user; this.persist() },
  setUser(user) { this.user = user; this.persist() },
  logout() { this.token = ''; this.user = null; localStorage.removeItem('story-user') },
  persist() { localStorage.setItem('story-user', JSON.stringify({ token: this.token, user: this.user })) },
  get isAdmin() { return !!this.user && this.user.role >= 1 },
  get isOwner() { return !!this.user && this.user.role === 2 }
})
```

`src/router/index.js`（使用 hash 模式，Spring Boot 托管无需 SPA fallback）：

```js
import { createRouter, createWebHashHistory } from 'vue-router'
import { userStore } from '../stores/user'
import HomeView from '../views/HomeView.vue'
import LoginView from '../views/LoginView.vue'
import MyFragmentsView from '../views/MyFragmentsView.vue'
import AdminView from '../views/AdminView.vue'
const routes = [
  { path: '/', component: HomeView },
  { path: '/login', component: LoginView },
  { path: '/my', component: MyFragmentsView, meta: { requiresAuth: true } },
  { path: '/admin', component: AdminView, meta: { requiresAuth: true, requiresAdmin: true } }
]
const router = createRouter({ history: createWebHashHistory(), routes })
router.beforeEach((to) => {
  if (to.meta.requiresAuth && !userStore.token) return '/login'
  if (to.meta.requiresAdmin && !userStore.isAdmin) return '/'
})
export default router
```

`src/api/request.js`：

```js
import axios from 'axios'
import { userStore } from '../stores/user'
const request = axios.create({ baseURL: '/api', timeout: 10000 })
request.interceptors.request.use((config) => {
  if (userStore.token) config.headers.Authorization = `Bearer ${userStore.token}`
  return config
})
request.interceptors.response.use(
  (res) => {
    const body = res.data
    if (body.code !== 200) return Promise.reject(new Error(body.message || '请求失败'))
    return body.data
  },
  (err) => {
    if (err.response?.status === 401) {
      userStore.logout()
      if (!location.hash.startsWith('#/login')) location.hash = '#/login'
    }
    const msg = err.response?.data?.message || err.message || '网络错误'
    return Promise.reject(new Error(msg))
  }
)
export default request
```

`src/api/auth.js`、`src/api/fragments.js`、`src/api/admin.js`：

```js
// auth.js
import request from './request'
export const getCaptcha = () => request.get('/auth/captcha')
export const register = (data) => request.post('/auth/register', data)
export const login = (data) => request.post('/auth/login', data)
export const getMe = () => request.get('/users/me')

// fragments.js
import request from './request'
export const getFragments = (pageNum, pageSize) => request.get('/fragments', { params: { pageNum, pageSize } })
export const submitFragment = (content, isAnonymous) => request.post('/fragments', { content, isAnonymous })
export const likeFragment = (id) => request.post(`/fragments/${id}/like`)
export const unlikeFragment = (id) => request.delete(`/fragments/${id}/like`)
export const getMyFragments = () => request.get('/fragments/my')
export const hideFragment = (id) => request.put(`/fragments/${id}/hide`)
export const unhideFragment = (id) => request.put(`/fragments/${id}/unhide`)
export const deleteFragment = (id) => request.delete(`/fragments/${id}`)

// admin.js
import request from './request'
export const getAdminFragments = (status, pageNum, pageSize) => request.get('/admin/fragments', { params: { status, pageNum, pageSize } })
export const approveFragment = (id) => request.post(`/admin/fragments/${id}/approve`)
export const adminDeleteFragment = (id) => request.delete(`/admin/fragments/${id}`)
export const getUsers = (pageNum, pageSize) => request.get('/admin/users', { params: { pageNum, pageSize } })
export const updateUserRole = (id, role) => request.put(`/admin/users/${id}/role`, { role })
export const updateAdminCode = (newCode) => request.put('/admin/config/admin-register-code', { newCode })
```

`src/App.vue`：

```vue
<script setup>
import { useRouter } from 'vue-router'
import { userStore } from './stores/user'
const router = useRouter()
function logout() { userStore.logout(); router.push('/login') }
</script>
<template>
  <div class="app">
    <nav class="nav">
      <span class="brand">故事碎片墙</span>
      <div class="nav-links">
        <router-link to="/">首页</router-link>
        <router-link v-if="userStore.token" to="/my">我的碎片</router-link>
        <router-link v-if="userStore.isAdmin" to="/admin">管理</router-link>
        <button v-if="userStore.token" class="link" @click="logout">退出</button>
        <router-link v-else to="/login">登录</router-link>
      </div>
    </nav>
    <router-view />
  </div>
</template>
```

`src/styles/main.css`：

```css
* { box-sizing: border-box; }
body { margin: 0; background: #f7f3ea; color: #4a4238; font-family: Georgia, "Songti SC", "SimSun", serif; line-height: 1.7; }
.app { max-width: 720px; margin: 0 auto; padding: 0 16px 64px; }
.nav { display: flex; justify-content: space-between; align-items: center; padding: 18px 0; border-bottom: 1px solid #e5dcc8; margin-bottom: 24px; }
.brand { font-size: 20px; letter-spacing: 2px; }
.nav-links { display: flex; gap: 16px; align-items: center; }
.nav-links a, .link { color: #8a7a5c; text-decoration: none; background: none; border: none; cursor: pointer; font: inherit; }
.nav-links a.router-link-active { color: #4a4238; font-weight: bold; }
.page-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16px; }
h1 { font-size: 26px; }
.card { background: #fbf8f1; border: 1px solid #eadfc8; border-radius: 10px; padding: 18px 20px; margin-bottom: 14px; box-shadow: 0 2px 8px rgba(74, 66, 56, .05); }
.meta { display: flex; gap: 12px; align-items: center; font-size: 13px; color: #a39476; margin-bottom: 8px; }
.author { color: #6d5f47; }
.content { white-space: pre-wrap; margin: 0 0 10px; font-size: 15px; }
.actions { display: flex; gap: 10px; align-items: center; }
.like-btn { background: none; border: none; cursor: pointer; color: #b48a6a; font-size: 14px; }
.like-btn.liked { color: #c96f4a; }
.btn { background: #eee4cf; border: 1px solid #dccba7; border-radius: 6px; padding: 6px 14px; cursor: pointer; font: inherit; font-size: 14px; }
.btn.primary { background: #4a4238; color: #f7f3ea; border-color: #4a4238; }
.btn.small { padding: 3px 10px; font-size: 13px; }
.btn.danger { background: #f3e2dd; border-color: #d9b3aa; color: #a3483a; }
.btn:disabled { opacity: .5; cursor: not-allowed; }
.input, .textarea { width: 100%; padding: 9px 12px; border: 1px solid #dccba7; border-radius: 6px; background: #fffdf8; font: inherit; margin-bottom: 10px; }
.textarea { resize: vertical; }
.count { text-align: right; font-size: 12px; color: #a39476; }
.checkbox { display: flex; align-items: center; gap: 6px; margin: 10px 0; font-size: 14px; }
.error { color: #a3483a; font-size: 14px; }
.toast { background: #4a4238; color: #f7f3ea; padding: 8px 14px; border-radius: 6px; font-size: 14px; }
.modal-mask { position: fixed; inset: 0; background: rgba(74, 66, 56, .35); display: flex; align-items: center; justify-content: center; z-index: 10; }
.modal { background: #fbf8f1; border-radius: 10px; padding: 24px; width: 420px; max-width: 92vw; }
.modal.small { width: 360px; }
.modal-actions { display: flex; justify-content: flex-end; gap: 10px; margin-top: 14px; }
.danger-text { color: #a3483a; }
.tag { font-size: 12px; padding: 1px 8px; border-radius: 10px; background: #eee4cf; color: #6d5f47; }
.tag.pending { background: #f3e2b8; color: #8a6a1f; }
.tag.hidden { background: #e2e0da; color: #6a675f; }
.tabs { display: flex; gap: 8px; margin-bottom: 16px; }
.tabs button { background: none; border: 1px solid #dccba7; border-radius: 6px; padding: 6px 14px; cursor: pointer; font: inherit; }
.tabs button.active { background: #4a4238; color: #f7f3ea; }
.table { width: 100%; border-collapse: collapse; background: #fbf8f1; }
.table th, .table td { border: 1px solid #eadfc8; padding: 8px 10px; font-size: 14px; text-align: left; }
.auth-page { display: flex; justify-content: center; padding-top: 40px; }
.auth-card { width: 380px; max-width: 92vw; background: #fbf8f1; border: 1px solid #eadfc8; border-radius: 10px; padding: 28px; }
.captcha-row { display: flex; gap: 10px; align-items: center; }
.captcha-img { height: 40px; cursor: pointer; border-radius: 6px; }
```

- [ ] **Step 2: 安装依赖并启动**

Run: `npm install`（在 `fragment‑story‑frontend` 下）
Run: `npm run dev`
Expected: 打开 `http://localhost:5173` 看到导航栏和空页面，无报错。

- [ ] **Step 3: 提交**

```bash
git add .
git commit -m "feat: 前端脚手架"
```

### Task 12: 登录 / 注册页 —— ✅ 登录/注册页已完成并通过前后端联调（2026-08-12，电脑 + 手机同一 WiFi 均可访问；修复：注册失败验证码不自动刷新——失败后自动换新验证码）

**联调期间修复的前端 bug（已解决）：**
- `Result` 的提示字段是 `text`，前端拦截器误读 `message` → 统一改读 `text`
- 登录/注册返回的是 `{token: "..."}`，前端误把整个对象当 token 存 → 改为取 `.token`
- 路由守卫：未登录访问首页应跳登录页（`/` 加 `meta.requiresAuth`）

**涉及文件：**
- `fragment‑story‑frontend/src/views/LoginView.vue`——登录/注册页（含「注册为管理员」与验证码）
- `fragment‑story‑frontend/src/components/CaptchaField.vue`——验证码组件（显示图片、输入答案）

**Files:**
- Create: `fragment‑story‑frontend/src/views/LoginView.vue`
- Create: `fragment‑story‑frontend/src/components/CaptchaField.vue`

**Interfaces:**
- Consumes: `api/auth.js`、`userStore`
- Produces: 登录 / 注册表单（注册含验证码、昵称、「注册为管理员」+ 特殊密码）

- [ ] **Step 1: 写组件**

`CaptchaField.vue`：

```vue
<script setup>
import { onMounted, reactive, watch } from 'vue'
import { getCaptcha } from '../api/auth'
const emit = defineEmits(['update'])
const state = reactive({ key: '', answer: '', img: '' })
async function refresh() {
  const data = await getCaptcha()
  state.key = data.captchaKey
  state.img = data.imageBase64
  state.answer = ''
}
watch(() => state.answer, (v) => emit('update', { key: state.key, answer: v }))
onMounted(refresh)
</script>
<template>
  <div class="captcha-row">
    <img v-if="state.img" :src="state.img" class="captcha-img" alt="验证码" title="点我刷新" @click="refresh" />
    <input v-model="state.answer" class="input" placeholder="验证码答案" required />
  </div>
</template>
```

`LoginView.vue`：

```vue
<script setup>
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import { login, register, getMe } from '../api/auth'
import { userStore } from '../stores/user'
import CaptchaField from '../components/CaptchaField.vue'

const router = useRouter()
const mode = ref('login')
const form = ref({ username: '', nickname: '', password: '', isAdmin: false, adminCode: '' })
const captcha = ref({ key: '', answer: '' })
const error = ref('')
const submitting = ref(false)

async function submit() {
  error.value = ''
  submitting.value = true
  try {
    let token
    if (mode.value === 'login') {
      token = await login({ username: form.value.username, password: form.value.password })
    } else {
      token = await register({
        username: form.value.username,
        nickname: form.value.nickname,
        password: form.value.password,
        isAdmin: form.value.isAdmin,
        adminCode: form.value.adminCode || null,
        captchaKey: captcha.value.key,
        captchaAnswer: captcha.value.answer
      })
    }
    userStore.setLogin(token, null)
    const me = await getMe()
    userStore.setUser(me)
    router.push(me.role >= 1 ? '/admin' : '/')
  } catch (e) {
    error.value = e.message
  } finally {
    submitting.value = false
  }
}
</script>
<template>
  <div class="auth-page">
    <div class="auth-card">
      <h2>{{ mode === 'login' ? '登录' : '注册' }}</h2>
      <p class="subtitle">匿名故事碎片墙</p>
      <form @submit.prevent="submit">
        <input v-model="form.username" class="input" placeholder="用户名" required />
        <input v-if="mode === 'register'" v-model="form.nickname" class="input" placeholder="昵称" required />
        <input v-model="form.password" type="password" class="input" placeholder="密码（至少 6 位）" required />
        <CaptchaField v-if="mode === 'register'" @update="captcha = $event" />
        <label v-if="mode === 'register'" class="checkbox">
          <input type="checkbox" v-model="form.isAdmin" /> 注册为管理员
        </label>
        <input v-if="mode === 'register' && form.isAdmin" v-model="form.adminCode" type="password" class="input" placeholder="管理员注册特殊密码" />
        <p v-if="error" class="error">{{ error }}</p>
        <button class="btn primary" type="submit" :disabled="submitting">
          {{ submitting ? '请稍候…' : mode === 'login' ? '登录' : '注册' }}
        </button>
      </form>
      <button class="link" @click="mode = mode === 'login' ? 'register' : 'login'">
        {{ mode === 'login' ? '没有账号？去注册' : '已有账号？去登录' }}
      </button>
    </div>
  </div>
</template>
```

- [ ] **Step 2: 手动验证**

Run: `npm run dev`，访问 `http://localhost:5173/#/login`
Expected: 注册流程（验证码刷新、昵称必填、勾选管理员后出现特殊密码框）与登录流程可用；错误提示可见。

- [ ] **Step 3: 提交**

```bash
git add .
git commit -m "feat: 登录注册页"
```

### Task 13: 首页碎片墙（列表 / 发布 / 点赞） —— ✅ 已完成（2026-08-12：发布框、列表、加载更多、点赞/取消、匿名发布；新增身份铭牌：站长金色/管理员蓝色显示在非匿名碎片作者旁，个人主页新增 /profile 页面；布局经可视化比选后定为**方案 C 瀑布流**——桌面 3 列/平板 2 列/手机 1 列（CSS Grid + dense）；**长碎片（≥200 字）自动独占一整行**；布局细节修复：标题/品牌不换行、导航与页头 flex-wrap、头像审核提示与铭牌分行显示）

> 功能追加（2026-08-12）：**头像上传与审核**——`user` 表新增 `avatar`（已审核）/`avatar_pending`（待审核）/`avatar_reject_reason`（拒绝原因）；`POST /api/users/avatar` 上传（jpg/png、≤2MB、128x128 保留透明通道存 PNG，存本地 `D:/HeadImage`）；管理端 `GET /api/admin/avatars` 审核队列 + `approve`/`reject`（**点击拒绝才展开原因输入框（带展开动画）+ 提交/取消；不填原因默认「头像不符合要求」**；拒绝时同步删除本地图片文件）；**前端圆形裁剪**：选图后弹裁剪框，**直接拖动圆形移动位置、拖右下角圆点调整大小**，裁成圆形 PNG 再上传；个人主页显示头像/上传/「审核中」/「被拒绝原因」（**可点「知道了」手动关闭**，重新上传或通过后自动消失）；修复：UserMapper 查询漏带 avatar 字段、头像通过后前端刷新仍显示审核中（App.vue 启动时同步 `/me`）。

**涉及文件：**
- `fragment‑story‑frontend/src/views/HomeView.vue`——首页（列表/加载更多/发布/点赞）
- `fragment‑story‑frontend/src/components/FragmentCard.vue`——碎片卡片
- `fragment‑story‑frontend/src/components/SubmitModal.vue`——发布弹窗（1000 字统计、匿名开关）
- `fragment‑story‑frontend/src/components/LoadMore.vue`、`EmptyState.vue`——加载更多、空状态组件

**Files:**
- Create: `fragment‑story‑frontend/src/views/HomeView.vue`
- Create: `fragment‑story‑frontend/src/components/FragmentCard.vue`
- Create: `fragment‑story‑frontend/src/components/SubmitModal.vue`
- Create: `fragment‑story‑frontend/src/components/LoadMore.vue`
- Create: `fragment‑story‑frontend/src/components/EmptyState.vue`

**Interfaces:**
- Consumes: `api/fragments.js`、`userStore`
- Produces: 首页列表 + 加载更多 + 发布弹窗（1000 字统计、匿名开关）+ 点赞/取消

- [x] **Step 1: 写组件**

`FragmentCard.vue`：

```vue
<script setup>
defineProps({ fragment: Object, showActions: Boolean })
defineEmits(['like', 'delete', 'hide', 'unhide'])
</script>
<template>
  <article class="card fragment-card">
    <div class="meta">
      <span class="author">{{ fragment.authorName }}</span>
      <span class="time">{{ fragment.createdAt }}</span>
      <span v-if="fragment.status === 0" class="tag pending">待审核</span>
      <span v-else-if="fragment.status === 2" class="tag hidden">已隐藏</span>
    </div>
    <p class="content">{{ fragment.content }}</p>
    <div class="actions">
      <button class="like-btn" :class="{ liked: fragment.likedByMe }" @click="$emit('like', fragment)">
        ♡ {{ fragment.likeCount }}
      </button>
      <template v-if="showActions">
        <button v-if="fragment.status === 1" class="btn small" @click="$emit('hide', fragment)">隐藏</button>
        <button v-else-if="fragment.status === 2" class="btn small" @click="$emit('unhide', fragment)">取消隐藏</button>
        <button class="btn small danger" @click="$emit('delete', fragment)">删除</button>
      </template>
    </div>
  </article>
</template>
```

`SubmitModal.vue`：

```vue
<script setup>
import { ref, watch } from 'vue'
import { submitFragment } from '../api/fragments'
const props = defineProps({ show: Boolean })
const emit = defineEmits(['close', 'submitted'])
const content = ref('')
const isAnonymous = ref(false)
const error = ref('')
const submitting = ref(false)
watch(() => props.show, (v) => { if (v) { content.value = ''; isAnonymous.value = false; error.value = '' } })
async function submit() {
  error.value = ''
  submitting.value = true
  try {
    await submitFragment(content.value, isAnonymous.value)
    emit('submitted')
    emit('close')
  } catch (e) {
    error.value = e.message
  } finally {
    submitting.value = false
  }
}
</script>
<template>
  <div v-if="show" class="modal-mask" @click.self="emit('close')">
    <div class="modal">
      <h3>写下你的碎片</h3>
      <textarea v-model="content" class="textarea" maxlength="1000" rows="6" placeholder="一小段故事，或今天的一点生活…"></textarea>
      <div class="count">{{ content.length }}/1000</div>
      <label class="checkbox"><input type="checkbox" v-model="isAnonymous" /> 匿名发布</label>
      <p v-if="error" class="error">{{ error }}</p>
      <div class="modal-actions">
        <button class="btn" @click="emit('close')">取消</button>
        <button class="btn primary" :disabled="submitting || !content.trim()" @click="submit">发布</button>
      </div>
    </div>
  </div>
</template>
```

`LoadMore.vue`、`EmptyState.vue`：

```vue
<!-- LoadMore.vue -->
<script setup>
defineProps({ loading: Boolean, hasMore: Boolean })
defineEmits(['load'])
</script>
<template>
  <div v-if="hasMore" style="text-align:center;margin:20px 0">
    <button class="btn" :disabled="loading" @click="$emit('load')">{{ loading ? '加载中…' : '加载更多' }}</button>
  </div>
</template>

<!-- EmptyState.vue -->
<script setup>
defineProps({ text: String })
</script>
<template>
  <div style="text-align:center;color:#a39476;padding:48px 0">{{ text }}</div>
</template>
```

`HomeView.vue`：

```vue
<script setup>
import { onMounted, ref } from 'vue'
import { getFragments, likeFragment, unlikeFragment } from '../api/fragments'
import { userStore } from '../stores/user'
import FragmentCard from '../components/FragmentCard.vue'
import SubmitModal from '../components/SubmitModal.vue'
import LoadMore from '../components/LoadMore.vue'
import EmptyState from '../components/EmptyState.vue'

const fragments = ref([])
const pageNum = ref(0)
const pageSize = 10
const hasMore = ref(true)
const loading = ref(false)
const showSubmit = ref(false)
const message = ref('')
let timer = null
function notice(text) { message.value = text; clearTimeout(timer); timer = setTimeout(() => (message.value = ''), 3000) }

async function loadMore() {
  if (loading.value || !hasMore.value) return
  loading.value = true
  try {
    const data = await getFragments(pageNum.value + 1, pageSize)
    fragments.value.push(...data.list)
    pageNum.value = data.pageNum
    hasMore.value = fragments.value.length < data.total
  } catch (e) { notice(e.message) } finally { loading.value = false }
}

async function toggleLike(fragment) {
  if (!userStore.token) { notice('请先登录'); return }
  try {
    if (fragment.likedByMe) {
      await unlikeFragment(fragment.id)
      fragment.likedByMe = false
      fragment.likeCount--
    } else {
      await likeFragment(fragment.id)
      fragment.likedByMe = true
      fragment.likeCount++
    }
  } catch (e) { notice(e.message) }
}

function onSubmitted() { notice('已提交，等待管理员审核'); loadMore() }
onMounted(loadMore)
</script>
<template>
  <div class="page">
    <div class="page-head">
      <h1>故事碎片墙</h1>
      <button class="btn primary" @click="showSubmit = true">写一块碎片</button>
    </div>
    <p v-if="message" class="toast">{{ message }}</p>
    <EmptyState v-if="!loading && fragments.length === 0" text="还没有碎片，来写第一块吧" />
    <FragmentCard v-for="f in fragments" :key="f.id" :fragment="f" @like="toggleLike" />
    <LoadMore :loading="loading" :has-more="hasMore" @load="loadMore" />
    <SubmitModal :show="showSubmit" @close="showSubmit = false" @submitted="onSubmitted" />
  </div>
</template>
```

- [x] **Step 2: 手动验证**

Run: `npm run dev`
Expected: 首页显示已发布碎片；未登录点赞提示登录；登录后可点赞/取消；发布弹窗 1000 字统计、匿名开关；发布后提示「等待管理员审核」。

- [x] **Step 3: 提交**

```bash
git add .
git commit -m "feat: 首页碎片墙"
```

### Task 14: 我的碎片页 —— ✅ 已完成（2026-08-12：全部状态列表、隐藏/取消隐藏、删除弹窗确认）

**涉及文件：**
- `fragment‑story‑frontend/src/views/MyFragmentsView.vue`——我的碎片页（隐藏/取消隐藏/删除）
- `fragment‑story‑frontend/src/components/ConfirmDialog.vue`——删除确认弹窗（「删除后无法撤回」）

**Files:**
- Create: `fragment‑story‑frontend/src/views/MyFragmentsView.vue`
- Create: `fragment‑story‑frontend/src/components/ConfirmDialog.vue`

**Interfaces:**
- Consumes: `api/fragments.js`
- Produces: 我的碎片列表（状态标签）+ 隐藏/取消隐藏 + 删除（弹窗确认）

- [x] **Step 1: 写组件**

`ConfirmDialog.vue`：

```vue
<script setup>
defineProps({ show: Boolean, title: String, message: String })
const emit = defineEmits(['confirm', 'cancel'])
</script>
<template>
  <div v-if="show" class="modal-mask" @click.self="emit('cancel')">
    <div class="modal small">
      <h3>{{ title }}</h3>
      <p class="danger-text">{{ message }}</p>
      <div class="modal-actions">
        <button class="btn" @click="emit('cancel')">取消</button>
        <button class="btn danger" @click="emit('confirm')">确认删除</button>
      </div>
    </div>
  </div>
</template>
```

`MyFragmentsView.vue`：

```vue
<script setup>
import { onMounted, ref } from 'vue'
import { getMyFragments, hideFragment, unhideFragment, deleteFragment } from '../api/fragments'
import FragmentCard from '../components/FragmentCard.vue'
import ConfirmDialog from '../components/ConfirmDialog.vue'
import EmptyState from '../components/EmptyState.vue'

const fragments = ref([])
const loading = ref(true)
const confirmTarget = ref(null)
const message = ref('')
async function load() { loading.value = true; try { fragments.value = await getMyFragments() } catch (e) { message.value = e.message } finally { loading.value = false } }
async function onHide(f) { try { await hideFragment(f.id); f.status = 2 } catch (e) { message.value = e.message } }
async function onUnhide(f) { try { await unhideFragment(f.id); f.status = 1 } catch (e) { message.value = e.message } }
async function onConfirmDelete() {
  try {
    await deleteFragment(confirmTarget.value.id)
    fragments.value = fragments.value.filter((x) => x.id !== confirmTarget.value.id)
  } catch (e) { message.value = e.message } finally { confirmTarget.value = null }
}
onMounted(load)
</script>
<template>
  <div class="page">
    <h1>我的碎片</h1>
    <p v-if="message" class="error">{{ message }}</p>
    <EmptyState v-if="!loading && fragments.length === 0" text="你还没有发过碎片" />
    <FragmentCard v-for="f in fragments" :key="f.id" :fragment="f" show-actions
      @hide="onHide" @unhide="onUnhide" @delete="confirmTarget = f" />
    <ConfirmDialog :show="!!confirmTarget" title="删除碎片" message="删除后无法撤回，确定删除吗？"
      @cancel="confirmTarget = null" @confirm="onConfirmDelete" />
  </div>
</template>
```

- [x] **Step 2: 手动验证**

Expected: 我的碎片显示全部状态；隐藏后首页不可见、取消隐藏恢复；删除弹窗提示「删除后无法撤回」。

- [x] **Step 3: 提交**

```bash
git add .
git commit -m "feat: 我的碎片页"
```

### Task 15: 管理后台页 —— ✅ 已完成（2026-08-12：待审核/已发布/头像审核/用户管理（站长）/配置（站长）五个 Tab；`api/admin.js` 补齐；**新增封禁功能**：审核卡片可「删除并封禁」，封禁弹窗支持**自定义天数或永久**（到期自动解封）+ **封禁理由必填**（被封禁用户登录时可见），用户管理可封禁/解封（仅站长可解封），用户列表**三态区分**（正常/暂时封禁至日期/永久封禁 + 理由）+ 状态筛选，封禁后登录返回 403 且提示带理由，旧 token 同步失效，**登录态中被封禁会弹出提示并自动退出登录**；**封禁权限矩阵**：站长最高权限（任何人不可封禁站长），管理员不能封禁管理员（仅站长可封禁管理员），普通用户管理员/站长均可封禁）

**涉及文件：**
- `fragment‑story‑frontend/src/views/AdminView.vue`——管理后台页（待审核/已发布/用户管理/配置四个 Tab）

**Files:**
- Create: `fragment‑story‑frontend/src/views/AdminView.vue`

**Interfaces:**
- Consumes: `api/admin.js`、`userStore`
- Produces: 三个 Tab（待审核 / 已发布 / 用户+配置，后两个仅站长）

- [x] **Step 1: 写页面**

`AdminView.vue`：

```vue
<script setup>
import { onMounted, ref } from 'vue'
import { getAdminFragments, approveFragment, adminDeleteFragment, getUsers, updateUserRole, updateAdminCode } from '../api/admin'
import { userStore } from '../stores/user'
import ConfirmDialog from '../components/ConfirmDialog.vue'
import EmptyState from '../components/EmptyState.vue'

const tab = ref('review')
const fragments = ref([])
const users = ref([])
const confirmTarget = ref(null)
const newCode = ref('')
const message = ref('')

async function loadFragments(status) {
  try { const d = await getAdminFragments(status, 1, 50); fragments.value = d.list }
  catch (e) { message.value = e.message }
}
async function loadUsers() {
  try { const d = await getUsers(1, 100); users.value = d.list }
  catch (e) { message.value = e.message }
}
async function onApprove(f) {
  try { await approveFragment(f.id); await loadFragments(tab.value === 'review' ? 0 : null) }
  catch (e) { message.value = e.message }
}
async function onConfirmDelete() {
  try {
    await adminDeleteFragment(confirmTarget.value.id)
    confirmTarget.value = null
    await loadFragments(tab.value === 'review' ? 0 : null)
  } catch (e) { message.value = e.message }
}
async function onRole(u, role) {
  try { await updateUserRole(u.id, role); await loadUsers() } catch (e) { message.value = e.message }
}
async function onSaveCode() {
  try { await updateAdminCode(newCode.value); message.value = '已更新'; newCode.value = '' } catch (e) { message.value = e.message }
}
function switchTab(t) {
  tab.value = t
  if (t === 'review') loadFragments(0)
  else if (t === 'published') loadFragments(1)
  else if (t === 'users') loadUsers()
}
onMounted(() => loadFragments(0))
</script>
<template>
  <div class="page">
    <h1>管理后台</h1>
    <nav class="tabs">
      <button :class="{ active: tab === 'review' }" @click="switchTab('review')">待审核</button>
      <button :class="{ active: tab === 'published' }" @click="switchTab('published')">已发布</button>
      <button v-if="userStore.isOwner" :class="{ active: tab === 'users' }" @click="switchTab('users')">用户</button>
      <button v-if="userStore.isOwner" :class="{ active: tab === 'config' }" @click="tab = 'config'">配置</button>
    </nav>
    <p v-if="message" class="toast">{{ message }}</p>

    <template v-if="tab === 'review' || tab === 'published'">
      <EmptyState v-if="fragments.length === 0" text="没有碎片" />
      <article v-for="f in fragments" :key="f.id" class="card">
        <div class="meta">
          <span class="author">{{ f.authorName }}</span>
          <span class="time">{{ f.createdAt }}</span>
          <span class="tag">{{ f.status === 0 ? '待审核' : '已发布' }}</span>
        </div>
        <p class="content">{{ f.content }}</p>
        <div class="actions">
          <button v-if="f.status === 0" class="btn small primary" @click="onApprove(f)">通过</button>
          <button class="btn small danger" @click="confirmTarget = f">删除</button>
        </div>
      </article>
    </template>

    <template v-if="tab === 'users'">
      <table class="table">
        <tr><th>ID</th><th>用户名</th><th>昵称</th><th>角色</th><th>操作</th></tr>
        <tr v-for="u in users" :key="u.id">
          <td>{{ u.id }}</td><td>{{ u.username }}</td><td>{{ u.nickname }}</td>
          <td>{{ u.role === 2 ? '站长' : u.role === 1 ? '管理员' : '普通' }}</td>
          <td>
            <button v-if="u.role === 0" class="btn small" @click="onRole(u, 1)">设为管理员</button>
            <button v-else-if="u.role === 1" class="btn small danger" @click="onRole(u, 0)">撤销管理员</button>
            <span v-else>—</span>
          </td>
        </tr>
      </table>
    </template>

    <template v-if="tab === 'config'">
      <div class="card">
        <h3>管理员注册特殊密码</h3>
        <input v-model="newCode" class="input" type="password" placeholder="新密码（至少 6 位）" />
        <button class="btn primary" @click="onSaveCode">保存</button>
      </div>
    </template>

    <ConfirmDialog :show="!!confirmTarget" title="删除碎片" message="删除后无法撤回，确定删除吗？"
      @cancel="confirmTarget = null" @confirm="onConfirmDelete" />
  </div>
</template>
```

- [x] **Step 2: 手动验证**

Expected: 管理员看到待审核/已发布两个 Tab；站长额外看到用户与配置 Tab；删除有二次确认弹窗。

- [x] **Step 3: 提交**

```bash
git add .
git commit -m "feat: 管理后台页"
```

### Task 16: 阶段 4 验收 —— 🔧 进行中（2026-08-12：前端 `npm run build` 通过；浏览器人工走查待用户确认）

**涉及文件：** 无（验收步骤，不改代码）

- [ ] **Step 1: 构建**

Run: `npm run build`
Expected: 生成 `dist/`，无报错。

- [ ] **Step 2: 全流程走查（前端 + 后端本地联调）**

- 注册（含验证码）→ 自动登录 → 首页
- 发布 → 提示等待审核 → 管理员审核通过 → 首页可见（匿名/实名两种）
- 点赞/取消、我的碎片隐藏/取消隐藏、删除弹窗
- 普通用户访问 /#/admin 被守卫拦回首页

- [ ] **Step 3: 提交**

```bash
git add .
git commit -m "test: 阶段4验收"
```

## 阶段 5：联调与部署

### Task 17: 后端托管前端静态文件 —— ✅ 已完成（2026-08-12：前端 `dist` 已打入后端 `static`，`mvn package` 单端口验证通过：8080 直接返回首页、接口正常）

**涉及文件：**
- `fragment‑story‑frontend/dist/*`——前端 `npm run build` 构建产物
- `fragment-story/src/main/resources/static/*`——前端产物拷贝到这里，由 Spring Boot 自动托管
- `fragment-story/pom.xml`——打包配置（如需调整）

**Files:**
- Modify: `fragment-story/pom.xml`（如需要，增加 resources 拷贝或手工拷贝说明）

- [ ] **Step 1: 构建前端**

Run: `npm run build`（在 `fragment‑story‑frontend` 下）
Expected: 生成 `dist/`。

- [ ] **Step 2: 拷贝到后端静态目录**

Run: `Copy-Item -Recurse -Force dist\* fragment-story\src\main\resources\static\`（PowerShell，工作区根目录执行）

- [ ] **Step 3: 打包并本地验证**

Run: `mvn -q -DskipTests package -f fragment-story/pom.xml`
Run: `java -jar fragment-story/target/fragment-story-0.0.1-SNAPSHOT.jar`
Expected: 浏览器打开 `http://localhost:8080` 直接看到首页；`http://localhost:8080/#/login` 可登录；接口 `/api/**` 正常。

- [ ] **Step 4: 提交**

```bash
git add .
git commit -m "build: 后端托管前端"
```

### Task 18: 腾讯云学生机部署 —— ✅ 已完成（2026-08-12：服务器（Ubuntu 24.04，2G/50G）已正式上线：JDK17 + MySQL8 安装、建库导入 4 张表、jar 上传、systemd 常驻、每日 3 点自动备份；公网首页与验证码接口均 200；含 git 提交 9dcf436）

**涉及文件：**
- `fragment-story/deploy/story.service`——systemd 服务单元（服务器上常驻运行 jar）
- `fragment-story/deploy/backup.sh`——MySQL 定时备份脚本
- `fragment-story/src/main/resources/application.properties`——服务器端修改数据库密码、JWT 密钥、**头像目录**（`app.upload.avatar-dir` 改为服务器路径）
- 服务器部署位置：`/etc/systemd/system/story.service`、`/home/ubuntu/story/`

> 图片存储说明（2026-08-12）：头像等用户上传图片**存服务器本地目录**（上线后不再用本地 `D:/HeadImage`）；服务器需先创建头像目录；**备份脚本同时备份 MySQL 数据库和头像图片目录**（图片是用户数据，丢失无法恢复）。本地 `D:/HeadImage` 仅用于开发。

**Files:**
- Create: `fragment-story/deploy/story.service`（systemd 单元）
- Create: `fragment-story/deploy/backup.sh`

- [x] **Step 1: 开通腾讯云轻量服务器**

- 完成学生认证（见 制定计划.md 第 12 节），购买轻量应用服务器：2 核 2G、Ubuntu 22.04、国内区（如广州）
- 控制台「防火墙」放行 TCP 8080 与 22，**来源必须是 `0.0.0.0/0`**（只填 `0.0.0.0` 放行无效）

- [x] **Step 2: 服务器初始化**

```bash
sudo apt update && sudo apt install -y openjdk-17-jre-headless mysql-server
sudo systemctl enable --now mysql
```

已执行（Ubuntu 24.04.4）：openjdk-17.0.19、mysql 8.0.46，`systemctl is-active mysql` = active。

创建数据库与应用账号：

```sql
CREATE DATABASE IF NOT EXISTS story_sliver DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER IF NOT EXISTS 'story'@'localhost' IDENTIFIED BY '<数据库密码>';
GRANT ALL PRIVILEGES ON story_sliver.* TO 'story'@'localhost';
FLUSH PRIVILEGES;
```

导入表结构：`mysql -ustory -p<数据库密码> story_sliver < init.sql`（init.sql 需去掉 `CREATE DATABASE` / `USE` 两行或单独执行）。

- [x] **Step 3: 修改服务器端配置**

不改 jar 内配置，用 systemd 环境变量覆盖（`deploy/story.service`）：
`SPRING_DATASOURCE_USERNAME=story`、`SPRING_DATASOURCE_PASSWORD=<数据库密码>`、`APP_UPLOAD_AVATAR_DIR=/home/ubuntu/story/headimage`、`APP_JWT_SECRET=openssl rand -hex 32` 随机生成。

- [x] **Step 4: 上传并部署**

```bash
scp fragment-story/target/fragment-story-0.0.1-SNAPSHOT.jar ubuntu@<服务器IP>:/home/ubuntu/story/
```

`story.service` 内容：

```ini
[Unit]
Description=Story Wall Backend
After=network.target mysql.service

[Service]
User=ubuntu
WorkingDirectory=/home/ubuntu/story
ExecStart=/usr/bin/java -jar /home/ubuntu/story/fragment-story-0.0.1-SNAPSHOT.jar --server.port=8080
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

```bash
sudo cp story.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now story
```

已执行：jar（27.6MB）上传至 `/home/ubuntu/story/`，`systemctl is-active story` = active，本机 `curl localhost:8080` 首页与 `/api/auth/captcha` 均 200。

- [x] **Step 5: 定时备份**

`backup.sh` 内容：

```bash
#!/bin/bash
mkdir -p /home/ubuntu/story/backups
mysqldump -ustory -p<数据库密码> story_sliver > /home/ubuntu/story/backups/story_$(date +%F).sql
find /home/ubuntu/story/backups -name '*.sql' -mtime +14 -delete
```

```bash
chmod +x backup.sh
crontab -e
# 添加一行：
0 3 * * * /home/ubuntu/story/backup.sh
```

已执行：crontab 已配置；首次测试发现 CRLF 换行问题（`$'\r': command not found`），用 `sed -i 's/\r$//'` 转 LF 后备份成功（sql + 头像 tar.gz 均已生成）。

- [x] **Step 6: 上线验收**

Expected（全部满足）：

- 公网浏览器打开 `http://<IP>:8080` 看到首页
- 注册（验证码）→ 发布 → 待审核 → 管理员通过 → 上墙 → 点赞/取消 → 隐藏/取消隐藏 → 删除（弹窗）
- 普通用户访问管理接口返回 403；非站长无法指定/撤销管理员、无法改注册密码
- `systemctl restart story` 后服务自动拉起；`backups/` 目录有当日备份

已执行（2026-08-12）：公网首页 HTTP 200（含「故事碎片墙」标题）、验证码接口 200、备份目录已有当日 sql + tar.gz。

- [x] **Step 7: 提交部署文件**

```bash
git add fragment-story/deploy
git commit -m "deploy: 腾讯云部署配置"
```

## 阶段 6：版本迭代规划（2026-08-14 归类）

> 状态：🔧 需求已确认，待按版本实施。以下为全部需求 + 模块拆分 + 涉及文件。每个 Task 完成后同步更新本表（状态 ✅/🔧/⚠、勾选、提交号）。**V2.0 起：新任务先把后端代码（逐行注解）写进本计划，前端只写需求不写代码；确认后实现。**

**版本归类：**

| 版本 | 包含 Task | 内容 |
| --- | --- | --- |
| V1.1 | Task 19、22、28、29 | 头像 bug 修复 + 点赞变红 + 默认头像（昵称首字）+ 注册账号禁汉字（小改动，先发） |
| V1.2 | Task 23、24、25、26、30 | 昵称唯一 / 修改昵称 / 敏感词 + 退出确认 + 设置下拉 / 修改密码 + 公告功能 |
| V2.0 | Task 20、21 | 他人主页（PC 抽屉 + 移动端跳转）+ 评论 / 举报 / 审核（大功能） |
| V3.0 | Task 27（新增） | 图片上传：碎片与评论均可带图（碎片图随故事审核，评论图不审核） |

### v1.1 数据库变更总览

| 变更 | 说明 |
| --- | --- |
| `user.nickname` 加唯一索引 | 支持「昵称唯一」；上线前先处理现有重复昵称 |
| 新表 `comment` | 评论：id、fragment_id、user_id、content、status（0正常 1已下架）、created_at、updated_at |
| 新表 `comment_report` | 评论举报：id、comment_id、reporter_id、reason、status（0待处理 1已处理）、handled_at |
| 新表 `sensitive_word` | 敏感词库：id、word、created_at（或改为配置文件维护，待确认 Q4） |
| `story_fragment.image` / `comment.image` 加字段 | V3.0：碎片与评论支持图片（存服务器图片路径，可空） |

### 待确认问题清单（Q1-Q10，确认后打勾）

- [x] **Q1 评论呈现方案**：✅ 已确认（2026-08-14）——**PC 与移动端均选方案 A 弹窗详情**：点卡片弹居中详情窗，评论先显示最新 5 条 + 「查看更多」逐步加载
- [x] **Q2 昵称审核方式**：✅ 已确认——注册 + 改昵称都用**敏感词库即时拦截**（不人工审核）
- [x] **Q3 敏感词库维护方式**：✅ 已确认——**数据库表 `sensitive_word` + 管理端维护**（大厂做法：后端校验为主 + 前端提示为辅，双端校验）
- [x] **Q4 他人个人主页**：✅ 已确认（2026-08-14）——展示内容：昵称、头像、身份铭牌、**仅「非匿名且已发布」的碎片**（匿名碎片一律不显示，预览与完整页同理）；跳转方式：**PC 用抽屉内嵌预览（最新 N 条 + 加载更多），点「查看完整主页」当前页跳转；移动端用当前页跳转（方案 1）**；完整主页碎片**分页/加载更多**展示，不一次性全加载
- [x] **Q5 评论字数上限**：✅ 已确认——**100 字**；排序：时间正序
- [x] **Q6 举报理由**：✅ 已确认——**必填**
- [x] **Q7 用户能否删除自己的评论**：✅ 已确认（2026-08-14）——**能，弹窗确认硬删除**
- [x] **Q8 评论发布频率限制**：✅ 已确认（2026-08-14）——**1 分钟 10 条**
- [x] **Q9 修改密码后是否强制重新登录**：✅ 已确认——**是**（旧 token 失效）
- [x] **Q10 点赞变色**：✅ 已确认现状 `FragmentVO.likedByMe` 存在，只改前端样式，无后端改动

---

### 版本 V1.1（已完成，2026-08-14）

### Task 19（V1.1）: 修复「头像审核通过后显示过小」—— ✅ 已完成（2026-08-14，含 git 提交 72c5c9d）

- 现象：某个用户头像审核通过后显示特别小，其余用户正常
- 原因（已确认 2026-08-14）：该用户第一次上传时**未选择裁剪大小**，原图本身很小 → 审核通过后展示很小；重新上传并裁剪后正常
- 修复方向：上传流程**强制裁剪**（前端必须选裁剪区域，后端校验最小尺寸），并对历史未裁剪图片兜底（展示时统一容器尺寸 + `object-fit: cover`，不随原图比例缩放）
- 涉及文件：前端头像上传组件（强制裁剪 + 后端最小尺寸校验）、FragmentCard.vue / ProfileView.vue 头像样式（统一容器与 object-fit）
- 验收：任意头像在任何位置显示尺寸一致；未裁剪的旧图片也不会极小展示

**已实现代码（含注解）：**

前端 `fragment-story-frontend/src/components/CropModal.vue`（confirm 里裁剪后自动贴合）：

```js
// ===== confirm() 里「圆形蒙版之后」的新增逻辑 =====
// 读出整张 512x512 画布每个像素的 RGBA 数据（imgData 是 Uint8ClampedArray）
const imgData = ctx.getImageData(0, 0, out, out).data
// minX/minY/maxX/maxY 用来记录「可见内容」的边界；初始设为画布大小/-1，后面循环里会收紧
let minX = out, minY = out, maxX = -1, maxY = -1
// 外层循环：遍历每一行像素（y 是纵坐标）
for (let y = 0; y < out; y++) {
  // 内层循环：遍历这一行的每一列像素（x 是横坐标）
  for (let x = 0; x < out; x++) {
    // 每个像素占 4 个字节（R,G,B,A）；(y*out+x)*4+3 就是该像素的 alpha（透明度）通道
    // alpha > 8 视为「可见内容」：抗锯齿边缘那种 alpha=1~8 的浅透明像素忽略，避免误判
    if (imgData[(y * out + x) * 4 + 3] > 8) {
      if (x < minX) minX = x  // 刷新最左边界
      if (x > maxX) maxX = x  // 刷新最右边界
      if (y < minY) minY = y  // 刷新最上边界
      if (y > maxY) maxY = y  // 刷新最下边界
    }
  }
}
// 边界找完后：外扩 8px 边距（防止圆边被切太紧），再以内容中心取正方形，放大铺满 512x512
// 效果：无论用户把圆形画得多小，导出的头像都会「顶满」整张图，不再出现大片透明边距
```

后端 `fragment-story/src/main/java/com/storysliver/service/impl/UserServiceImpl.java`（cropToFilledSquare）：

```java
// ===== uploadAvatar 里保存图片前的新增校验 =====
if (src.getWidth() < 64 || src.getHeight() < 64) {
    // 图片宽或高小于 64 像素：太小，放大后会糊，直接拒绝并提示用户换图
    throw new BusinessException(ResultCode.AVATAR_TOO_SMALL);
}
// 用「内容贴合」方式裁剪成 128x128 再写盘：裁掉透明空边，头像占满画布
ImageIO.write(cropToFilledSquare(src, AVATAR_SIZE), "png", target);

// ===== cropToFilledSquare 方法 =====
private BufferedImage cropToFilledSquare(BufferedImage src, int size) {
    int w = src.getWidth();  // 原图宽度
    int h = src.getHeight(); // 原图高度
    int minX = w, minY = h, maxX = -1, maxY = -1; // 内容边界初始值
    for (int y = 0; y < h; y++) {            // 遍历每一行
        for (int x = 0; x < w; x++) {        // 遍历每一列
            int alpha = (src.getRGB(x, y) >>> 24) & 0xFF; // 取该像素 alpha：getRGB 高 8 位是透明度
            if (alpha > 8) {                 // alpha>8 视为可见内容（忽略抗锯齿浅透明）
                if (x < minX) minX = x;      // 更新最左
                if (x > maxX) maxX = x;      // 更新最右
                if (y < minY) minY = y;      // 更新最上
                if (y > maxY) maxY = y;      // 更新最下
            }
        }
    }
    if (maxX < 0) {                          // 全透明（理论上不会发生）：退回整图裁剪，保证不抛异常
        return cropToSquare(src, size);
    }
    int pad = 4;                             // 内容边界外扩 4px，圆边不被切太紧
    minX = Math.max(0, minX - pad);          // 左边界减 pad，但不下越 0
    minY = Math.max(0, minY - pad);          // 上边界减 pad
    maxX = Math.min(w - 1, maxX + pad);      // 右边界加 pad，但不超过图宽
    maxY = Math.min(h - 1, maxY + pad);      // 下边界加 pad
    int side = Math.max(maxX - minX + 1, maxY - minY + 1); // 取宽高较大值做正方形边长
    int cx = (minX + maxX) / 2;              // 内容中心 x
    int cy = (minY + maxY) / 2;              // 内容中心 y
    int x0 = Math.max(0, Math.min(cx - side / 2, w - side)); // 正方形起点 x，越界则夹到合法范围
    int y0 = Math.max(0, Math.min(cy - side / 2, h - side)); // 正方形起点 y
    BufferedImage crop = src.getSubimage(x0, y0, side, side); // 裁出正方形区域
    BufferedImage out = new BufferedImage(size, size, BufferedImage.TYPE_INT_ARGB); // 输出图（保留透明通道）
    Graphics2D g = out.createGraphics();     // 拿到画笔
    g.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);      // 开抗锯齿，边缘平滑
    g.setRenderingHint(RenderingHints.KEY_INTERPOLATION, RenderingHints.VALUE_INTERPOLATION_BILINEAR); // 双线性插值，缩放不糊
    g.drawImage(crop, 0, 0, size, size, null); // 把正方形内容画到 128x128 输出图
    g.dispose();                             // 释放画笔资源
    return out;                              // 返回处理好的头像
}
```

`common/ResultCode.java` 新增：`AVATAR_TOO_SMALL(400, "图片太小，请选择尺寸更大的图片")`

### Task 22（V1.1）: 点赞心形变红色—— ✅ 已完成（2026-08-14，含 git 提交 72c5c9d）

- 需求：点赞后小心心变**纯红色**（不再只是数量变化）
- 现状：后端 `FragmentVO.likedByMe` 已存在（当前用户是否已赞），**无后端改动**
- 涉及文件：前端 FragmentCard.vue（点赞按钮根据 likedByMe 切换红色实心/灰色空心样式）
- 验收：点赞 → 红色实心；取消 → 恢复；数量照常变化

**已实现代码（含注解）：**

`fragment-story-frontend/src/components/FragmentCard.vue`：

```html
<!-- :class 里 liked 为 true 时加 .liked 样式（红色）；点击触发父组件的 like 事件 -->
<button class="like-btn" :class="{ liked: fragment.likedByMe }" @click="$emit('like', fragment)">
  {{ fragment.likedByMe ? '♥' : '♡' }} <!-- 已赞显示实心红心 ♥，未赞显示空心 ♡ -->
  {{ fragment.likeCount }}              <!-- 点赞数量照常显示 -->
</button>
```

`fragment-story-frontend/src/styles/main.css`：

```css
/* .liked 时把心形颜色改成纯红色；未赞时用默认的棕色 .like-btn 颜色 */
.like-btn.liked { color: #e0564f; }
```

### Task 28（V1.1）: 默认头像——未设置头像时显示昵称首字占位—— ✅ 已完成（2026-08-14，含 git 提交 72c5c9d）

- 需求：用户**未设置头像**时，发布碎片不显示头像 → 改为与个人主页初始默认一致：**昵称首字符的圆形占位头像**
- 说明：个人主页一开始就有默认头像（首字圆形），碎片卡片/评论处没有兜底，需统一
- 涉及文件：前端头像组件 / FragmentCard.vue / 评论头像处（无 `avatar` 时显示昵称首字符圆形占位，颜色可按昵称哈希取色保持稳定）；后端无需改动（昵称字段已有）
- 验收：未设置头像的用户，碎片卡片、评论、个人主页均显示首字默认头像；设置了头像的仍显示图片

**已实现代码（含注解）：**

新建 `fragment-story-frontend/src/components/UserAvatar.vue`（统一头像组件，全项目复用）：

```vue
<script setup>
import { computed } from 'vue'
// 用户头像组件：有已审核头像显示图片；没有则显示昵称首字圆形占位（和一开始个人主页的默认头像一致）
const props = defineProps({
  avatar: { type: String, default: '' }, // 已审核头像 URL；为空说明没有头像，走首字占位
  name: { type: String, default: '' },   // 昵称；取它的第一个字符当占位文字
  size: { type: Number, default: 30 }    // 展示直径（像素）；调用处按位置传，如卡片 20、主页 72
})
const PALETTE = ['#d9cfbc', '#c9d7c5', '#d5cdb0', '#cfc4d6', '#c8d4da'] // 温暖色板：占位背景可选的颜色
const firstChar = computed(() => (props.name || '?').charAt(0)) // 昵称为空时用 '?' 兜底；取第 0 个字符
const color = computed(() => {
  let h = 0                                      // 哈希初始值
  for (const ch of props.name || '') {           // 遍历昵称的每个字符
    h = (h * 31 + ch.charCodeAt(0)) >>> 0        // 累加字符编码生成哈希；>>>0 转成无符号整数防止溢出
  }
  return PALETTE[h % PALETTE.length]             // 用哈希取模选颜色：同一昵称永远同色，不同昵称大概率不同色
})
</script>
<template>
  <!-- 有头像：渲染图片；宽高用 size 控制，圆形 + cover 防变形 -->
  <img v-if="avatar" :src="avatar" class="ua-img" :style="{ width: size + 'px', height: size + 'px' }" alt="" />
  <!-- 没头像：渲染圆形占位；背景用哈希色，字号随 size 缩放（42% 视觉协调） -->
  <div v-else class="ua-char" :style="{ width: size + 'px', height: size + 'px', background: color, fontSize: Math.round(size * 0.42) + 'px' }">{{ firstChar }}</div>
</template>
<style scoped>
.ua-img { border-radius: 50%; object-fit: cover; display: block; flex: none; } /* 圆形头像：cover 等比裁剪不变形 */
.ua-char { border-radius: 50%; display: flex; align-items: center; justify-content: center; color: #6b614f; font-weight: 500; flex: none; } /* 占位：圆形 + 文字居中 */
</style>
```

`FragmentCard.vue` 使用（匿名碎片不显示头像）：

```html
<!-- 匿名碎片（isAnonymous===1）不渲染头像；实名用户用 UserAvatar：有图显示图，没图显示首字占位 -->
<UserAvatar v-if="fragment.isAnonymous !== 1" :avatar="fragment.authorAvatar" :name="fragment.authorName" :size="20" />
```

### Task 29（V1.1）: 注册账号禁止汉字（已注册用户不受影响）—— ✅ 已完成（2026-08-14，含 git 提交 72c5c9d）

- 需求：注册时**账号（username）不允许包含汉字**；之前已注册的账号不管
- 校验规则：只允许英文字母、数字、下划线（如 `^[A-Za-z0-9_]{4,20}$`，长度沿用现状可调）
- 涉及文件：后端 RegisterRequest/UserServiceImpl 注册校验（正则 + 提示）、common/ResultCode.java（新增账号格式错误码）、前端注册页即时提示；老账号登录不受影响
- 验收：带汉字的账号注册被拒并提示；字母/数字/下划线可注册；老账号正常登录

**已实现代码（含注解）：**

后端 `UserServiceImpl.register`（在密码校验之后、查重之前）：

```java
// 动作3.5（Task 29）：用户名格式校验——只允许字母/数字/下划线（禁止汉字）
String username = request.getUsername() == null ? "" : request.getUsername().trim(); // 先判 null 再 trim：防空指针，去首尾空格
if (!username.matches("^[A-Za-z0-9_]{4,20}$")) { // 正则：4-20 位，只含字母/数字/下划线；带汉字或符号就进不来
    throw new BusinessException(ResultCode.USERNAME_INVALID); // 抛业务异常，全局处理器转成统一 JSON 返回前端
}
```

`common/ResultCode.java` 新增：`USERNAME_INVALID(400, "用户名只能包含字母、数字和下划线（4-20位），不能有汉字")`

前端 `LoginView.vue`（提交前先拦截，减少无效请求）：

```js
// 只有注册模式才校验；正则与后端一致，前端先拦截能少发一次无效请求
if (mode.value === 'register' && !/^[A-Za-z0-9_]{4,20}$/.test(form.value.username)) {
  error.value = '用户名只能包含字母、数字和下划线（4-20位），不能有汉字' // 直接显示在表单下方
  return // 中止提交，不发请求
}
```

> 附带修复：`AuthInterceptorTest` 缺少 `userMapper` mock（封禁功能遗留），已补上，测试 27/27 全绿。

### 版本 V1.2（已完成，2026-08-14）

### Task 23（V1.2）: 个人主页显示 @昵称 + 昵称唯一 —— ✅ 已完成（2026-08-14，含 git 提交 f1b0ff6）

- 需求：主页显示 **@昵称**（如 @aaa），不再显示 @账号（@qqq）
- 昵称唯一：注册时校验重复；`user.nickname` 加唯一索引
- 现有重复昵称处理：上线前查重，重复用户提示改名或自动加后缀（待确认处理方式）
- 涉及文件：后端 UserMapper.java（注册查重）、pojo/User.java、resources/db 增量 SQL；前端 ProfileView.vue、FragmentCard.vue（作者显示处）
- 验收：注册重复昵称被拒；主页显示 @昵称

**已实现代码（含注解）：**

`resources/db/upgrade_v1.2.sql`（执行前已确认线上无重复昵称）：

```sql
-- 1. 昵称唯一：给 nickname 列加唯一索引；执行前先确认无重复昵称，否则加索引会失败
ALTER TABLE `user` ADD UNIQUE KEY uk_nickname (nickname);
```

`UserMapper.java` 新增：

```java
/** 按昵称精确查询（v1.2）：注册/修改昵称时的唯一性校验 */
@Select("select id, username, nickname, ... from `user` where nickname = #{nickname}") // #{nickname} 是预编译占位，防 SQL 注入
User selectByNickname(String nickname); // 返回命中的用户；null 表示这个昵称没人用
```

`UserServiceImpl.register` 新增动作 4.5（昵称唯一 + 敏感词，入库前拦截）；`ProfileView.vue` 主页副标题改为：

```html
<!-- 优先显示昵称；昵称为空（老数据）才回退显示账号；? 是可选链，user 为 null 时不报错 -->
<p class="subtitle">@{{ userStore.user?.nickname || userStore.user?.username }}</p>
```

### Task 24（V1.2）: 修改昵称 + 敏感词库（注册/改昵称校验）—— ✅ 已完成（2026-08-14，含 git 提交 f1b0ff6）

- 入口：设置下拉菜单（见 Task 26，已确认修改昵称放在设置下拉框里）
- 修改昵称规则：昵称唯一 + 敏感词校验（命中提示重写）；是否需人工审核待确认（见 Q2）
- 注册时昵称同样过敏感词库
- 敏感词库实现：待确认（见 Q3）
- 涉及文件：
  - 后端：SensitiveWordService.java + impl（校验）、UserService/Controller（改昵称接口）、mapper、增量 SQL（若用数据库表）；若需人工审核则加 nickname_pending/nickname_reject_reason 字段 + 管理端审核 Tab
  - 前端：注册页昵称提示、设置里的「修改昵称」弹窗、管理后台（若需审核）
- 验收：命中敏感词提示重写；重复昵称被拒；修改成功即时生效（或进审核，按 Q2 定）

**已实现代码（含注解）：**

`upgrade_v1.2.sql` 建敏感词表 + 默认词：

```sql
CREATE TABLE IF NOT EXISTS sensitive_word (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',          -- 自增主键
    word VARCHAR(50) NOT NULL COMMENT '敏感词（唯一）',                  -- 敏感词内容，不能为空
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间', -- 加词时间，数据库自动填
    PRIMARY KEY (id), UNIQUE KEY uk_word (word)                        -- 主键 + 词唯一（重复词插不进去）
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='敏感词库';             -- InnoDB 支持事务，utf8mb4 支持中文
INSERT IGNORE INTO sensitive_word (word) VALUES ('傻逼'),('你妈'),('去死'),('垃圾广告'),('博彩'),('诈骗'); -- 预置默认词；IGNORE 让重复词静默跳过
```

`SensitiveWordServiceImpl`（内存缓存 + 包含匹配，增删即刷新，不用重启）：

```java
private volatile List<String> cache = new ArrayList<>(); // 词库缓存；volatile 保证一个线程改完，其他线程立刻看到新值
@PostConstruct public void init() { refresh(); }        // 启动时（Bean 初始化后）加载一次词库
private void refresh() { cache = sensitiveWordMapper.selectAllWords(); } // 从数据库全量重载缓存
public boolean containsSensitive(String text) {
    for (String word : cache) if (!word.isEmpty() && text.contains(word)) return true; // 包含匹配：text 中出现任意词即命中
    return false; // 全部词都不命中才返回 false
}
```

`UserServiceImpl.updateNickname`（唯一 + 敏感词 + 长度校验后更新）与 `PUT /api/users/nickname` 接口；管理后台新增「敏感词」Tab（仅站长增删）。

### Task 25（V1.2）: 退出登录确认弹窗 —— ✅ 已完成（2026-08-14，含 git 提交 f1b0ff6）

- 需求：点击退出 → 屏幕中间弹窗「确定要退出登录吗？」（防止误触）
- 涉及文件：前端 Header/导航组件，复用现有 ConfirmDialog.vue
- 验收：点退出先弹窗，确认才退出，取消留在当前页

**已实现代码（含注解）：**

`App.vue` 中退出改为先弹确认框（复用 ConfirmDialog）：

```html
<button v-if="userStore.token" class="link" @click="askLogout">退出</button> <!-- 点退出不直接登出，只打开确认框 -->
<ConfirmDialog :show="showLogoutConfirm" title="退出登录" message="确定要退出登录吗？"
  confirm-text="退出" @confirm="confirmLogout" @cancel="cancelLogout" /> <!-- 确认才登出，取消留在当前页 -->
```

```js
function confirmLogout() {
  showLogoutConfirm.value = false; // 关掉确认框
  userStore.logout();              // 清空 token 和用户信息（localStorage）
  router.push('/login')            // 跳到登录页
}
```

### Task 26（V1.2）: 设置下拉菜单 + 修改密码 —— ✅ 已完成（2026-08-14，含 git 提交 f1b0ff6）

- 设置入口：导航栏「退出」左边加「设置」图标，鼠标悬停弹出下拉菜单；**修改昵称、修改密码都放在这个下拉框里**（已确认）
- 修改密码：输入旧密码 + 新密码；**旧密码校验正确才能修改**；BCrypt 加密落库；改完强制重新登录（见 Q9）
- 涉及文件：
  - 后端：pojo/User/UpdatePasswordRequest、UserController 或 AuthController 新增 `PUT /api/users/password`、UserService 校验旧密码逻辑、common/ResultCode.java
  - 前端：Header 组件（设置下拉菜单）、修改密码弹窗、api 模块
- 验收：旧密码错误被拒；旧密码正确可改；改后重新登录生效

**已实现代码（含注解）：**

`App.vue` 设置下拉（退出左边，悬停弹出）：

```html
<div v-if="userStore.token" class="settings"> <!-- 只有登录用户才显示设置 -->
  <button class="link">设置 ▾</button>          <!-- 触发按钮；悬停展开下拉（CSS .settings:hover） -->
  <div class="settings-menu">                   <!-- 下拉菜单容器 -->
    <button class="link" @click="openNickname">修改昵称</button> <!-- 点开修改昵称弹窗 -->
    <button class="link" @click="openPassword">修改密码</button> <!-- 点开修改密码弹窗 -->
  </div>
</div>
```

后端 `UserServiceImpl.updatePassword`（旧密码 matches 校验 → 新密码 BCrypt 加密落库）+ `PUT /api/users/password`；改密成功后前端清 token 强制回登录页：

```java
User user = userMapper.selectById(userId); // 查出当前用户（要拿库里存的密码哈希做比对）
if (user == null || oldPassword == null || !passwordEncoder.matches(oldPassword, user.getPassword())) {
    throw new BusinessException(ResultCode.OLD_PASSWORD_WRONG); // BCrypt 不可逆，只能拿明文去 matches 哈希
}
if (newPassword == null || newPassword.length() < 6) {
    throw new BusinessException(ResultCode.PASSWORD_TOO_WEAK); // 新密码强度校验
}
userMapper.updatePassword(userId, passwordEncoder.encode(newPassword)); // 加密后落库，绝不存明文
```

### Task 30（V1.2）: 公告功能（站长在线编辑，进站弹窗，刷新不重弹）—— ✅ 已完成（2026-08-14，含 git 提交 f1b0ff6）

- 需求：
  - **每次进入网站都弹**：直接访问、登录后进入、注册后进入首页都会弹（新开标签/重新进站 = 新会话 = 会弹）
  - **同一标签页内刷新不重复弹**；站长下架后不再弹
  - 公告内容**站长在线编辑**（标题/正文/图片），不用重新打包
  - **只有站长**能编辑/上架/下架；管理员和其他人都看不到公告管理入口
  - 上架后弹出；**下架后不再弹出**
  - 支持图片（站长上传，存服务器）
- 设计：
  - 数据表 `announcement`：id、title、content、image_url（可空）、status（0下架 1上架）、created_at、updated_at
  - 弹窗规则：`GET /api/announcements/active` 返回**最新一条上架**公告；前端用 **sessionStorage** 记录 `announcement-seen=<id>`——同一标签页会话内刷新不重弹；关闭标签/新开访问（新会话）重新弹；登录/注册后首次进入首页会弹；站长发新公告（id 变化）→ 重新弹；下架后接口返回空 → 不弹
  - 交互（2026-08-14 已确认）：进入网站默认弹 **A 居中弹窗** → 点「我知道了」收起为 **B 公告栏** → 点 B 重新展开 A → 点 B 上的 ✕ 直接关闭（记已读）；A 分两种排版：**有图**（顶部图片区 + 标题 + 正文 + 按钮）/ **无图**（紧凑居中排版，无图片区）；**B 公告栏放在导航栏下方（不遮挡导航），公告文字横向滚动（90 秒滚完一遍）**；圆角方案：弹窗圆角、全宽公告条直角（已确认）
  - 权限：公告管理接口全部 `@RequireRole(User.ROLE_OWNER)`（站长）
- 接口：
  - `GET /api/announcements/active`（公开，游客/登录用户都弹）
  - `GET /api/admin/announcements`（站长：全部公告列表，含下架）
  - `POST /api/admin/announcements`（新建）
  - `PUT /api/admin/announcements/{id}`（编辑）
  - `PUT /api/admin/announcements/{id}/status`（上架/下架）
  - `DELETE /api/admin/announcements/{id}`（删除，可选）
  - `POST /api/admin/announcements/upload-image`（站长上传公告图片，复用现有上传逻辑，存 `/uploads/` 目录）
- 涉及文件：
  - 后端：pojo/Announcement.java、pojo/Admin/AnnouncementRequest.java、mapper/AnnouncementMapper.java、service/AnnouncementService.java + impl、controller/AnnouncementController.java（公开 + 管理）、resources/db 增量 SQL、common/ResultCode.java（公告相关错误码）
  - 前端：api/announcement.js、components/AnnouncementModal.vue（A 弹窗 + B 公告栏双状态组件，支持有图/无图两种排版）、HomeView.vue（进入时拉取公告 + sessionStorage 判断是否已读）、AdminView.vue（新增「公告管理」Tab，**仅站长显示**；编辑表单 + 上架/下架 + 图片上传）
- 验收：站长新建并上架 → 新开标签访问弹 A（有图/无图排版正确）→「我知道了」收成 B → 点 B 回 A → ✕ 关闭；同一标签刷新不再弹；登录/注册后首次进首页弹；下架后不再弹；新公告（id 变化）重新弹；管理员看不到公告管理 Tab；管理员/普通用户调公告管理接口返回 403
- 待确认：Q-A 同一时间可上架多条公告，弹窗只弹最新一条（推荐）；Q-B 图片先做 1 张主图（推荐）；Q-C 放 V1.2 一起做（推荐）；Q-D 标题 ≤50 字、正文 ≤2000 字（推荐）

**已实现代码（含注解）：**

`upgrade_v1.2.sql` 建公告表；`AnnouncementMapper.selectActiveLatest`：

```java
/** 查询最新一条上架公告：进站弹窗用；id 倒序保证「最新」 */
@Select("select id, title, content, image_url as imageUrl, status, created_at as createdAt, updated_at as updatedAt " +
        "from announcement where status = 1 order by id desc limit 1") // status=1 只查上架；倒序+limit 1 取最新一条
Announcement selectActiveLatest(); // 返回最新上架公告；没有则 null
```

公开接口 `GET /api/announcements/active`（WebConfig 已排除 JWT 拦截，游客可读）；管理接口全部 `@RequireRole(User.ROLE_OWNER)`（仅站长）。前端 `AnnouncementModal.vue` 实现 A 弹窗（有图/无图）→ B 公告栏 → ✕ 关闭；`App.vue` 进入首页时拉取公告，`sessionStorage` 记 `announcement-seen=<id>` 实现「刷新不重弹、新开标签重新弹、新公告重新弹」。

```js
// App.vue：进入首页检查公告（登录/注册后跳回首页也会触发）
const d = await getActiveAnnouncement() // 拉最新上架公告；没有上架时 d 为 null
if (d && d.id && sessionStorage.getItem('announcement-seen') !== String(d.id)) {
  announcement.value = d // 当前会话没看过这条公告（id 不一致）才弹窗
  // 展示即记已读：sessionStorage 在标签页内刷新仍保留 → 刷新不重弹；新开标签是新会话 → 重新弹
  sessionStorage.setItem('announcement-seen', String(d.id))
  showAnn.value = true
}
// 登录/注册成功进入首页前，LoginView 会先 removeItem('announcement-seen') → 登录/注册后首次进首页重新弹
```

### 版本 V2.0（待实施，2026-08-14 后端代码已先写入）

> 说明：V2.0 起按新约定——**后端代码先写在本计划（逐行注解），前端只写需求不写代码**；确认后再写进项目。

### Task 20（V2.0）: 点击头像/昵称查看他人主页（匿名不可点；PC 抽屉 + 移动端跳转）

- 需求：他人碎片上的头像或昵称可点击 → 查看该用户个人主页；匿名碎片（is_anonymous=1）不可点击
- 展示规则（Q4 已确认）：他人主页与预览抽屉**只展示该用户「非匿名且已发布」的碎片**；完整主页碎片**分页/加载更多**
- 交互（Q4 已确认）：**PC** 点头像 → 右侧抽屉预览（最新 N 条）+「查看完整主页」→ 当前页跳转；**移动端**直接当前页跳转
- 前端（只描述不写代码）：FragmentCard 头像/昵称点击（匿名禁用）、公开路由 `/profile/:userId`、PC 抽屉组件、加载更多

**后端代码（先写，确认后实现）：**

`resources/db/upgrade_v2.0.sql`（V2.0 无新表，复用现有表）：

```sql
-- V2.0 不需要改表：他人主页只查「非匿名 + 已发布」的碎片，靠现有字段过滤即可
-- （is_anonymous=0 AND status=1）
```

`pojo/ProfileVO.java`（新增）：

```java
@Data
public class ProfileVO {
    private Long userId;                  // 用户 id（前端路由跳转用）
    private String nickname;              // 昵称（主页显示 @昵称）
    private String avatar;                // 已审核头像 URL（可能为空）
    private Integer role;                 // 角色（前端渲染铭牌）
    private PageBean<FragmentVO> fragments; // 该用户「非匿名已发布」碎片分页（含 total/list）
}
```

`UserMapper.java` 新增安全查询（**不能返回 password/email**）：

```java
/** 公开用户信息（v2.0）：只取展示需要的字段，绝不带 password/email */
@Select("select id, nickname, avatar, role from `user` where id = #{id}")
User selectPublicById(Long id);
```

`StoryFragmentMapper.java` 新增（配合 PageHelper 分页）：

```java
/** 某用户的公开碎片（v2.0）：非匿名 + 已发布，时间倒序；PageHelper 会自动拼 limit */
@Select("select * from story_fragment where user_id = #{userId} and is_anonymous = 0 and status = 1 order by id desc")
List<StoryFragment> selectPublicByUser(Long userId);
```

`UserService.java` 新增方法 + `UserServiceImpl` 实现：

```java
/** 公开主页（v2.0）：返回用户信息 + 非匿名已发布碎片分页 */
ProfileVO getPublicProfile(Long userId, int pageNum, int pageSize);
```

```java
@Override
public ProfileVO getPublicProfile(Long userId, int pageNum, int pageSize) {
    User u = userMapper.selectPublicById(userId);   // 查公开信息；查不到说明用户不存在
    if (u == null) {
        throw new BusinessException(ResultCode.USER_NOT_FOUND); // 抛 404，前端提示「用户不存在」
    }
    PageHelper.startPage(pageNum, pageSize);        // 开启分页：下一句查询自动带上 limit
    List<StoryFragment> list = storyFragmentMapper.selectPublicByUser(userId); // 只查非匿名已发布
    Page<StoryFragment> page = (Page<StoryFragment>) list; // PageHelper 把 list 强转成 Page 才能拿 total
    ProfileVO vo = new ProfileVO();                 // 组装返回对象
    vo.setUserId(u.getId());                        // 用户 id
    vo.setNickname(u.getNickname());                // 昵称
    vo.setAvatar(u.getAvatar());                    // 头像
    vo.setRole(u.getRole());                        // 角色（铭牌）
    List<FragmentVO> vos = list.stream()            // 把实体转成展示 VO
        .map(f -> fragmentService.toVO(f, false))   // false：他人主页不需要「我是否已赞」标记
        .collect(Collectors.toList());              // 收集成 List
    vo.setFragments(new PageBean<>(page.getTotal(), vos)); // total 给前端做分页
    return vo;                                      // 返回结果
}
```

`controller/PublicProfileController.java`（新增，公开接口）：

```java
@RestController
@RequestMapping("/api/users/{userId}/profile")
public class PublicProfileController {
    @Autowired private UserService userService; // 用户业务

    @GetMapping
    public Result profile(@PathVariable Long userId,
                          @RequestParam(defaultValue = "1") int pageNum,
                          @RequestParam(defaultValue = "9") int pageSize) {
        return Result.success(userService.getPublicProfile(userId, pageNum, pageSize));
    }
}
```

`WebConfig.java` 排除拦截（公开接口，游客可看主页）：

```java
"/api/users/*/profile", // v2.0：他人主页公开可看，不需要登录
```

> 前端需新增 `GET /api/users/{userId}/profile` 调用；PC 抽屉先拉第一页（pageSize=4），「查看完整主页」跳转 `/profile/:userId` 再加载更多。

---

### Task 21（V2.0）: 评论功能（免审核 + 举报 + 管理员审核）

- 需求：登录用户可评论（**免审核**，发布即显示）；可举报（理由必填）；管理员审核举报：a 下架评论（硬删除）/ b 不下架 / c 封禁该评论用户
- 呈现方案（Q1 已确认）：方案 A 弹窗详情，评论先显示最新 5 条 + 「查看更多」；**评论 100 字上限、时间正序**（Q5 已确认）
- 前端（只描述不写代码）：碎片详情弹窗内评论区、评论输入框、举报按钮、AdminView 新增「评论举报」Tab

**后端代码（先写，确认后实现）：**

`resources/db/upgrade_v2.0.sql` 新增两张表：

```sql
CREATE TABLE IF NOT EXISTS comment (
    id          BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    fragment_id BIGINT UNSIGNED NOT NULL COMMENT '所属碎片id',
    user_id     BIGINT UNSIGNED NOT NULL COMMENT '评论者id',
    content     VARCHAR(100)    NOT NULL COMMENT '评论内容（≤100字）',
    status      TINYINT         NOT NULL DEFAULT 0 COMMENT '0正常 1已下架',
    created_at  DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    KEY idx_fragment (fragment_id, id)   -- 按碎片查评论的索引
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评论';

CREATE TABLE IF NOT EXISTS comment_report (
    id          BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    comment_id  BIGINT UNSIGNED NOT NULL COMMENT '被举报评论id',
    reporter_id BIGINT UNSIGNED NOT NULL COMMENT '举报人id',
    reason      VARCHAR(255)    NOT NULL COMMENT '举报理由（必填）',
    status      TINYINT         NOT NULL DEFAULT 0 COMMENT '0待处理 1已处理',
    handled_at  DATETIME        DEFAULT NULL COMMENT '处理时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_comment_reporter (comment_id, reporter_id), -- 同一个人不能重复举报同一评论
    KEY idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评论举报';
```

`pojo/Comment.java`、`pojo/CommentVO.java`、`pojo/CommentReport.java`（新增）：

```java
@Data
public class Comment {
    private Long id;          // 主键
    private Long fragmentId;  // 所属碎片
    private Long userId;      // 评论者
    private String content;   // 内容
    private Integer status;   // 0正常 1下架
    private LocalDateTime createdAt; // 创建时间
}

@Data
public class CommentVO {      // 返回给前端的评论（不含 user_id 敏感字段）
    private Long id;          // 评论 id
    private String content;   // 内容
    private String authorName;// 评论者昵称
    private String authorAvatar; // 评论者头像（可空）
    private Boolean mine;     // 是否自己发的（前端显示「删除自己的评论」）
    private String createdAt; // 格式化时间
}

@Data
public class CommentReport {  // 举报记录（管理端列表要带冗余字段）
    private Long id;          // 举报 id
    private Long commentId;   // 被举报评论 id
    private Long reporterId;  // 举报人 id
    private String reason;    // 举报理由
    private Integer status;   // 0待处理 1已处理
    private LocalDateTime handledAt; // 处理时间
    private String commentContent;   // 冗余：评论内容（join 查出来，展示用）
    private Long fragmentId;         // 冗余：所属碎片 id
    private Long commenterId;        // 冗余：评论者用户 id（封禁用）
    private String commenterName;    // 冗余：评论者昵称
}
```

`mapper/CommentMapper.java`（新增）：

```java
@Mapper
public interface CommentMapper {
    @Insert("insert into comment (fragment_id, user_id, content) values (#{fragmentId}, #{userId}, #{content})")
    @Options(useGeneratedKeys = true, keyProperty = "id") // 回填自增 id
    int insert(Comment comment);

    @Select("select id, fragment_id as fragmentId, user_id as userId, content, status, created_at as createdAt " +
            "from comment where fragment_id = #{fragmentId} and status = 0 order by id asc") // 时间正序（Q5）
    List<Comment> selectByFragment(Long fragmentId); // PageHelper 分页

    @Select("select id, fragment_id as fragmentId, user_id as userId, content, status, created_at as createdAt " +
            "from comment where id = #{id}")
    Comment selectById(Long id);

    @Delete("delete from comment where id = #{id}") // 下架=硬删除（管理员或作者本人）
    int deleteById(Long id);
}
```

`mapper/CommentReportMapper.java`（新增）：

```java
@Mapper
public interface CommentReportMapper {
    @Insert("insert into comment_report (comment_id, reporter_id, reason) values (#{commentId}, #{reporterId}, #{reason})")
    int insert(CommentReport report);

    @Select("select id, comment_id as commentId, reporter_id as reporterId, reason, status, handled_at as handledAt " +
            "from comment_report where comment_id = #{commentId} and reporter_id = #{reporterId}")
    CommentReport selectByCommentAndReporter(@Param("commentId") Long commentId, @Param("reporterId") Long reporterId);

    @Select("select r.id, r.comment_id as commentId, r.reporter_id as reporterId, r.reason, r.status, r.handled_at as handledAt, " +
            "c.content as commentContent, c.fragment_id as fragmentId, c.user_id as commenterId, u.nickname as commenterName " +
            "from comment_report r " +
            "left join comment c on r.comment_id = c.id " +   // 连评论表取内容
            "left join `user` u on c.user_id = u.id " +       // 连用户表取昵称
            "where r.status = 0 order by r.id asc")           // 只查待处理，最早的在最前
    List<CommentReport> selectPending();

    @Select("select id, comment_id as commentId, reporter_id as reporterId, reason, status, handled_at as handledAt " +
            "from comment_report where id = #{id}")
    CommentReport selectById(Long id);

    @Update("update comment_report set status = 1, handled_at = now() where id = #{id}")
    int markHandled(Long id);
}
```

`service/CommentService.java` + `impl`（新增，核心逻辑）：

```java
public interface CommentService {
    void add(Long userId, Long fragmentId, String content);                 // 发评论（免审核）
    PageBean<CommentVO> listByFragment(Long userId, Long fragmentId, int pageNum, int pageSize); // 列表
    void deleteOwn(Long userId, Long commentId);                            // 删自己的评论（硬删除）
    void report(Long userId, Long commentId, String reason);                // 举报（理由必填、防重复）
    List<CommentReport> listPendingReports();                               // 管理端待处理举报
    CommentReport getReport(Long reportId);                                 // 单条举报（处理前取详情）
    void deleteCommentByAdmin(Long commentId);                              // 管理端下架（硬删除）
    void markReportHandled(Long reportId);                                  // 标记举报已处理
}
```

```java
@Service
public class CommentServiceImpl implements CommentService {
    @Autowired private CommentMapper commentMapper;         // 评论表
    @Autowired private CommentReportMapper reportMapper;    // 举报表
    @Autowired private UserMapper userMapper;               // 查评论者昵称/头像
    @Autowired private StoryFragmentMapper fragmentMapper;  // 校验碎片存在

    @Override
    public void add(Long userId, Long fragmentId, String content) {
        content = content == null ? "" : content.trim();    // 先判空再 trim，防空指针
        if (content.isEmpty()) {                            // 空内容直接拒绝
            throw new BusinessException(ResultCode.COMMENT_EMPTY);
        }
        if (content.length() > 100) {                       // 100 字上限（Q5）
            throw new BusinessException(ResultCode.COMMENT_TOO_LONG);
        }
        if (fragmentMapper.selectById(fragmentId) == null) {// 碎片不存在就拒绝，防止挂空评论
            throw new BusinessException(ResultCode.FRAGMENT_NOT_PUBLISHED);
        }
        Comment c = new Comment();                          // 组装评论对象
        c.setFragmentId(fragmentId);                        // 所属碎片
        c.setUserId(userId);                                // 评论者
        c.setContent(content);                              // 内容
        commentMapper.insert(c);                            // 免审核：直接落库即显示
    }

    @Override
    public PageBean<CommentVO> listByFragment(Long userId, Long fragmentId, int pageNum, int pageSize) {
        PageHelper.startPage(pageNum, pageSize);            // 开启分页
        List<Comment> list = commentMapper.selectByFragment(fragmentId); // 正序查询
        Page<Comment> page = (Page<Comment>) list;          // 拿 total
        List<CommentVO> vos = list.stream().map(c -> {      // 每条评论转 VO
            CommentVO vo = new CommentVO();                 // 新建展示对象
            vo.setId(c.getId());                            // 评论 id
            vo.setContent(c.getContent());                  // 内容
            User author = userMapper.selectById(c.getUserId()); // 查评论者
            vo.setAuthorName(author == null ? "已注销" : author.getNickname()); // 昵称（防空）
            vo.setAuthorAvatar(author == null ? null : author.getAvatar());     // 头像
            vo.setMine(userId != null && userId.equals(c.getUserId())); // 是否自己的评论
            return vo;                                      // 返回组装好的 VO
        }).collect(Collectors.toList());                    // 收集成 List
        return new PageBean<>(page.getTotal(), vos);        // 返回分页结果
    }

    @Override
    public void deleteOwn(Long userId, Long commentId) {
        Comment c = commentMapper.selectById(commentId);    // 先查评论
        if (c == null) {                                    // 不存在（已被删）直接返回成功，幂等
            return;
        }
        if (!c.getUserId().equals(userId)) {                // 不是自己的不能删
            throw new BusinessException(ResultCode.NOT_AUTHOR);
        }
        commentMapper.deleteById(commentId);                // 硬删除（弹窗确认，不可恢复）
    }

    @Override
    public void report(Long userId, Long commentId, String reason) {
        reason = reason == null ? "" : reason.trim();       // 理由去空格
        if (reason.isEmpty()) {                             // 理由必填（Q6）
            throw new BusinessException(ResultCode.REPORT_REASON_REQUIRED);
        }
        if (commentMapper.selectById(commentId) == null) {  // 评论不存在
            throw new BusinessException(ResultCode.COMMENT_NOT_FOUND);
        }
        if (reportMapper.selectByCommentAndReporter(commentId, userId) != null) { // 防重复举报
            throw new BusinessException(ResultCode.REPORT_DUPLICATE);
        }
        CommentReport r = new CommentReport();              // 组装举报记录
        r.setCommentId(commentId);                          // 被举报评论
        r.setReporterId(userId);                            // 举报人
        r.setReason(reason);                                // 理由
        reportMapper.insert(r);                             // 进待处理队列
    }

    @Override
    public List<CommentReport> listPendingReports() { return reportMapper.selectPending(); }
    @Override
    public CommentReport getReport(Long reportId) {
        CommentReport r = reportMapper.selectById(reportId); // 查举报
        if (r == null) throw new BusinessException(ResultCode.COMMENT_NOT_FOUND); // 不存在抛 404
        return r;
    }
    @Override
    public void deleteCommentByAdmin(Long commentId) { commentMapper.deleteById(commentId); } // 下架=硬删除
    @Override
    public void markReportHandled(Long reportId) { reportMapper.markHandled(reportId); }
}
```

`controller/CommentController.java`（新增；GET 列表游客可看，AuthInterceptor 按 GET 放行，POST 需登录）：

```java
@RestController
@RequestMapping("/api/comments")
public class CommentController {
    @Autowired private CommentService commentService; // 评论业务

    @PostMapping
    public Result add(@RequestBody CommentRequest req) { // 发评论
        commentService.add(UserContext.getUserId(), req.getFragmentId(), req.getContent());
        return Result.success();
    }

    @GetMapping
    public Result list(@RequestParam Long fragmentId,
                       @RequestParam(defaultValue = "1") int pageNum,
                       @RequestParam(defaultValue = "5") int pageSize) {
        return Result.success(commentService.listByFragment(UserContext.tryGetUserId(), fragmentId, pageNum, pageSize));
    }

    @DeleteMapping("/{id}")
    public Result delete(@PathVariable Long id) { // 删自己的评论
        commentService.deleteOwn(UserContext.getUserId(), id);
        return Result.success();
    }

    @PostMapping("/{id}/report")
    public Result report(@PathVariable Long id, @RequestBody Map<String, String> body) { // 举报
        commentService.report(UserContext.getUserId(), id, body.get("reason"));
        return Result.success();
    }
}
```

`controller/AdminCommentController.java`（新增；管理员+站长）：

```java
@RestController
@RequestMapping("/api/admin/comments")
public class AdminCommentController {
    @Autowired private CommentService commentService; // 评论业务
    @Autowired private AdminService adminService;     // 封禁用户复用现有逻辑

    @GetMapping("/reports")
    @RequireRole({User.ROLE_ADMIN, User.ROLE_OWNER}) // 管理员/站长可看举报
    public Result reports() { return Result.success(commentService.listPendingReports()); }

    @PostMapping("/reports/{id}/handle")
    @RequireRole({User.ROLE_ADMIN, User.ROLE_OWNER})
    public Result handle(@PathVariable Long id, @RequestBody HandleReportRequest req) {
        CommentReport r = commentService.getReport(id);      // 先取举报详情（含评论者 id）
        String action = req.getAction();                     // dismiss=不下架 / delete=下架 / ban=封禁用户
        if ("delete".equals(action) || "ban".equals(action)) {
            commentService.deleteCommentByAdmin(r.getCommentId()); // 下架评论（硬删除）
        }
        if ("ban".equals(action)) {                          // 封禁评论用户
            adminService.banUser(r.getCommenterId(), req.getBanDays(), req.getBanReason());
        }
        commentService.markReportHandled(id);                // 标记举报已处理
        return Result.success();
    }
}
```

`AuthInterceptor` 调整：`GET /api/comments` 游客放行（参照现有 GET /api/fragments 的判断逻辑）；`ResultCode` 新增：`COMMENT_EMPTY / COMMENT_TOO_LONG / COMMENT_NOT_FOUND / REPORT_DUPLICATE / REPORT_REASON_REQUIRED / USER_NOT_FOUND`；新增请求体 `CommentRequest{fragmentId,content}`、`HandleReportRequest{action,banDays,banReason}`。

---

### 版本 V3.0（待实施，2026-08-14 后端代码已先写入）

### Task 27（V3.0）: 图片上传（碎片与评论均可带图）

- 需求：碎片可带图（随故事审核，通过才上墙）；评论可带图（**不审核**，发布即显示）；排版注意图文混排
- 前端（只描述不写代码）：发布框/评论框图片选择与预览、FragmentCard/评论图文排版、卡片图片比例自适应

**后端代码（先写，确认后实现）：**

`resources/db/upgrade_v3.0.sql`：

```sql
ALTER TABLE story_fragment ADD COLUMN image VARCHAR(255) DEFAULT NULL COMMENT '碎片图片URL（可空，随碎片审核）';
ALTER TABLE comment ADD COLUMN image VARCHAR(255) DEFAULT NULL COMMENT '评论图片URL（可空，不审核）';
```

`service/UploadService.java` + impl（新增，抽公共上传，头像/公告/碎片/评论共用）：

```java
@Service
public class UploadServiceImpl implements UploadService {
    @Value("${app.upload.avatar-dir}") private String uploadDir; // 统一上传目录

    @Override
    public String saveImage(MultipartFile file, String prefix, long maxSize) {
        String contentType = file == null ? null : file.getContentType(); // 先判空再取类型
        if (contentType == null || !List.of("image/jpeg","image/png","image/webp","image/gif").contains(contentType)) {
            throw new BusinessException(ResultCode.BAD_REQUEST, "图片只支持 jpg/png/webp/gif"); // 类型白名单
        }
        if (file.getSize() > maxSize) { // 大小限制（碎片/评论可传不同上限）
            throw new BusinessException(ResultCode.BAD_REQUEST, "图片不能超过 " + maxSize / 1024 / 1024 + "MB");
        }
        String fileName = prefix + "_" + System.currentTimeMillis() + "_" + (int)(Math.random() * 10000) + ".png"; // 唯一文件名
        File dir = new File(uploadDir);           // 目录对象
        if (!dir.exists()) dir.mkdirs();          // 不存在就建
        File target = new File(dir, fileName);    // 目标文件
        try {
            BufferedImage src = ImageIO.read(file.getInputStream()); // 读图片
            if (src == null) throw new BusinessException(ResultCode.BAD_REQUEST, "无法解析图片"); // 不是真图片
            ImageIO.write(src, "png", target);    // 写盘
        } catch (IOException e) {
            throw new BusinessException(ResultCode.INTERNAL_ERROR, "图片保存失败");
        }
        return "/uploads/" + fileName;            // 返回可访问 URL
    }
}
```

`FragmentSubmitRequest` 加 `image` 字段、`StoryFragment` 加 `image` 字段、`FragmentVO` 加 `image` 字段；`Comment`/`CommentVO` 加 `image` 字段。发布/评论接口接收 `image` 存库；碎片审核通过机制（status=1）天然保证「图随文审核」；评论 `image` 直接存、发布即显示。

- 验收：碎片带图发布 → 待审核 → 通过后图文上墙；评论带图发布即显示；图片过大/类型不对被拒；手机端图文排版正常

## Self-Review（已执行）

- **Spec coverage**：制定计划.md 的 F1-F8、A1-A4、生命周期、权限矩阵、安全项、里程碑全部有对应 Task（Task 1-18 逐一对应）。
- **Placeholder scan**：无 TBD/TODO；所有代码步骤均含实际代码与运行命令；Task 9 的 `updateAdminCode` 占位体已在同一任务内替换为完整实现。
- **Type consistency**：`deleteByFragmentAndUser(fragmentId, userId)` 允许 userId=null 并在 XML 用 `<if>` 支持；`FragmentService` 各方法签名在 Task 7 定义并被 Task 9 复用；`@RequireRole` 与 `User` 角色常量一致；前端 `userStore`/`request`/API 模块签名在 Task 11 定义并被 Task 12-15 复用。

## Execution Handoff

计划已保存。两种执行方式：

1. **Subagent-Driven（推荐）**：每个 Task 派一个全新子代理实现，任务之间做两阶段评审，迭代快
2. **Inline Execution**：在当前会话用 executing-plans 批量执行，设置检查点逐批评审

选择哪种方式？（执行阶段会先把工作区初始化为 git 仓库，便于每个 Task 的提交步骤生效）
